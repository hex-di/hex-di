// Auto-generated by scripts/bundle-playground-types.ts
// Package: @hex-di/query
export const dts = `declare module "@hex-di/query" {
/**
 * A branded port type that serves as a compile-time contract for a service interface.
 *
 * The Port type uses TypeScript's structural typing with a branded property
 * to achieve nominal typing. Two ports are only compatible if they have:
 * 1. The same service interface type \`T\`
 * 2. The same port name \`TName\`
 *
 * @typeParam TName - The literal string type for the port name (defaults to \`string\`)
 * @typeParam T - The service interface type (phantom type - exists only at compile time)
 *
 * @remarks
 * - The \`__brand\` property carries both the service type and name in a tuple
 * - The \`__portName\` property exposes the name for debugging and error messages
 * - Both properties are readonly to prevent modification
 * - The brand symbol value is \`undefined\` at runtime (zero overhead)
 *
 * @see {@link createPort} - Factory function to create port tokens
 * @see {@link InferService} - Utility to extract the service type from a port
 * @see {@link InferPortName} - Utility to extract the name type from a port
 *
 * @example Direct type usage
 * \`\`\`typescript
 * interface Logger {
 *   log(message: string): void;
 * }
 *
 * // Create typed port tokens
 * type ConsoleLoggerPort = Port<'ConsoleLogger', Logger>;
 * type FileLoggerPort = Port<'FileLogger', Logger>;
 *
 * // These are type-incompatible despite same interface
 * declare const consolePort: ConsoleLoggerPort;
 * declare const filePort: FileLoggerPort;
 * // consolePort = filePort; // Type error!
 * \`\`\`
 *
 * @example With createPort (recommended)
 * \`\`\`typescript
 * // Use createPort for value + type duality
 * const LoggerPort = createPort<'Logger', Logger>('Logger');
 * type LoggerPortType = typeof LoggerPort;
 * \`\`\`
 */
export type Port<TName extends string, T> = {
	/**
	 * Brand property for nominal typing.
	 * Contains a tuple of [PortName, ServiceType] at the type level.
	 * Value is undefined at runtime.
	 */
	readonly __brand: [
		TName,
		T
	];
	/**
	 * The port name, exposed for debugging and error messages.
	 */
	readonly __portName: TName;
};
/**
 * Error type returned when type inference utilities receive a non-Port type.
 *
 * This branded error provides actionable guidance in IDE tooltips when users
 * accidentally pass incorrect types to InferService or InferPortName.
 *
 * @typeParam T - The type that was passed (preserved for debugging)
 *
 * @remarks
 * Instead of returning opaque \`never\`, this error type provides:
 * - Clear error message explaining what was expected
 * - The actual type that was received (for debugging)
 * - A hint about common mistakes (e.g., forgetting \`typeof\`)
 *
 * @example IDE tooltip when misused
 * \`\`\`typescript
 * type Result = InferService<string>;
 * // Hovering shows:
 * // {
 * //   __errorBrand: "NotAPortError";
 * //   __message: "Expected a Port type created with createPort() or port()";
 * //   __received: string;
 * //   __hint: "Use InferService<typeof YourPort>, not InferService<YourPort>";
 * // }
 * \`\`\`
 */
export type NotAPortError<T> = {
	readonly __errorBrand: "NotAPortError";
	readonly __message: "Expected a Port type created with createPort() or port()";
	readonly __received: T;
	readonly __hint: "Use InferService<typeof YourPort>, not InferService<YourPort>";
};
/**
 * Extracts the service interface type from a Port type.
 *
 * This utility type uses conditional type inference to extract the phantom
 * type parameter \`T\` from a \`Port<TName, T>\`. If the provided type is not
 * a valid Port, it returns a descriptive \`NotAPortError\` type.
 *
 * @typeParam P - The Port type to extract the service from
 * @returns The service interface type \`T\`, or \`NotAPortError<P>\` if P is not a Port
 *
 * @see {@link Port} - The port type this utility extracts from
 * @see {@link InferPortName} - Companion utility to extract the port name
 * @see {@link createPort} - Factory function to create port tokens
 * @see {@link NotAPortError} - The error type returned for invalid inputs
 *
 * @example Successful extraction
 * \`\`\`typescript
 * interface Logger {
 *   log(message: string): void;
 * }
 *
 * const LoggerPort = createPort<'Logger', Logger>('Logger');
 * type LoggerService = InferService<typeof LoggerPort>;
 * // LoggerService = Logger
 * \`\`\`
 *
 * @example Error case (descriptive error type)
 * \`\`\`typescript
 * type Invalid = InferService<string>;
 * // Invalid = NotAPortError<string>
 * // IDE shows: { __errorBrand: "NotAPortError", __message: "Expected a Port...", ... }
 * \`\`\`
 */
export type InferService<P> = P extends Port<infer _TName, infer T> ? T : NotAPortError<P>;
/**
 * Extracts the port name literal type from a Port type.
 *
 * This utility type uses conditional type inference to extract the name
 * type parameter \`TName\` from a \`Port<TName, T>\`. If the provided type is not
 * a valid Port, it returns a descriptive \`NotAPortError\` type.
 *
 * @typeParam P - The Port type to extract the name from
 * @returns The port name literal type \`TName\`, or \`NotAPortError<P>\` if P is not a Port
 *
 * @see {@link Port} - The port type this utility extracts from
 * @see {@link InferService} - Companion utility to extract the service type
 * @see {@link createPort} - Factory function to create port tokens
 * @see {@link NotAPortError} - The error type returned for invalid inputs
 *
 * @example Successful extraction
 * \`\`\`typescript
 * interface Logger {
 *   log(message: string): void;
 * }
 *
 * const LoggerPort = createPort<'Logger', Logger>('Logger');
 * type PortName = InferPortName<typeof LoggerPort>;
 * // PortName = 'Logger'
 * \`\`\`
 *
 * @example Error case (descriptive error type)
 * \`\`\`typescript
 * type Invalid = InferPortName<number>;
 * // Invalid = NotAPortError<number>
 * // IDE shows: { __errorBrand: "NotAPortError", __message: "Expected a Port...", ... }
 * \`\`\`
 */
export type InferPortName<P> = P extends Port<infer TName, infer _T> ? TName : NotAPortError<P>;
declare const __metadataKey: unique symbol;
/**
 * Discriminator for hexagonal architecture port types.
 *
 * - \`'inbound'\` - Driving ports (use cases, primary adapters)
 * - \`'outbound'\` - Driven ports (infrastructure, secondary adapters)
 *
 * @see {@link DirectedPort} - The port type that carries direction
 * @see {@link createInboundPort} - Factory for inbound ports
 * @see {@link createOutboundPort} - Factory for outbound ports
 */
export type PortDirection = "inbound" | "outbound";
/**
 * Optional metadata for documenting ports.
 *
 * Metadata provides human-readable documentation that can be used for:
 * - IDE tooltips and documentation
 * - Dependency graph visualization
 * - API documentation generation
 *
 * All properties are optional and readonly.
 *
 * @example
 * \`\`\`typescript
 * const UserServicePort = createInboundPort<'UserService', UserService>({
 *   name: 'UserService',
 *   description: 'Handles user CRUD operations',
 *   category: 'domain',
 *   tags: ['user', 'crud', 'core'],
 * });
 * \`\`\`
 */
export interface PortMetadata {
	/** Human-readable description of the port's purpose */
	readonly description?: string;
	/** Categorical grouping for organization */
	readonly category?: string;
	/** Searchable tags for filtering and discovery */
	readonly tags?: readonly string[];
}
/**
 * A port with hexagonal architecture direction and optional metadata.
 *
 * DirectedPort extends the base Port type with additional type-level branding:
 * - \`TDirection\` - Whether this is an inbound (driving) or outbound (driven) port
 * - Metadata association for documentation purposes
 *
 * DirectedPorts are structurally compatible with base Ports, enabling gradual
 * adoption without breaking existing code.
 *
 * @typeParam TName - The literal string type for the port name
 * @typeParam TService - The service interface type (phantom type)
 * @typeParam TDirection - 'inbound' or 'outbound'
 *
 * @see {@link InboundPort} - Convenience alias for inbound directed ports
 * @see {@link OutboundPort} - Convenience alias for outbound directed ports
 * @see {@link createInboundPort} - Factory for creating inbound ports
 * @see {@link createOutboundPort} - Factory for creating outbound ports
 *
 * @example
 * \`\`\`typescript
 * // DirectedPort is assignable to Port (backward compatible)
 * const port: DirectedPort<'Logger', Logger, 'inbound'> = createInboundPort({
 *   name: 'Logger',
 * });
 * const basePort: Port<'Logger', Logger> = port; // OK
 * \`\`\`
 */
export type DirectedPort<TName extends string, TService, TDirection extends PortDirection, TCategory extends string = string> = Port<TName, TService> & {
	readonly __directionBrand: TDirection;
	readonly __metadataKey: PortMetadata;
	readonly __categoryBrand: TCategory;
};
/**
 * Converts a tuple/array type to a union of its element types.
 *
 * @typeParam T - A tuple or array type
 * @returns Union of all element types, or \`never\` for empty array
 */
export type TupleToUnion<T extends readonly Port<string, unknown>[]> = T extends readonly [
] ? never : T[number];
/**
 * A descriptive error type that replaces silent \`never\` in inference contexts.
 *
 * ## Purpose
 *
 * When type inference fails, TypeScript typically returns \`never\`. This is
 * problematic because:
 *
 * 1. **Silent failures**: \`never\` disappears in unions (\`A | never = A\`)
 * 2. **Unclear errors**: IDE tooltips just show \`never\` with no context
 * 3. **Debugging difficulty**: Hard to trace why a type became \`never\`
 *
 * \`InferenceError\` provides a structured alternative that:
 * - Shows in IDE tooltips with source and message
 * - Doesn't disappear in unions (it's an object type, not \`never\`)
 * - Includes the problematic input for debugging
 *
 * @typeParam TSource - Identifier for where the error originated (e.g., type name)
 * @typeParam TMessage - Human-readable error description
 * @typeParam TInput - The problematic input that caused the inference failure
 *
 * @example Replacing silent never
 * \`\`\`typescript
 * // Before: Returns never silently
 * type ExtractName<T> = T extends { name: infer N } ? N : never;
 *
 * // After: Returns descriptive error
 * type ExtractName<T> = T extends { name: infer N }
 *   ? N
 *   : InferenceError<"ExtractName", "Input must have a 'name' property", T>;
 * \`\`\`
 */
export type InferenceError<TSource extends string, TMessage extends string, TInput = unknown> = {
	readonly __inferenceError: true;
	readonly __source: TSource;
	readonly __message: TMessage;
	readonly __input: TInput;
};
/**
 * Discriminator for sync vs async factory functions.
 */
export type FactoryKind = "sync" | "async";
/**
 * Lifetime scope for an adapter's service instance.
 *
 * | Lifetime    | Description                                                      |
 * |-------------|------------------------------------------------------------------|
 * | \`singleton\` | One instance per container, shared across all resolutions        |
 * | \`scoped\`    | One instance per scope, isolated from parent and sibling scopes  |
 * | \`transient\` | New instance on every resolution                                 |
 */
export type Lifetime = "singleton" | "scoped" | "transient";
/**
 * Type representing empty dependencies (no required ports).
 *
 * This is a branded empty type that:
 * - IS assignable from empty object literals: \`const deps: EmptyDeps = {}\` ✓
 * - PREVENTS arbitrary key access: \`deps.nonExistent\` ✗ (type error)
 *
 * The brand uses \`?: never\` which means the property can only be satisfied
 * by omitting it entirely (since \`never\` has no values). This makes \`{}\`
 * assignable while preventing index access.
 */
export type EmptyDeps = {
	readonly __emptyDepsBrand?: never;
};
/**
 * Maps a union of Port types to an object type for dependency injection.
 *
 * When \`TRequires\` is \`never\` (no dependencies), returns \`EmptyDeps\` which
 * is a branded empty type that prevents arbitrary key access.
 *
 * When \`TRequires\` is a Port union, returns an object type with port names
 * as keys and service types as values.
 *
 * @example
 * \`\`\`typescript
 * // No dependencies - returns EmptyDeps (branded empty type)
 * type NoDeps = ResolvedDeps<never>;
 * const deps: NoDeps = {}; // ✓ OK
 * deps.anything; // ✗ Error: Property 'anything' does not exist
 *
 * // With dependencies - returns mapped object type
 * type WithDeps = ResolvedDeps<typeof LoggerPort | typeof DatabasePort>;
 * // { Logger: Logger; Database: Database }
 * \`\`\`
 */
export type ResolvedDeps<TRequires> = [
	TRequires
] extends [
	never
] ? EmptyDeps : {
	[TPort in TRequires as InferPortName<TPort> & string]: InferService<TPort>;
};
/**
 * Maps a tuple of Port types to a typed dependencies object.
 *
 * This is the canonical type for expressing "these ports -> this deps object"
 * across all HexDI libraries (store, flow, query). It composes \`TupleToUnion\`
 * and \`ResolvedDeps\` to convert a readonly tuple of ports into a keyed object
 * where port names are keys and service types are values.
 *
 * For empty tuples, returns \`EmptyDeps\` (branded empty type that prevents
 * arbitrary key access).
 *
 * @typeParam TRequires - A readonly tuple of Port types
 *
 * @example
 * \`\`\`typescript
 * const LoggerPort = createPort<Logger>('Logger');
 * const DbPort = createPort<Database>('Database');
 *
 * type Deps = PortDeps<readonly [typeof LoggerPort, typeof DbPort]>;
 * // { Logger: Logger; Database: Database }
 *
 * type NoDeps = PortDeps<readonly []>;
 * // EmptyDeps
 * \`\`\`
 */
export type PortDeps<TRequires extends readonly Port<string, unknown>[]> = ResolvedDeps<TupleToUnion<TRequires>>;
/**
 * A branded adapter type that captures the complete contract for a service implementation.
 *
 * ## Type Parameter Constraints
 *
 * | Parameter     | Constraint           | Purpose                                    |
 * |---------------|----------------------|--------------------------------------------|
 * | \`TProvides\`   | \`out\` (covariant)    | Port type this adapter implements          |
 * | \`TRequires\`   | invariant            | Union of required port types               |
 * | \`TLifetime\`   | \`extends Lifetime\`   | Declared lifetime (normalized for async) |
 * | \`TFactoryKind\`| \`extends FactoryKind\`| Must be "sync" | "async"                   |
 * | \`TClonable\`   | \`extends boolean\`    | Literal \`true\` or \`false\`                  |
 * | \`TRequiresTuple\`| derived            | Computed from TRequires for runtime array  |
 *
 * ## Variance Annotations
 *
 * The \`out\` modifier on \`TProvides\`, \`TFactoryKind\`, \`TClonable\`, and \`TRequiresTuple\`
 * marks them as covariant-only. This means:
 * - \`Adapter<DerivedPort, ...>\` is assignable to \`Adapter<BasePort, ...>\`
 * - Enables correct subtyping for adapters in collections
 *
 * \`TRequires\` is invariant (no modifier) because:
 * - The factory function both reads (contravariant) and returns (covariant) based on it
 * - Changing \`TRequires\` changes the factory signature bidirectionally
 *
 * ## Example
 *
 * \`\`\`typescript
 * // Full type signature of an adapter:
 * type LoggerAdapter = Adapter<
 *   typeof LoggerPort,  // TProvides: the port being implemented
 *   never,              // TRequires: no dependencies (never = empty union)
 *   "singleton",        // TLifetime: instance lifetime
 *   "sync",             // TFactoryKind: synchronous factory
 *   false,              // TClonable: not safe to shallow-clone
 *   readonly [],        // TRequiresTuple: empty tuple (computed from TRequires)
 *   never               // TError: no error channel (infallible factory)
 * >;
 * \`\`\`
 */
export type Adapter<out TProvides, TRequires, TLifetime extends string, // Widened to accept error message strings
out TFactoryKind extends FactoryKind = "sync", out TClonable extends boolean = false, out TRequiresTuple extends readonly unknown[] = [
	TRequires
] extends [
	never
] ? readonly [
] : readonly TRequires[], out TError = never> = {
	/**
	 * Brand property for nominal typing.
	 */
	readonly __adapterBrand?: [
		TProvides,
		TRequires,
		TLifetime,
		TFactoryKind,
		TClonable,
		TError
	];
	/**
	 * The port this adapter provides/implements.
	 */
	readonly provides: TProvides;
	/**
	 * The ports this adapter depends on.
	 */
	readonly requires: TRequiresTuple;
	/**
	 * The lifetime scope for this adapter's service instances.
	 *
	 * For sync factories, this is the declared lifetime.
	 * For async factories, this is either "singleton" (valid) or an error message
	 * string (invalid - when non-singleton lifetime was specified with async factory).
	 *
	 * The error string makes the adapter unusable with GraphBuilder, producing
	 * a compile-time error.
	 */
	readonly lifetime: TLifetime;
	/**
	 * The factory kind discriminator.
	 */
	readonly factoryKind: TFactoryKind;
	/**
	 * Factory function that creates the service instance.
	 */
	readonly factory: TFactoryKind extends "async" ? (deps: ResolvedDeps<TRequires>) => Promise<InferService<TProvides>> : (deps: ResolvedDeps<TRequires>) => InferService<TProvides>;
	/**
	 * Whether this adapter's service can be safely shallow-cloned.
	 *
	 * When \`true\`, the adapter's instances can be used with forked inheritance mode,
	 * which creates a shallow clone for child containers.
	 *
	 * When \`false\` (default), forked inheritance mode will fail at compile time,
	 * requiring the use of shared or isolated mode instead.
	 *
	 * @remarks
	 * Mark as clonable only for services that:
	 * - Have no resource handles (sockets, file handles, connections)
	 * - Have no external references that would become shared
	 * - Are value-like objects where shallow cloning produces valid instances
	 *
	 * @default false
	 */
	readonly clonable: TClonable;
	/**
	 * Optional finalizer function called during disposal.
	 */
	finalizer?(instance: InferService<TProvides>): void | Promise<void>;
};
/**
 * Structural interface matching ANY Adapter without using \`any\`.
 *
 * This uses TypeScript's variance rules to create a type that ALL Adapters
 * are assignable to:
 * - \`unknown\` in covariant positions (outputs/reads)
 * - \`never\` in contravariant positions (inputs/writes)
 *
 * When used as a constraint \`<A extends AdapterConstraint>\`, the generic parameter \`A\`
 * preserves the EXACT adapter type for full inference.
 *
 * @example
 * \`\`\`typescript
 * // All adapters match this constraint
 * function process<A extends AdapterConstraint>(adapter: A): InferAdapterProvides<A> {
 *   // A is inferred as exact adapter type, not widened to AdapterConstraint
 * }
 *
 * const result = process(LoggerAdapter);
 * // result is LoggerPort, not unknown!
 * \`\`\`
 *
 * @remarks
 * This follows the Effect-TS pattern of \`Layer.Any\`, \`Service.Any\` interfaces.
 * The key insight is that we only match the STRUCTURE of an Adapter, not its
 * exact type parameters, while preserving full type inference through generics.
 */
export interface AdapterConstraint {
	/**
	 * The port this adapter provides (read-only, covariant).
	 * Uses Port<string, unknown> as the widest Port type.
	 */
	readonly provides: Port<string, unknown>;
	/**
	 * The ports this adapter depends on (read-only, covariant).
	 * Each element is a Port with \`__portName\` for runtime identification.
	 */
	readonly requires: readonly Port<string, unknown>[];
	/**
	 * The lifetime scope (fixed union, all values assignable).
	 */
	readonly lifetime: Lifetime;
	/**
	 * The factory kind discriminator (fixed union, all values assignable).
	 */
	readonly factoryKind: FactoryKind;
	/**
	 * Factory function (contravariant in params, covariant in return).
	 * \`never[]\` params accept any function signature.
	 */
	readonly factory: (...args: never[]) => unknown;
	/**
	 * Whether this adapter's service can be safely shallow-cloned.
	 * Defaults to false for safety.
	 */
	readonly clonable: boolean;
	/**
	 * Optional finalizer (contravariant param accepts any instance type).
	 */
	finalizer?(instance: never): void | Promise<void>;
}
/**
 * Event emitted by a library inspector.
 *
 * Wraps library-specific events with source identification for the
 * container's unified event stream.
 */
export interface LibraryEvent {
	/** Library name (matches LibraryInspector.name) */
	readonly source: string;
	/** Library-specific event type string */
	readonly type: string;
	/** Library-specific event payload */
	readonly payload: Readonly<Record<string, unknown>>;
	/** Event timestamp (Date.now()) */
	readonly timestamp: number;
}
/**
 * Listener callback for library inspector events.
 */
export type LibraryEventListener = (event: LibraryEvent) => void;
/**
 * Protocol that all library inspectors implement to participate
 * in the container's unified inspection system.
 *
 * Libraries implement this interface to make their domain-specific
 * inspection data discoverable and queryable through the container.
 */
export interface LibraryInspector {
	/** Unique library identifier. Must be lowercase kebab-case. */
	readonly name: string;
	/**
	 * Returns a frozen snapshot of the library's current state.
	 * The shape is library-specific but always a frozen record.
	 */
	getSnapshot(): Readonly<Record<string, unknown>>;
	/**
	 * Subscribe to library-specific events.
	 * Returns an unsubscribe function.
	 * Optional -- libraries without push-based events omit this.
	 */
	subscribe?(listener: LibraryEventListener): () => void;
	/**
	 * Dispose the library inspector and clean up resources.
	 * Optional -- called when the container disposes.
	 */
	dispose?(): void;
}
declare const RESULT_BRAND: unique symbol;
declare const OPTION_BRAND: unique symbol;
/**
 * The presence variant of \`Option\`. Represents a value that exists.
 *
 * \`Some<T>\` carries a \`value\` of type \`T\` and provides transformation,
 * chaining, extraction, and conversion methods. Instances are created
 * via the \`some()\` factory and are frozen plain objects branded with \`OPTION_BRAND\`.
 *
 * @example
 * \`\`\`ts
 * import { some } from '@hex-di/result';
 *
 * const opt = some(42);
 * opt.isSome();      // true
 * opt.map(x => x * 2); // Some(84)
 * opt.unwrapOr(0);   // 42
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/behaviors/09-option.md — BEH-09-002
 */
export interface Some<T> {
	readonly _tag: "Some";
	readonly value: T;
	readonly [OPTION_BRAND]: true;
	isSome(): this is Some<T>;
	isNone(): this is None;
	isSomeAnd(predicate: (value: T) => boolean): boolean;
	map<U>(f: (value: T) => U): Some<U>;
	filter(predicate: (value: T) => boolean): Option\$1<T>;
	flatten<U>(this: Some<Option\$1<U>>): Option\$1<U>;
	zip<U>(other: Option\$1<U>): Option\$1<[
		T,
		U
	]>;
	zipWith<U, R>(other: Option\$1<U>, f: (a: T, b: U) => R): Option\$1<R>;
	andThen<U>(f: (value: T) => Option\$1<U>): Option\$1<U>;
	orElse(f: () => Option\$1<T>): Some<T>;
	match<A, B>(onSome: (value: T) => A, onNone: () => B): A;
	unwrapOr<U>(defaultValue: U): T;
	expect(message: string): T;
	toResult<E>(onNone: () => E): Ok<T, E>;
	toNullable(): T;
	toUndefined(): T;
	toJSON(): {
		_tag: "Some";
		_schemaVersion: 1;
		value: T;
	};
	transpose<U, E>(this: Some<Result<U, E>>): Result<Some<U>, E>;
}
/**
 * The absence variant of \`Option\`. Represents the lack of a value.
 *
 * \`None\` carries no value and short-circuits all transformations and chaining operations.
 * Extraction methods return defaults or throw. A single frozen singleton is reused
 * for all \`none()\` calls.
 *
 * @example
 * \`\`\`ts
 * import { none } from '@hex-di/result';
 *
 * const opt = none();
 * opt.isNone();      // true
 * opt.map(x => x * 2); // None
 * opt.unwrapOr(0);   // 0
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/behaviors/09-option.md — BEH-09-003
 */
export interface None {
	readonly _tag: "None";
	readonly [OPTION_BRAND]: true;
	isSome(): this is Some<never>;
	isNone(): this is None;
	isSomeAnd(predicate: (value: never) => boolean): boolean;
	map<U>(f: (value: never) => U): None;
	filter(predicate: (value: never) => boolean): None;
	flatten(): None;
	zip<U>(other: Option\$1<U>): None;
	zipWith<U, R>(other: Option\$1<U>, f: (a: never, b: U) => R): None;
	andThen<U>(f: (value: never) => Option\$1<U>): None;
	orElse<T>(f: () => Option\$1<T>): Option\$1<T>;
	match<A, B>(onSome: (value: never) => A, onNone: () => B): B;
	unwrapOr<U>(defaultValue: U): U;
	expect(message: string): never;
	toResult<E>(onNone: () => E): Err<never, E>;
	toNullable(): null;
	toUndefined(): undefined;
	toJSON(): {
		_tag: "None";
		_schemaVersion: 1;
	};
	transpose(): Ok<None, never>;
}
type Option\$1<T> = Some<T> | None;
export interface ResultAsync<T, E> extends PromiseLike<Result<T, E>> {
	map<U>(f: (value: T) => U | Promise<U>): ResultAsync<U, E>;
	mapErr<F>(f: (error: E) => F | Promise<F>): ResultAsync<T, F>;
	mapBoth<U, F>(onOk: (value: T) => U | Promise<U>, onErr: (error: E) => F | Promise<F>): ResultAsync<U, F>;
	andThen<U, F>(f: (value: T) => Result<U, F> | ResultAsync<U, F>): ResultAsync<U, E | F>;
	orElse<U, F>(f: (error: E) => Result<U, F> | ResultAsync<U, F>): ResultAsync<T | U, F>;
	andTee(f: (value: T) => void | Promise<void>): ResultAsync<T, E>;
	orTee(f: (error: E) => void | Promise<void>): ResultAsync<T, E>;
	andThrough<F>(f: (value: T) => Result<unknown, F> | ResultAsync<unknown, F>): ResultAsync<T, E | F>;
	inspect(f: (value: T) => void): ResultAsync<T, E>;
	inspectErr(f: (error: E) => void): ResultAsync<T, E>;
	match<A, B>(onOk: (value: T) => A | Promise<A>, onErr: (error: E) => B | Promise<B>): Promise<A | B>;
	unwrapOr<U>(defaultValue: U): Promise<T | U>;
	unwrapOrElse<U>(f: (error: E) => U): Promise<T | U>;
	orDie(): Promise<T>;
	toNullable(): Promise<T | null>;
	toUndefined(): Promise<T | undefined>;
	intoTuple(): Promise<[
		null,
		T
	] | [
		E,
		null
	]>;
	merge(): Promise<T | E>;
	flatten<U>(this: ResultAsync<Result<U, E>, E>): ResultAsync<U, E>;
	flip(): ResultAsync<E, T>;
	toJSON(): Promise<{
		_tag: "Ok";
		_schemaVersion: 1;
		value: T;
	} | {
		_tag: "Err";
		_schemaVersion: 1;
		error: E;
	}>;
}
/**
 * The success variant of {@link Result}. Contains a \`value\` of type \`T\`.
 *
 * \`Ok\` is an interface, not a class -- instances are plain objects with closure-based methods
 * created by the {@link ok} factory function. The phantom type parameter \`E\` has no runtime
 * representation; the \`ok()\` factory produces \`Ok<T, never>\`, where \`never\` extends any \`E\`.
 *
 * @typeParam T - The type of the success value.
 * @typeParam E - The type of the error (phantom on Ok; defaults to \`never\` from the factory).
 *
 * @example
 * \`\`\`ts
 * import { ok } from '@hex-di/result';
 *
 * const result = ok(42);
 * console.log(result.isOk());  // true
 * console.log(result.value);   // 42
 * \`\`\`
 *
 * @since v1.0.0
 * @see {@link spec/result/behaviors/01-types-and-guards.md | BEH-01-004}
 */
export interface Ok<T, E> {
	readonly _tag: "Ok";
	readonly value: T;
	readonly [RESULT_BRAND]: true;
	isOk(): this is Ok<T, E>;
	isErr(): this is Err<T, E>;
	isOkAnd(predicate: (value: T) => boolean): boolean;
	isErrAnd(predicate: (error: E) => boolean): boolean;
	map<U>(f: (value: T) => U): Ok<U, E>;
	mapErr<F>(f: (error: E) => F): Ok<T, F>;
	mapBoth<U, F>(onOk: (value: T) => U, onErr: (error: E) => F): Ok<U, F>;
	flatten<U, E2>(this: Ok<Result<U, E2>, E>): Result<U, E | E2>;
	flip(): Err<E, T>;
	and<U, F>(other: Result<U, F>): Result<U, E | F>;
	or<U, F>(other: Result<U, F>): Ok<T, E>;
	andThen<U, F>(f: (value: T) => Result<U, F>): Result<U, E | F>;
	orElse<U, F>(f: (error: E) => Result<U, F>): Ok<T, E>;
	andTee(f: (value: T) => void): Ok<T, E>;
	orTee(f: (error: E) => void): Ok<T, E>;
	andThrough<F>(f: (value: T) => Result<unknown, F>): Result<T, E | F>;
	inspect(f: (value: T) => void): Ok<T, E>;
	inspectErr(f: (error: E) => void): Ok<T, E>;
	match<A, B>(onOk: (value: T) => A, onErr: (error: E) => B): A;
	unwrapOr<U>(defaultValue: U): T;
	unwrapOrElse<U>(f: (error: E) => U): T;
	mapOr<U>(defaultValue: U, f: (value: T) => U): U;
	mapOrElse<U>(defaultF: (error: E) => U, f: (value: T) => U): U;
	contains(value: T): boolean;
	containsErr(error: E): boolean;
	expect(message: string): T;
	expectErr(message: string): never;
	orDie(): T;
	toNullable(): T;
	toUndefined(): T;
	intoTuple(): [
		null,
		T
	];
	merge(): T;
	toAsync(): ResultAsync<T, E>;
	asyncMap<U>(f: (value: T) => Promise<U>): ResultAsync<U, E>;
	asyncAndThen<U, F>(f: (value: T) => ResultAsync<U, F>): ResultAsync<U, E | F>;
	toOption(): Some<T>;
	toOptionErr(): None;
	transpose<U>(this: Ok<Option\$1<U>, E>): Option\$1<Result<U, E>>;
	toJSON(): {
		_tag: "Ok";
		_schemaVersion: 1;
		value: T;
	};
	[Symbol.iterator](): Generator<never, T, unknown>;
}
/**
 * The failure variant of {@link Result}. Contains an \`error\` of type \`E\`.
 *
 * \`Err\` is an interface, not a class -- instances are plain objects with closure-based methods
 * created by the {@link err} factory function. The phantom type parameter \`T\` has no runtime
 * representation; the \`err()\` factory produces \`Err<never, E>\`, where \`never\` extends any \`T\`.
 *
 * @typeParam T - The type of the success value (phantom on Err; defaults to \`never\` from the factory).
 * @typeParam E - The type of the error.
 *
 * @example
 * \`\`\`ts
 * import { err } from '@hex-di/result';
 *
 * const result = err('not found');
 * console.log(result.isErr());  // true
 * console.log(result.error);    // 'not found'
 * \`\`\`
 *
 * @since v1.0.0
 * @see {@link spec/result/behaviors/01-types-and-guards.md | BEH-01-005}
 */
export interface Err<T, E> {
	readonly _tag: "Err";
	readonly error: E;
	readonly [RESULT_BRAND]: true;
	isOk(): this is Ok<T, E>;
	isErr(): this is Err<T, E>;
	isOkAnd(predicate: (value: T) => boolean): boolean;
	isErrAnd(predicate: (error: E) => boolean): boolean;
	map<U>(f: (value: T) => U): Err<U, E>;
	mapErr<F>(f: (error: E) => F): Err<T, F>;
	mapBoth<U, F>(onOk: (value: T) => U, onErr: (error: E) => F): Err<U, F>;
	flatten<U, E2>(this: Err<Result<U, E2>, E>): Err<U, E>;
	flip(): Ok<E, T>;
	and<U, F>(other: Result<U, F>): Err<U, E>;
	or<U, F>(other: Result<U, F>): Result<T | U, F>;
	andThen<U, F>(f: (value: T) => Result<U, F>): Err<U, E>;
	orElse<U, F>(f: (error: E) => Result<U, F>): Result<T | U, F>;
	andTee(f: (value: T) => void): Err<T, E>;
	orTee(f: (error: E) => void): Err<T, E>;
	andThrough<F>(f: (value: T) => Result<unknown, F>): Err<T, E>;
	inspect(f: (value: T) => void): Err<T, E>;
	inspectErr(f: (error: E) => void): Err<T, E>;
	match<A, B>(onOk: (value: T) => A, onErr: (error: E) => B): B;
	unwrapOr<U>(defaultValue: U): U;
	unwrapOrElse<U>(f: (error: E) => U): U;
	mapOr<U>(defaultValue: U, f: (value: T) => U): U;
	mapOrElse<U>(defaultF: (error: E) => U, f: (value: T) => U): U;
	contains(value: T): boolean;
	containsErr(error: E): boolean;
	expect(message: string): never;
	expectErr(message: string): E;
	orDie(): never;
	toNullable(): null;
	toUndefined(): undefined;
	intoTuple(): [
		E,
		null
	];
	merge(): E;
	toAsync(): ResultAsync<T, E>;
	asyncMap<U>(f: (value: T) => Promise<U>): ResultAsync<U, E>;
	asyncAndThen<U, F>(f: (value: T) => ResultAsync<U, F>): ResultAsync<U, E | F>;
	toOption(): None;
	toOptionErr(): Some<E>;
	transpose(): Some<Err<never, E>>;
	toJSON(): {
		_tag: "Err";
		_schemaVersion: 1;
		error: E;
	};
	[Symbol.iterator](): Generator<Err<never, E>, never, unknown>;
}
/**
 * A discriminated union of {@link Ok} and {@link Err}.
 *
 * \`Result<T, E>\` represents either a success (\`Ok\`) holding a value of type \`T\`,
 * or a failure (\`Err\`) holding an error of type \`E\`. TypeScript narrows to the
 * correct variant after checking \`_tag\`, \`isOk()\`, or \`isErr()\`.
 *
 * @typeParam T - The type of the success value.
 * @typeParam E - The type of the error value.
 *
 * @example
 * \`\`\`ts
 * import { ok, err, type Result } from '@hex-di/result';
 *
 * function divide(a: number, b: number): Result<number, string> {
 *   if (b === 0) return err('division by zero');
 *   return ok(a / b);
 * }
 *
 * const result = divide(10, 2);
 * if (result.isOk()) {
 *   console.log(result.value); // 5
 * }
 * \`\`\`
 *
 * @since v1.0.0
 * @see {@link spec/result/behaviors/01-types-and-guards.md | BEH-01-006}
 */
export type Result<T, E> = Ok<T, E> | Err<T, E>;
/**
 * Extracts the success type \`T\` from a \`Result<T, E>\` or \`ResultAsync<T, E>\`.
 *
 * @example
 * \`\`\`ts
 * import { ok } from '@hex-di/result';
 * import type { InferOk } from '@hex-di/result';
 *
 * type R = ReturnType<typeof ok<number>>;
 * type T = InferOk<R>; // number
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type InferOk<R> = R extends Result<infer T, unknown> ? T : R extends ResultAsync<infer T, unknown> ? T : never;
/**
 * Extracts the error type \`E\` from a \`Result<T, E>\` or \`ResultAsync<T, E>\`.
 *
 * @example
 * \`\`\`ts
 * import { err } from '@hex-di/result';
 * import type { InferErr } from '@hex-di/result';
 *
 * type R = ReturnType<typeof err<string>>;
 * type E = InferErr<R>; // string
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type InferErr<R> = R extends Result<unknown, infer E> ? E : R extends ResultAsync<unknown, infer E> ? E : never;
/**
 * Extracts the \`Ok\` types from a tuple of \`Result\` or \`ResultAsync\` values as a mapped tuple.
 *
 * @example
 * \`\`\`ts
 * import type { Result, InferOkTuple } from '@hex-di/result';
 *
 * type Rs = [Result<number, string>, Result<boolean, Error>];
 * type Oks = InferOkTuple<Rs>; // [number, boolean]
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type InferOkTuple<T extends readonly (Result<unknown, unknown> | ResultAsync<unknown, unknown>)[]> = {
	[K in keyof T]: InferOk<T[K]>;
};
/**
 * Extracts the union of all \`Err\` types from a tuple of \`Result\` or \`ResultAsync\` values.
 *
 * @example
 * \`\`\`ts
 * import type { Result, InferErrUnion } from '@hex-di/result';
 *
 * type Rs = [Result<number, "a">, Result<boolean, "b">];
 * type Errs = InferErrUnion<Rs>; // "a" | "b"
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type InferErrUnion<T extends readonly (Result<unknown, unknown> | ResultAsync<unknown, unknown>)[]> = InferErr<T[number]>;
/**
 * Extracts the union of \`Ok\` types from a tuple of \`Result\` or \`ResultAsync\` values.
 * Useful for \`Result.any\` which returns a single Ok from the first success.
 *
 * @example
 * \`\`\`ts
 * import type { Result, InferOkUnion } from '@hex-di/result';
 *
 * type Rs = [Result<number, string>, Result<boolean, string>];
 * type OkU = InferOkUnion<Rs>; // number | boolean
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type InferOkUnion<T extends readonly (Result<unknown, unknown> | ResultAsync<unknown, unknown>)[]> = InferOk<T[number]>;
/**
 * A non-empty array type that guarantees at least one element.
 * The first element is always present (\`T\`), followed by zero or more additional elements.
 *
 * @example
 * \`\`\`ts
 * import type { NonEmptyArray } from '@hex-di/result';
 *
 * const valid: NonEmptyArray<number> = [1, 2, 3]; // ok
 * // const invalid: NonEmptyArray<number> = [];    // type error
 *
 * function head<T>(arr: NonEmptyArray<T>): T {
 *   return arr[0]; // T, not T | undefined
 * }
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type NonEmptyArray<T> = [
	T,
	...T[]
];
/**
 * Extracts the \`Err\` types from a tuple of \`Result\` or \`ResultAsync\` values as a mapped tuple.
 * Useful for \`Result.any\` error collection.
 *
 * @example
 * \`\`\`ts
 * import type { Result, InferErrTuple } from '@hex-di/result';
 *
 * type Rs = [Result<number, "a">, Result<boolean, "b">];
 * type Errs = InferErrTuple<Rs>; // ["a", "b"]
 * \`\`\`
 *
 * @since v1.0.0
 * @see spec/result/type-system/utility.md
 */
export type InferErrTuple<T extends readonly (Result<unknown, unknown> | ResultAsync<unknown, unknown>)[]> = {
	[K in keyof T]: InferErr<T[K]>;
};
declare function all<R extends readonly Result<unknown, unknown>[]>(...results: R): Result<InferOkTuple<R>, InferErrUnion<R>>;
declare function allSettled<R extends readonly Result<unknown, unknown>[]>(...results: R): Result<InferOkTuple<R>, InferErrUnion<R>[]>;
declare function any<R extends readonly Result<unknown, unknown>[]>(...results: R): Result<InferOkUnion<R>, InferErrTuple<R>>;
export type InferErrUnionFromRecord<R extends Record<string, Result<unknown, unknown>>> = InferErr<R[keyof R]>;
declare function collect<R extends Record<string, Result<unknown, unknown>>>(results: R): Result<{
	[K in keyof R]: InferOk<R[K]>;
}, InferErrUnionFromRecord<R>>;
declare function partition<T, E>(results: readonly Result<T, E>[]): [
	T[],
	E[]
];
declare function forEach<T, U, E>(items: readonly T[], f: (item: T, index: number) => Result<U, E>): Result<U[], E>;
declare function zipOrAccumulate<R extends readonly Result<unknown, unknown>[]>(...results: R): Result<InferOkTuple<R>, NonEmptyArray<InferErrUnion<R>>>;
declare class ResultAsync\$1<T, E> implements ResultAsync<T, E> {
	#private;
	private constructor();
	/**
	 * Creates a \`ResultAsync\` wrapping an \`Ok\` value.
	 *
	 * @example
	 * \`\`\`ts
	 * import { ResultAsync } from '@hex-di/result';
	 *
	 * const result = ResultAsync.ok(42);
	 * const inner = await result; // Ok(42)
	 * \`\`\`
	 *
	 * @since v1.0.0
	 * @see spec/result/behaviors/06-async.md — BEH-06-003
	 */
	static ok<T>(value: T): ResultAsync\$1<T, never>;
	/**
	 * Creates a \`ResultAsync\` wrapping an \`Err\` value.
	 *
	 * @example
	 * \`\`\`ts
	 * import { ResultAsync } from '@hex-di/result';
	 *
	 * const result = ResultAsync.err("not found");
	 * const inner = await result; // Err("not found")
	 * \`\`\`
	 *
	 * @since v1.0.0
	 * @see spec/result/behaviors/06-async.md — BEH-06-003
	 */
	static err<E>(error: E): ResultAsync\$1<never, E>;
	/**
	 * Wraps a \`Promise<T>\` that might reject into a \`ResultAsync\`.
	 *
	 * Resolution produces \`Ok(value)\`. Rejection produces \`Err(mapErr(reason))\`.
	 *
	 * @example
	 * \`\`\`ts
	 * import { ResultAsync } from '@hex-di/result';
	 *
	 * const result = ResultAsync.fromPromise(
	 *   fetch("/api/users").then(r => r.json()),
	 *   (error) => \`Request failed: \${error}\`
	 * );
	 * \`\`\`
	 *
	 * @since v1.0.0
	 * @see spec/result/behaviors/06-async.md — BEH-06-003
	 */
	static fromPromise<T, E>(promise: Promise<T>, mapErr: (error: unknown) => E): ResultAsync\$1<T, E>;
	/**
	 * Wraps a \`Promise<T>\` that is guaranteed to never reject into a \`ResultAsync\`.
	 *
	 * Resolution produces \`Ok(value)\`. Since the promise never rejects, the error
	 * type is \`never\`.
	 *
	 * @example
	 * \`\`\`ts
	 * import { ResultAsync } from '@hex-di/result';
	 *
	 * const result = ResultAsync.fromSafePromise(Promise.resolve(42));
	 * const inner = await result; // Ok(42)
	 * \`\`\`
	 *
	 * @since v1.0.0
	 * @see spec/result/behaviors/06-async.md — BEH-06-003
	 */
	static fromSafePromise<T>(promise: Promise<T>): ResultAsync\$1<T, never>;
	/**
	 * Creates a ResultAsync from a Promise that resolves to a Result.
	 *
	 * This is useful when you have an async function that already returns Result
	 * values (e.g., sequential effect execution loops that check \`_tag\` and return
	 * early on error). It avoids the need for \`andThen\` flattening, which has
	 * inference issues in TypeScript 5.9.
	 *
	 * The promise must never reject — it should always resolve to either
	 * ok(value) or err(error).
	 *
	 * @param promise - A Promise that resolves to a Result<T, E>
	 * @returns A ResultAsync<T, E> wrapping the promise
	 */
	static fromResult<T, E>(promise: Promise<Result<T, E>>): ResultAsync\$1<T, E>;
	/**
	 * Wraps an async function that might throw into a function returning \`ResultAsync\`.
	 *
	 * Returns a new function with the same parameter types that returns \`ResultAsync\`
	 * instead of \`Promise\`. Rejections are caught and mapped via \`mapErr\`.
	 *
	 * @example
	 * \`\`\`ts
	 * import { ResultAsync } from '@hex-di/result';
	 *
	 * const safeReadFile = ResultAsync.fromThrowable(
	 *   async (path: string) => fs.promises.readFile(path, "utf-8"),
	 *   (error) => \`Read failed: \${error}\`
	 * );
	 *
	 * const result = safeReadFile("config.json");
	 * // ResultAsync<string, string>
	 * \`\`\`
	 *
	 * @since v1.0.0
	 * @see spec/result/behaviors/06-async.md — BEH-06-003
	 */
	static fromThrowable<A extends readonly unknown[], T, E>(fn: (...args: A) => Promise<T>, mapErr: (error: unknown) => E): (...args: A) => ResultAsync\$1<T, E>;
	static fromCallback<T, E>(fn: (callback: (error: E | null, value: T) => void) => void): ResultAsync\$1<T, E>;
	static race<R extends readonly ResultAsync\$1<unknown, unknown>[]>(...results: R): ResultAsync\$1<InferOkUnion<R>, InferErrUnion<R>>;
	static all: typeof all;
	static allSettled: typeof allSettled;
	static any: typeof any;
	static collect: typeof collect;
	static partition: typeof partition;
	static forEach: typeof forEach;
	static zipOrAccumulate: typeof zipOrAccumulate;
	then<A = Result<T, E>, B = never>(onfulfilled?: ((value: Result<T, E>) => A | PromiseLike<A>) | null | undefined, onrejected?: ((reason: unknown) => B | PromiseLike<B>) | null | undefined): PromiseLike<A | B>;
	map<U>(f: (value: T) => U | Promise<U>): ResultAsync\$1<U, E>;
	mapErr<F>(f: (error: E) => F | Promise<F>): ResultAsync\$1<T, F>;
	mapBoth<U, F>(onOk: (value: T) => U | Promise<U>, onErr: (error: E) => F | Promise<F>): ResultAsync\$1<U, F>;
	andThen<U, F>(f: (value: T) => Result<U, F> | ResultAsync<U, F>): ResultAsync\$1<U, E | F>;
	orElse<U, F>(f: (error: E) => Result<U, F> | ResultAsync<U, F>): ResultAsync\$1<T | U, F>;
	andTee(f: (value: T) => void | Promise<void>): ResultAsync\$1<T, E>;
	orTee(f: (error: E) => void | Promise<void>): ResultAsync\$1<T, E>;
	andThrough<F>(f: (value: T) => Result<unknown, F> | ResultAsync<unknown, F>): ResultAsync\$1<T, E | F>;
	inspect(f: (value: T) => void): ResultAsync\$1<T, E>;
	inspectErr(f: (error: E) => void): ResultAsync\$1<T, E>;
	match<A, B>(onOk: (value: T) => A | Promise<A>, onErr: (error: E) => B | Promise<B>): Promise<A | B>;
	unwrapOr<U>(defaultValue: U): Promise<T | U>;
	unwrapOrElse<U>(f: (error: E) => U): Promise<T | U>;
	orDie(): Promise<T>;
	toNullable(): Promise<T | null>;
	toUndefined(): Promise<T | undefined>;
	intoTuple(): Promise<[
		null,
		T
	] | [
		E,
		null
	]>;
	merge(): Promise<T | E>;
	flatten<U>(this: ResultAsync\$1<Result<U, E>, E>): ResultAsync\$1<U, E>;
	flip(): ResultAsync\$1<E, T>;
	toJSON(): Promise<{
		_tag: "Ok";
		_schemaVersion: 1;
		value: T;
	} | {
		_tag: "Err";
		_schemaVersion: 1;
		error: E;
	}>;
}
export type QueryFetcher<TData, TParams, TError> = (params: TParams, context: FetchContext) => ResultAsync\$1<TData, TError>;
export type MutationExecutor<TData, TInput, TError> = (input: TInput, context: MutationContext) => ResultAsync\$1<TData, TError>;
export interface FetchContext {
	/** AbortSignal for cancellation */
	readonly signal: AbortSignal;
	/** Custom metadata passed to the fetcher */
	readonly meta?: Readonly<Record<string, unknown>>;
	/** Page parameter for infinite queries */
	readonly pageParam?: unknown;
	/** Direction for infinite queries */
	readonly direction?: "forward" | "backward";
	/**
	 * Optional callback for progressive/streaming cache updates.
	 * Called with intermediate reduced values as chunks arrive,
	 * enabling UI updates before the full stream completes.
	 */
	readonly onProgress?: (intermediateData: unknown) => void;
}
export interface MutationContext {
	/** AbortSignal for cancellation */
	readonly signal: AbortSignal;
	/** Custom metadata */
	readonly meta?: Readonly<Record<string, unknown>>;
}
export type StreamedFetcher<TData, TParams, TError, TChunk> = (params: TParams, context: FetchContext) => ResultAsync\$1<{
	stream: AsyncIterable<TChunk>;
	reducer: (acc: TData, chunk: TChunk) => TData;
	initialValue: TData;
	refetchMode?: "reset" | "append" | "replace";
}, TError>;
/**
 * Configuration options for queries and mutations.
 *
 * @packageDocumentation
 */
/**
 * Default configuration values for queries.
 * Specified at port level, overridable per-use.
 */
export interface QueryDefaults {
	/** Time before data is considered stale (ms). Default: 0 */
	readonly staleTime: number;
	/** Time to keep unused data in cache after last observer detaches (ms). Default: 300_000 (5 min) */
	readonly cacheTime: number;
	/** Number of retry attempts on failure. Default: 3 */
	readonly retry: number | boolean | ((failureCount: number, error: unknown) => boolean);
	/** Delay between retries. Default: exponential backoff */
	readonly retryDelay: number | ((attempt: number, error: unknown) => number);
	/** Refetch on component mount. Default: true */
	readonly refetchOnMount: boolean | "always";
	/** Refetch on window focus. Default: true */
	readonly refetchOnWindowFocus: boolean | "always";
	/** Refetch on network reconnect. Default: true */
	readonly refetchOnReconnect: boolean | "always";
	/** Auto-refetch interval (ms). Default: false (disabled) */
	readonly refetchInterval: number | false;
	/** Continue polling when window is not focused. Default: false */
	readonly refetchIntervalInBackground: boolean;
	/** Network mode. Default: "online" */
	readonly networkMode: "online" | "always" | "offlineFirst";
	/** Structural sharing for stable references. Default: true */
	readonly structuralSharing: boolean;
}
/**
 * Global default values for query configuration.
 */
export declare const DEFAULT_QUERY_OPTIONS: QueryDefaults;
/**
 * Default configuration values for mutations.
 */
export interface MutationDefaults {
	readonly retry?: number | boolean;
	readonly retryDelay?: number;
}
export interface FetchOptions {
	readonly staleTime?: number;
	readonly signal?: AbortSignal;
	readonly meta?: Readonly<Record<string, unknown>>;
}
export interface PrefetchOptions {
	readonly staleTime?: number;
	readonly signal?: AbortSignal;
}
export interface EnsureOptions {
	readonly staleTime?: number;
	readonly signal?: AbortSignal;
}
export interface RefetchOptions {
	readonly cancelRefetch?: boolean;
}
export interface MutateOptions<TData, TInput, TError, TContext> {
	readonly signal?: AbortSignal;
	readonly meta?: Readonly<Record<string, unknown>>;
	readonly onMutate?: (input: TInput) => TContext | Promise<TContext>;
	readonly onSuccess?: (data: TData, input: TInput, context: TContext | undefined) => void;
	readonly onError?: (error: TError, input: TInput, context: TContext | undefined) => void;
	readonly onSettled?: (data: TData | undefined, error: TError | undefined, input: TInput, context: TContext | undefined) => void;
}
/**
 * Runtime symbol for identifying query ports.
 * Uses Symbol.for() for cross-module stability.
 */
export declare const QUERY_PORT_SYMBOL: unique symbol;
declare const __queryErrorType: unique symbol;
export interface QueryPortConfig<_TData, _TParams, _TError, TName extends string, TDependsOn extends ReadonlyArray<AnyQueryPort> = [
]> {
	/** Unique name -- becomes the cache key prefix and port identifier */
	readonly name: TName;
	/** Structural data dependencies on other query ports */
	readonly dependsOn?: TDependsOn;
	/** Default query options (can be overridden per-use) */
	readonly defaults?: Partial<QueryDefaults>;
}
export interface QueryPort<TName extends string = string, TData = unknown, TParams = void, TError = Error, TDependsOn extends ReadonlyArray<AnyQueryPort> = [
]> extends DirectedPort<TName, QueryFetcher<TData, TParams, TError>, "inbound"> {
	/** Phantom: compile-time error type */
	readonly __queryErrorType: TError;
	/** Runtime brand: identifies this as a QueryPort */
	readonly [QUERY_PORT_SYMBOL]: true;
	/** Query-specific configuration */
	readonly config: QueryPortConfig<TData, TParams, TError, TName, TDependsOn>;
}
/**
 * Convenience alias for query ports with erased type parameters.
 *
 * Uses \`never\` for TParams because TParams is in a contravariant position
 * (function parameter in QueryFetcher). For a QueryPort<..., TParams, ...>
 * to be assignable to AnyQueryPort, AnyQueryPort's TParams must be a subtype
 * of every possible TParams — which is \`never\`.
 */
export type AnyQueryPort = QueryPort<string, unknown, never, unknown>;
/**
 * Creates a query port definition using curried generics.
 *
 * Stage 1: Explicit type parameters (data, params, error).
 * Stage 2: Inferred configuration (name, defaults, dependsOn).
 */
export declare function createQueryPort<TData, TParams = void, TError = Error>(): <const TName extends string, const TDependsOn extends ReadonlyArray<AnyQueryPort> = [
]>(config: QueryPortConfig<TData, TParams, TError, TName, TDependsOn>) => QueryPort<TName, TData, TParams, TError, TDependsOn>;
export declare function isQueryPort(value: unknown): value is QueryPort;
/**
 * Unvalidated effects -- used in createMutationPort config where graph
 * context is not yet available. Accepts any QueryPort.
 */
export interface MutationEffects {
	/** Query ports to mark stale and refetch. Active queries refetch immediately. */
	readonly invalidates?: ReadonlyArray<AnyQueryPort>;
	/** Query ports to remove from cache entirely. */
	readonly removes?: ReadonlyArray<AnyQueryPort>;
}
/**
 * Runtime symbol for identifying mutation ports.
 */
export declare const MUTATION_PORT_SYMBOL: unique symbol;
declare const __mutationErrorType: unique symbol;
declare const __mutationContextType: unique symbol;
export interface MutationPortConfig<_TData, _TInput, _TError, _TContext, TName extends string> {
	/** Unique name -- identifier for the mutation */
	readonly name: TName;
	/** Cache side effects triggered on successful mutation */
	readonly effects?: MutationEffects;
	/** Default mutation options */
	readonly defaults?: Partial<MutationDefaults>;
}
export interface MutationPort<TName extends string = string, TData = unknown, TInput = void, TError = Error, TContext = unknown> extends DirectedPort<TName, MutationExecutor<TData, TInput, TError>, "inbound"> {
	/** Phantom: compile-time error type */
	readonly __mutationErrorType: TError;
	/** Phantom: compile-time optimistic update context type */
	readonly __mutationContextType: TContext;
	/** Runtime brand: identifies this as a MutationPort */
	readonly [MUTATION_PORT_SYMBOL]: true;
	/** Mutation-specific configuration */
	readonly config: MutationPortConfig<TData, TInput, TError, TContext, TName>;
}
/**
 * Convenience alias for mutation ports with erased type parameters.
 *
 * Uses \`never\` for TInput because TInput is in a contravariant position
 * (function parameter in MutationExecutor). For a MutationPort<..., TInput, ...>
 * to be assignable to AnyMutationPort, AnyMutationPort's TInput must be a subtype
 * of every possible TInput — which is \`never\`.
 */
export type AnyMutationPort = MutationPort<string, unknown, never, unknown, unknown>;
/**
 * Creates a mutation port definition using curried generics.
 *
 * Stage 1: Explicit type parameters (data, input, error, context).
 * Stage 2: Inferred configuration (name, effects, defaults).
 */
export declare function createMutationPort<TData, TInput = void, TError = Error, TContext = unknown>(): <const TName extends string>(config: MutationPortConfig<TData, TInput, TError, TContext, TName>) => MutationPort<TName, TData, TInput, TError, TContext>;
export declare function isMutationPort(value: unknown): value is MutationPort;
/**
 * Configuration for creating a query adapter without dependencies.
 */
export interface QueryAdapterConfigNoDeps<TPort extends AnyQueryPort> {
	readonly factory: () => InferService<TPort>;
	readonly lifetime?: Lifetime;
	readonly requires?: undefined;
}
/**
 * Configuration for creating a query adapter with dependencies.
 */
export interface QueryAdapterConfigWithDeps<TPort extends AnyQueryPort, TRequires extends ReadonlyArray<Port<string, unknown>>> {
	readonly requires: TRequires;
	readonly factory: (deps: PortDeps<TRequires>) => InferService<TPort>;
	readonly lifetime?: Lifetime;
}
/**
 * Creates a standard HexDI Adapter for a query port.
 *
 * The factory returns a \`QueryFetcher<TData, TParams, TError>\`, which is
 * the service type of the port (\`InferService<QueryPort<...>>\`).
 *
 * @example No dependencies
 * \`\`\`typescript
 * const UsersAdapter = createQueryAdapter(UsersPort, {
 *   factory: () =>
 *     (params, { signal }) =>
 *       ResultAsync.fromPromise(fetch("/api/users", { signal }), classifyError),
 * });
 * \`\`\`
 *
 * @example With dependencies
 * \`\`\`typescript
 * const UsersAdapter = createQueryAdapter(UsersPort, {
 *   requires: [HttpClientPort],
 *   factory: ({ HttpClient }) =>
 *     (params, { signal }) =>
 *       ResultAsync.fromPromise(HttpClient.get("/api/users"), classifyError),
 * });
 * \`\`\`
 */
export declare function createQueryAdapter<TPort extends AnyQueryPort>(port: TPort, config: QueryAdapterConfigNoDeps<TPort>): AdapterConstraint;
export declare function createQueryAdapter<TPort extends AnyQueryPort, TRequires extends ReadonlyArray<Port<string, unknown>>>(port: TPort, config: QueryAdapterConfigWithDeps<TPort, TRequires>): AdapterConstraint;
/**
 * Configuration for creating a mutation adapter without dependencies.
 */
export interface MutationAdapterConfigNoDeps<TPort extends AnyMutationPort> {
	readonly factory: () => InferService<TPort>;
	readonly lifetime?: Lifetime;
	readonly requires?: undefined;
}
/**
 * Configuration for creating a mutation adapter with dependencies.
 */
export interface MutationAdapterConfigWithDeps<TPort extends AnyMutationPort, TRequires extends ReadonlyArray<Port<string, unknown>>> {
	readonly requires: TRequires;
	readonly factory: (deps: PortDeps<TRequires>) => InferService<TPort>;
	readonly lifetime?: Lifetime;
}
/**
 * Creates a standard HexDI Adapter for a mutation port.
 *
 * The factory returns a \`MutationExecutor<TData, TInput, TError>\`, which is
 * the service type of the port (\`InferService<MutationPort<...>>\`).
 *
 * @example No dependencies
 * \`\`\`typescript
 * const CreateUserAdapter = createMutationAdapter(CreateUserPort, {
 *   factory: () =>
 *     (input, { signal }) =>
 *       ResultAsync.fromPromise(fetch("/api/users", { method: "POST", body: input, signal }), classifyError),
 * });
 * \`\`\`
 *
 * @example With dependencies
 * \`\`\`typescript
 * const CreateUserAdapter = createMutationAdapter(CreateUserPort, {
 *   requires: [HttpClientPort],
 *   factory: ({ HttpClient }) =>
 *     (input, { signal }) =>
 *       ResultAsync.fromPromise(HttpClient.post("/api/users", input, { signal }), classifyError),
 * });
 * \`\`\`
 */
export declare function createMutationAdapter<TPort extends AnyMutationPort>(port: TPort, config: MutationAdapterConfigNoDeps<TPort>): AdapterConstraint;
export declare function createMutationAdapter<TPort extends AnyMutationPort, TRequires extends ReadonlyArray<Port<string, unknown>>>(port: TPort, config: MutationAdapterConfigWithDeps<TPort, TRequires>): AdapterConstraint;
/**
 * Configuration for creating a streamed query adapter without dependencies.
 */
export interface StreamedQueryAdapterConfigNoDeps<TData, TParams, TError, TChunk> {
	readonly factory: () => StreamedFetcher<TData, TParams, TError, TChunk>;
	readonly lifetime?: Lifetime;
	readonly requires?: undefined;
}
/**
 * Configuration for creating a streamed query adapter with dependencies.
 */
export interface StreamedQueryAdapterConfigWithDeps<TData, TParams, TError, TChunk, TRequires extends ReadonlyArray<Port<string, unknown>>> {
	readonly requires: TRequires;
	readonly factory: (deps: PortDeps<TRequires>) => StreamedFetcher<TData, TParams, TError, TChunk>;
	readonly lifetime?: Lifetime;
}
/**
 * Creates a standard HexDI Adapter for a query port backed by a streaming
 * data source.
 *
 * The factory returns a \`StreamedFetcher\` which produces an async stream,
 * a reducer, and an initial value. The adapter wraps this into a standard
 * \`QueryFetcher\` that consumes the stream and returns the final reduced value.
 *
 * @example No dependencies
 * \`\`\`typescript
 * const EventsAdapter = createStreamedQueryAdapter(EventsPort, {
 *   factory: () =>
 *     (params, { signal }) =>
 *       ResultAsync.ok({
 *         stream: eventSource(params, signal),
 *         reducer: (acc, chunk) => [...acc, chunk],
 *         initialValue: [],
 *       }),
 * });
 * \`\`\`
 */
export declare function createStreamedQueryAdapter<TData, TParams, TError, TChunk, TName extends string>(port: AnyQueryPort & {
	readonly __portName: TName;
}, config: StreamedQueryAdapterConfigNoDeps<TData, TParams, TError, TChunk>): AdapterConstraint;
export declare function createStreamedQueryAdapter<TData, TParams, TError, TChunk, TName extends string, TRequires extends ReadonlyArray<Port<string, unknown>>>(port: AnyQueryPort & {
	readonly __portName: TName;
}, config: StreamedQueryAdapterConfigWithDeps<TData, TParams, TError, TChunk, TRequires>): AdapterConstraint;
/**
 * Deterministic JSON serialization.
 *
 * Produces the same string regardless of key insertion order, enabling
 * stable cache key generation.
 *
 * @packageDocumentation
 */
/**
 * Recursively serializes a value to a deterministic JSON string.
 * Object keys are sorted lexicographically; arrays preserve order.
 */
export declare function stableStringify(value: unknown): string;
/**
 * Structural sharing (replaceEqualDeep).
 *
 * Preserves reference equality for unchanged portions of a data tree,
 * minimizing unnecessary re-renders in React and other reactive frameworks.
 *
 * @packageDocumentation
 */
/**
 * Deep comparison that reuses previous references where possible.
 *
 * Returns \`prev\` (by reference) for any sub-tree that is structurally equal,
 * and \`next\` values only where changes exist.
 */
export declare function replaceEqualDeep<T>(prev: T, next: T): T;
/**
 * A branded cache key tuple: [portName, paramsHash].
 *
 * The brand prevents ad-hoc construction of cache keys outside of
 * \`createCacheKey\`, ensuring all keys pass through deterministic
 * serialization.
 */
export type CacheKey<TName extends string = string> = readonly [
	portName: TName,
	paramsHash: string
] & {
	readonly __cacheKeyBrand: true;
};
/**
 * Minimal interface for creating cache keys.
 * Accepts any object with a \`__portName\` property, avoiding variance
 * issues with the full QueryPort type.
 */
export interface PortLike<TName extends string = string> {
	readonly __portName: TName;
}
/**
 * Create a CacheKey from a port-like object and params.
 * Ensures deterministic parameter serialization via stableStringify.
 */
export declare function createCacheKey<TName extends string>(port: PortLike<TName>, params: unknown): CacheKey<TName>;
/**
 * Create a cache key from a port name and params directly.
 * Used internally when the port object is not available.
 */
export declare function createCacheKeyFromName(portName: string, params: unknown): CacheKey;
/**
 * Serialize a CacheKey to a single string for use as Map key.
 */
export declare function serializeCacheKey(key: CacheKey): string;
/**
 * Check if a cache key belongs to a given port (by name prefix).
 */
export declare function cacheKeyMatchesPort(key: CacheKey, portName: string): boolean;
/**
 * Query resolution error types.
 *
 * QueryResolutionError is a tagged union representing all infrastructure-level
 * query failures produced by the QueryClient.
 *
 * @packageDocumentation
 */
export type QueryResolutionError = QueryFetchFailed | QueryCancelled | QueryTimeout | QueryAdapterMissing | QueryInvalidationCycle | QueryDisposed | BatchExecutionFailed;
export interface QueryFetchFailed {
	readonly _tag: "QueryFetchFailed";
	readonly portName: string;
	readonly params: unknown;
	readonly retryAttempt: number;
	readonly cause: unknown;
}
export interface QueryCancelled {
	readonly _tag: "QueryCancelled";
	readonly portName: string;
	readonly params: unknown;
}
export interface QueryTimeout {
	readonly _tag: "QueryTimeout";
	readonly portName: string;
	readonly params: unknown;
	readonly timeoutMs: number;
}
export interface QueryAdapterMissing {
	readonly _tag: "QueryAdapterMissing";
	readonly portName: string;
}
export interface QueryInvalidationCycle {
	readonly _tag: "QueryInvalidationCycle";
	readonly chain: readonly string[];
	readonly depth: number;
}
export interface QueryDisposed {
	readonly _tag: "QueryDisposed";
	readonly portName: string;
}
export declare function queryFetchFailed(portName: string, params: unknown, retryAttempt: number, cause: unknown): QueryFetchFailed;
export declare function queryCancelled(portName: string, params: unknown): QueryCancelled;
export declare function queryTimeout(portName: string, params: unknown, timeoutMs: number): QueryTimeout;
export declare function queryAdapterMissing(portName: string): QueryAdapterMissing;
export declare function queryInvalidationCycle(chain: readonly string[], depth: number): QueryInvalidationCycle;
export declare function queryDisposed(portName: string): QueryDisposed;
export interface BatchExecutionFailed {
	readonly _tag: "BatchExecutionFailed";
	readonly cause: unknown;
	readonly code: "BATCH_EXECUTION_FAILED";
	readonly isProgrammingError: false;
	readonly message: string;
}
export declare function BatchExecutionFailed(fields: {
	readonly cause: unknown;
}): BatchExecutionFailed;
export type QueryStatus = "pending" | "success" | "error";
export type FetchStatus = "idle" | "fetching" | "paused";
export interface QueryState<TData, TError = Error> {
	readonly status: QueryStatus;
	readonly fetchStatus: FetchStatus;
	readonly isPending: boolean;
	readonly isSuccess: boolean;
	readonly isError: boolean;
	readonly isFetching: boolean;
	readonly isRefetching: boolean;
	readonly isLoading: boolean;
	readonly isStale: boolean;
	readonly isPlaceholderData: boolean;
	readonly isPaused: boolean;
	readonly result: Result<TData, TError> | undefined;
	readonly data: TData | undefined;
	readonly error: TError | null;
	readonly dataUpdatedAt: number | undefined;
	readonly errorUpdatedAt: number | undefined;
	readonly refetch: (options?: RefetchOptions) => ResultAsync\$1<TData, TError | QueryResolutionError>;
}
export type MutationStatus = "idle" | "pending" | "success" | "error";
export interface MutationState<TData, TError = Error> {
	readonly status: MutationStatus;
	readonly isPending: boolean;
	readonly isSuccess: boolean;
	readonly isError: boolean;
	readonly isIdle: boolean;
	readonly result: Result<TData, TError> | undefined;
	readonly data: TData | undefined;
	readonly error: TError | null;
}
/**
 * Per-Container Isolated Reactive System Factory
 *
 * Ports alien-signals' module-level signal/computed/effect implementation
 * into an instance-scoped factory. Each call to \`createIsolatedReactiveSystem()\`
 * returns an independent reactive graph with its own batching, queue, and
 * subscriber tracking — eliminating cross-container interference.
 *
 * Built on \`createReactiveSystem\` from \`alien-signals/system\`, which provides
 * the low-level dependency graph primitives (link, unlink, propagate, checkDirty,
 * shallowPropagate). This module adds the signal/computed/effect layer on top,
 * using closure-scoped state instead of module globals.
 *
 * @packageDocumentation
 */
/**
 * An isolated reactive system instance.
 *
 * Each instance maintains its own dependency graph, batch queue, and
 * subscriber tracking. Signals created in one system cannot register
 * as dependencies in another system's computeds or effects.
 */
export interface ReactiveSystemInstance {
	/** Create a reactive signal with read/write function interface */
	signal<T>(initialValue: T): {
		(): T;
		(value: T): void;
	};
	/** Create a computed value that auto-tracks dependencies */
	computed<T>(getter: (previousValue?: T) => T): () => T;
	/** Create a reactive effect; returns a dispose function */
	effect(fn: () => void): () => void;
	/** Begin a batch — defers notifications until matching endBatch() */
	startBatch(): void;
	/** End a batch — flushes deferred notifications when outermost batch ends */
	endBatch(): void;
	/** Get the current active subscriber (for untracked reads) */
	getActiveSub(): ReactiveNode | undefined;
	/** Set the active subscriber; returns the previous one */
	setActiveSub(sub?: ReactiveNode): ReactiveNode | undefined;
}
export interface ReactiveNode {
	deps?: Link;
	depsTail?: Link;
	subs?: Link;
	subsTail?: Link;
	flags: number;
	[key: string]: unknown;
}
export interface Link {
	version: number;
	dep: ReactiveNode;
	sub: ReactiveNode;
	prevSub: Link | undefined;
	nextSub: Link | undefined;
	prevDep: Link | undefined;
	nextDep: Link | undefined;
}
/**
 * Creates a fully isolated reactive system.
 *
 * All state (cycle counter, batch depth, active subscriber, effect queue)
 * is closure-scoped to the returned instance. Two systems cannot interfere
 * with each other's batching, notification ordering, or dependency tracking.
 */
export declare function createIsolatedReactiveSystem(): ReactiveSystemInstance;
/**
 * Runs \`fn\` outside the current reactive tracking scope.
 *
 * When a \`system\` is provided, uses that system's subscriber tracking.
 * Otherwise uses the global alien-signals tracking.
 */
export declare function untracked<T>(fn: () => T, system?: ReactiveSystemInstance): T;
/** A reactive value that tracks dependents */
export interface Signal<T> {
	/** Read the current value and register as a dependency */
	get(): T;
	/** Write a new value and notify dependents */
	set(value: T): void;
	/** Read without tracking (no dependency registration) */
	peek(): T;
}
/**
 * Creates a reactive signal.
 *
 * @param initial - Initial signal value
 * @param system - Optional isolated reactive system. When provided, the signal
 *   lives in that system's dependency graph instead of the global one.
 */
export declare function createSignal<T>(initial: T, system?: ReactiveSystemInstance): Signal<T>;
/** A derived signal that auto-tracks dependencies */
export interface Computed<T> {
	/** Read the computed value (lazy, cached until deps change) */
	get(): T;
	/** Read without tracking */
	peek(): T;
}
/**
 * Creates a computed signal.
 *
 * @param fn - Computation function
 * @param system - Optional isolated reactive system
 */
export declare function createComputed<T>(fn: () => T, system?: ReactiveSystemInstance): Computed<T>;
/** A reactive effect that runs when tracked signals change */
export interface ReactiveEffect {
	/** Run the effect and track dependencies */
	run(): void;
	/** Stop tracking and dispose */
	dispose(): void;
}
/**
 * Creates a reactive effect.
 *
 * @param fn - Effect function (runs immediately and re-runs when deps change)
 * @param system - Optional isolated reactive system
 */
export declare function createEffect(fn: () => void, system?: ReactiveSystemInstance): ReactiveEffect;
/**
 * A signal-backed cache entry. Source signals are written by the cache;
 * computed signals derive convenience state for observers and hooks.
 */
export interface ReactiveCacheEntry<TData, TError = Error> {
	/** The cache key this entry belongs to (for identification) */
	readonly key: string;
	readonly result\$: Signal<Result<TData, TError> | undefined>;
	readonly fetchStatus\$: Signal<FetchStatus>;
	readonly fetchCount\$: Signal<number>;
	readonly isInvalidated\$: Signal<boolean>;
	readonly dataUpdatedAt\$: Signal<number | undefined>;
	readonly errorUpdatedAt\$: Signal<number | undefined>;
	readonly status: Computed<QueryStatus>;
	readonly data: Computed<TData | undefined>;
	readonly error: Computed<TError | null>;
	readonly isPending: Computed<boolean>;
	readonly isSuccess: Computed<boolean>;
	readonly isError: Computed<boolean>;
	readonly isFetching: Computed<boolean>;
	readonly isLoading: Computed<boolean>;
	readonly isRefetching: Computed<boolean>;
}
export interface CacheEntrySnapshot<TData = unknown, TError = unknown> {
	readonly result: Result<TData, TError> | undefined;
	readonly data: TData | undefined;
	readonly error: TError | null;
	readonly status: QueryStatus;
	readonly fetchStatus: FetchStatus;
	readonly dataUpdatedAt: number | undefined;
	readonly errorUpdatedAt: number | undefined;
	readonly fetchCount: number;
	readonly isInvalidated: boolean;
}
/**
 * Creates a reactive cache entry with signal-backed state.
 *
 * @param key - Serialized cache key identifying this entry
 * @param system - Optional isolated reactive system for per-scope isolation
 */
export declare function createReactiveCacheEntry<TData, TError>(key: string, system?: ReactiveSystemInstance): ReactiveCacheEntry<TData, TError>;
/**
 * Creates a non-reactive snapshot from a reactive cache entry.
 * Reads all signals via \`.peek()\` (no tracking).
 */
export declare function getSnapshot<TData, TError>(entry: ReactiveCacheEntry<TData, TError>): CacheEntrySnapshot<TData, TError>;
export declare function incrementSubscribers(entry: ReactiveCacheEntry<unknown, unknown>): void;
export declare function decrementSubscribers(entry: ReactiveCacheEntry<unknown, unknown>): void;
export declare function hasSubscribers(entry: ReactiveCacheEntry<unknown, unknown>): boolean;
export declare function getSubscriberCount(entry: ReactiveCacheEntry<unknown, unknown>): number;
/**
 * Legacy CacheEntry interface for backward compatibility.
 * Code that consumed the old immutable CacheEntry can use CacheEntrySnapshot instead.
 */
export type CacheEntry<TData = unknown, TError = unknown> = CacheEntrySnapshot<TData, TError>;
/**
 * Creates a pending snapshot (legacy compat).
 */
export declare function createPendingEntry<TData, TError>(): CacheEntrySnapshot<TData, TError>;
interface PortLike\$1 {
	readonly __portName: string;
}
export type CacheEvent = {
	readonly type: "added";
	readonly key: CacheKey;
	readonly entry: CacheEntrySnapshot;
} | {
	readonly type: "updated";
	readonly key: CacheKey;
	readonly entry: CacheEntrySnapshot;
} | {
	readonly type: "removed";
	readonly key: CacheKey;
} | {
	readonly type: "invalidated";
	readonly key: CacheKey;
} | {
	readonly type: "cleared";
};
export type CacheListener = (event: CacheEvent) => void;
export type Unsubscribe = () => void;
export interface GarbageCollectorConfig {
	readonly interval: number;
	readonly enabled: boolean;
}
export interface Clock {
	readonly now: () => number;
}
export interface QueryCache {
	get(port: PortLike\$1, params: unknown): CacheEntrySnapshot | undefined;
	has(port: PortLike\$1, params: unknown): boolean;
	getAll(): ReadonlyMap<string, CacheEntrySnapshot>;
	findByPort(port: PortLike\$1): ReadonlyArray<readonly [
		CacheKey,
		CacheEntrySnapshot
	]>;
	find(predicate: (entry: CacheEntrySnapshot, key: CacheKey) => boolean): ReadonlyArray<readonly [
		CacheKey,
		CacheEntrySnapshot
	]>;
	getEntry(port: PortLike\$1, params: unknown): ReactiveCacheEntry<unknown, unknown> | undefined;
	getOrCreateEntry(port: PortLike\$1, params: unknown): ReactiveCacheEntry<unknown, unknown>;
	set(port: PortLike\$1, params: unknown, data: unknown, options?: {
		readonly structuralSharing?: boolean;
	}): void;
	setError(port: PortLike\$1, params: unknown, error: unknown): void;
	invalidate(port: PortLike\$1, params?: unknown): void;
	remove(port: PortLike\$1, params?: unknown): void;
	clear(): void;
	getOrCreate(port: PortLike\$1, params: unknown): CacheEntrySnapshot;
	incrementObservers(port: PortLike\$1, params: unknown): void;
	decrementObservers(port: PortLike\$1, params: unknown): void;
	subscribe(listener: CacheListener): Unsubscribe;
	readonly size: number;
	dispose(): void;
}
export interface QueryCacheConfig {
	readonly clock?: Clock;
	readonly gc?: Partial<GarbageCollectorConfig>;
	readonly reactiveSystem?: ReactiveSystemInstance;
}
export declare function createQueryCache(config?: QueryCacheConfig): QueryCache;
export interface RetryConfig {
	readonly retry: number | boolean | ((failureCount: number, error: unknown) => boolean);
	readonly retryDelay: number | ((attempt: number, error: unknown) => number);
}
/**
 * Execute a fetch function with retry logic.
 *
 * Returns the first successful result, or a QueryFetchFailed error after
 * all retries are exhausted.
 */
export declare function fetchWithRetry<TData, TError>(portName: string, params: unknown, fetcher: () => ResultAsync\$1<TData, TError>, config: RetryConfig, signal?: AbortSignal, onRetry?: (attempt: number) => void): ResultAsync\$1<TData, TError | QueryFetchFailed>;
/**
 * Query Tracing Types
 *
 * Type definitions for the Query tracing subsystem:
 * - TracerLike: Minimal tracer interface (no dependency on @hex-di/tracing)
 * - QueryTracingHook: Domain hook for fetch/mutation span lifecycle
 * - QueryTracingHookOptions: Configuration for creating a hook
 *
 * @packageDocumentation
 */
/**
 * Minimal tracer interface for distributed tracing integration.
 *
 * Any tracing adapter (@hex-di/tracing-datadog, @hex-di/tracing-jaeger, etc.)
 * can provide a compatible object without @hex-di/query depending on those packages.
 */
export interface TracerLike {
	pushSpan(name: string, attributes?: Record<string, string>): void;
	popSpan(status: "ok" | "error"): void;
}
/**
 * Attributes for query fetch span start.
 */
export interface QueryFetchSpanAttributes {
	readonly cacheHit: boolean;
	readonly deduplicated: boolean;
	readonly staleTimeMs: number;
}
/**
 * Attributes for query fetch span end.
 */
export interface QueryFetchEndAttributes {
	readonly durationMs: number;
	readonly result: "ok" | "error";
	readonly errorTag?: string;
	readonly retryAttempt?: number;
}
/**
 * Attributes for mutation span start.
 */
export interface QueryMutationSpanAttributes {
	readonly portName: string;
}
/**
 * Attributes for mutation span end.
 */
export interface QueryMutationEndAttributes {
	readonly durationMs: number;
	readonly result: "ok" | "error";
	readonly errorTag?: string;
}
/**
 * Hook for creating distributed tracing spans for fetch and mutation operations.
 */
export interface QueryTracingHook {
	onFetchStart(portName: string, params: string, attrs: QueryFetchSpanAttributes): void;
	onFetchEnd(portName: string, ok: boolean): void;
	onMutationStart(portName: string, input: string, attrs: QueryMutationSpanAttributes): void;
	onMutationEnd(portName: string, ok: boolean): void;
}
/**
 * Options for creating a QueryTracingHook.
 */
export interface QueryTracingHookOptions {
	readonly tracer: TracerLike;
	/** Optional filter to control which ports are traced. Return true to trace. */
	readonly filter?: (portName: string) => boolean;
	/** Whether to trace mutation operations. Defaults to true. */
	readonly traceMutations?: boolean;
	/** Optional scope ID to include in span attributes. */
	readonly scopeId?: string;
	/** Optional trace context entries to include in span attributes. */
	readonly traceContext?: Record<string, string>;
}
export interface PersistenceError {
	readonly _tag: "PersistenceError";
	readonly operation: "persist" | "restore" | "remove" | "restoreAll" | "clear";
	readonly cause: unknown;
}
export interface CachePersister {
	persistQuery(key: CacheKey, entry: CacheEntry<unknown, unknown>): ResultAsync\$1<void, PersistenceError>;
	restoreQuery(key: CacheKey): ResultAsync\$1<CacheEntry<unknown, unknown> | undefined, PersistenceError>;
	removeQuery(key: CacheKey): ResultAsync\$1<void, PersistenceError>;
	restoreAll(): ResultAsync\$1<ReadonlyArray<[
		CacheKey,
		CacheEntry<unknown, unknown>
	]>, PersistenceError>;
	clear(): ResultAsync\$1<void, PersistenceError>;
}
export interface QueryClientLike {
	fetchQuery<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams): ResultAsync\$1<TData, TError | QueryResolutionError>;
	readonly cache: {
		get(port: {
			readonly __portName: string;
		}, params: unknown): CacheEntry<unknown, unknown> | undefined;
		getOrCreate(port: {
			readonly __portName: string;
		}, params: unknown): CacheEntry<unknown, unknown>;
		incrementObservers(port: {
			readonly __portName: string;
		}, params: unknown): void;
		decrementObservers(port: {
			readonly __portName: string;
		}, params: unknown): void;
		subscribe(listener: (event: unknown) => void): () => void;
	};
}
export interface QueryObserverOptions<TData, TError> {
	readonly enabled?: boolean;
	readonly staleTime?: number;
	readonly refetchOnMount?: boolean | "always";
	readonly select?: (data: TData) => unknown;
	readonly notifyOnChangeProps?: ReadonlyArray<keyof QueryState<TData, TError>>;
}
export interface QueryObserver<TData, TError = Error> {
	subscribe(listener: (state: QueryState<TData, TError>) => void): () => void;
	getState(): QueryState<TData, TError>;
	refetch(options?: RefetchOptions): ResultAsync\$1<TData, TError | QueryResolutionError>;
	destroy(): void;
	readonly isDestroyed: boolean;
}
export declare function createQueryObserver<TData, TParams, TError, TName extends string>(client: QueryClientLike, port: QueryPort<TName, TData, TParams, TError>, params: TParams, options?: QueryObserverOptions<TData, TError>): QueryObserver<TData, TError>;
/**
 * Minimal container interface that the QueryClient requires for resolving
 * fetchers and executors from the DI container.
 *
 * This avoids a hard dependency on the full runtime container type,
 * allowing the QueryClient to work with any object that satisfies
 * the resolve protocol.
 *
 * Uses a non-generic signature so that typed containers (whose resolve
 * constrains the port parameter to known ports) are structurally
 * compatible without requiring casts.
 */
export interface QueryContainer {
	resolve(port: Port<string, unknown>): unknown;
}
export interface QueryClient {
	fetchQuery<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams, options?: FetchOptions): ResultAsync\$1<TData, TError | QueryResolutionError>;
	prefetchQuery<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams, options?: PrefetchOptions): ResultAsync\$1<void, TError | QueryResolutionError>;
	ensureQueryData<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams, options?: FetchOptions): ResultAsync\$1<TData, TError | QueryResolutionError>;
	getQueryData<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams): TData | undefined;
	setQueryData<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams, dataOrUpdater: TData | ((prev: TData | undefined) => TData)): void;
	mutate<TData, TInput, TError, TContext, TName extends string>(port: MutationPort<TName, TData, TInput, TError, TContext>, input: TInput, options?: MutateOptions<TData, TInput, TError, TContext>): ResultAsync\$1<TData, TError | QueryResolutionError>;
	getQueryState<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams): QueryState<TData, TError> | undefined;
	invalidateQueries<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params?: TParams): ResultAsync\$1<void, QueryResolutionError>;
	invalidateMatching(predicate: (entry: CacheEntry<unknown, unknown>, key: CacheKey) => boolean): ResultAsync\$1<void, QueryResolutionError>;
	invalidateAll(): ResultAsync\$1<void, QueryResolutionError>;
	removeQueries<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params?: TParams): void;
	cancelQueries<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params?: TParams): ResultAsync\$1<void, never>;
	cancelAll(): ResultAsync\$1<void, never>;
	reset<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params?: TParams): void;
	clear(): void;
	observe<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams, options?: QueryObserverOptions<TData, TError>): QueryObserver<TData, TError>;
	subscribeToQuery<TData, TParams, TError, TName extends string>(port: QueryPort<TName, TData, TParams, TError>, params: TParams, callback: (state: QueryState<TData, TError>) => void): () => void;
	readonly cache: QueryCache;
	subscribe(listener: CacheListener): Unsubscribe;
	createChild(): QueryClient;
	dispose(): void;
	readonly isDisposed: boolean;
	readonly defaults: QueryDefaults;
	/** Count of currently in-flight fetch operations */
	isFetching(filters?: {
		readonly port?: {
			readonly __portName: string;
		};
	}): number;
	/** Count of currently in-flight mutation operations */
	isMutating(): number;
	/** Pause background operations (fetches, polling, GC) */
	pause(): void;
	/** Resume background operations */
	resume(): void;
	/** Whether the client is currently paused */
	readonly isPaused: boolean;
	/** Subscribe to query lifecycle events for instrumentation */
	subscribeToEvents(listener: (event: QueryClientEvent) => void): Unsubscribe;
}
export type FetchTrigger = "mount" | "refetch-manual" | "refetch-focus" | "refetch-interval" | "refetch-reconnect" | "invalidation" | "mutation-effect" | "prefetch" | "ensure";
export type QueryClientEvent = {
	readonly type: "fetch-started";
	readonly portName: string;
	readonly params: unknown;
	readonly trigger: FetchTrigger;
} | {
	readonly type: "fetch-completed";
	readonly portName: string;
	readonly params: unknown;
	readonly durationMs: number;
} | {
	readonly type: "fetch-error";
	readonly portName: string;
	readonly params: unknown;
	readonly durationMs: number;
	readonly errorTag?: string;
} | {
	readonly type: "fetch-cancelled";
	readonly portName: string;
	readonly params: unknown;
} | {
	readonly type: "cache-hit";
	readonly portName: string;
	readonly params: unknown;
} | {
	readonly type: "deduplicated";
	readonly portName: string;
	readonly params: unknown;
} | {
	readonly type: "invalidated";
	readonly portName: string;
	readonly params: unknown | undefined;
} | {
	readonly type: "observer-added";
	readonly portName: string;
	readonly params: unknown;
} | {
	readonly type: "observer-removed";
	readonly portName: string;
	readonly params: unknown;
} | {
	readonly type: "retry";
	readonly portName: string;
	readonly params: unknown;
	readonly attempt: number;
} | {
	readonly type: "mutation-started";
	readonly portName: string;
	readonly input: unknown;
} | {
	readonly type: "mutation-completed";
	readonly portName: string;
	readonly input: unknown;
	readonly success: boolean;
} | {
	readonly type: "mutation-effect-applied";
	readonly mutationPortName: string;
	readonly targetPortName: string;
	readonly effect: "invalidates" | "removes";
	readonly entriesAffected: number;
};
export interface QueryClientConfig {
	/** DI container for resolving query/mutation adapters */
	readonly container: QueryContainer;
	readonly clock?: Clock;
	readonly defaults?: Partial<QueryDefaults>;
	readonly maxInvalidationDepth?: number;
	/** Optional tracing hook for distributed tracing integration.
	 * Takes precedence over \`tracer\` if both are provided. */
	readonly tracingHook?: QueryTracingHook;
	/**
	 * Shorthand: pass a TracerLike and a QueryTracingHook is auto-created.
	 * Ignored when \`tracingHook\` is already provided.
	 */
	readonly tracer?: TracerLike;
	/** Optional persistence configuration for durable cache storage */
	readonly persister?: {
		readonly persister: CachePersister;
		readonly buster: string;
		readonly maxAge: number;
	};
}
export declare function createQueryClient(config: QueryClientConfig): QueryClient;
export interface MutationClientLike {
	mutate<TData, TInput, TError, TContext, TName extends string>(port: MutationPort<TName, TData, TInput, TError, TContext>, input: TInput, options?: MutateOptions<TData, TInput, TError, TContext>): ResultAsync\$1<TData, TError | QueryResolutionError>;
}
export interface MutationObserverOptions<TData, TInput, TError, TContext> {
	readonly onMutate?: (input: TInput) => TContext | Promise<TContext>;
	readonly onSuccess?: (data: TData, input: TInput, context: TContext | undefined) => void;
	readonly onError?: (error: TError | QueryResolutionError, input: TInput, context: TContext | undefined) => void;
	readonly onSettled?: (data: TData | undefined, error: (TError | QueryResolutionError) | undefined, input: TInput, context: TContext | undefined) => void;
}
interface MutationObserver\$1<TData, TInput, TError> {
	subscribe(listener: (state: MutationState<TData, TError | QueryResolutionError>) => void): () => void;
	getState(): MutationState<TData, TError | QueryResolutionError>;
	mutate(input: TInput): void;
	mutateAsync(input: TInput): ResultAsync\$1<TData, TError | QueryResolutionError>;
	reset(): void;
	destroy(): void;
	readonly isDestroyed: boolean;
}
export declare function createMutationObserver<TData, TInput, TError, TContext, TName extends string>(client: MutationClientLike, port: MutationPort<TName, TData, TInput, TError, TContext>, options?: MutationObserverOptions<TData, TInput, TError, TContext>): MutationObserver\$1<TData, TInput, TError>;
export interface DehydratedState {
	readonly version: 3;
	readonly queries: ReadonlyArray<{
		readonly cacheKey: CacheKey;
		readonly result: {
			readonly _tag: "Ok";
			readonly value: unknown;
		} | {
			readonly _tag: "Err";
			readonly error: unknown;
		};
		readonly dataUpdatedAt: number;
	}>;
}
/**
 * Extracts the current query cache state into a serializable format.
 *
 * Includes both success and error entries. Pending entries (no data and
 * no error) are omitted since they don't provide useful SSR state.
 */
export declare function dehydrate(client: QueryClient): DehydratedState;
/**
 * Restores dehydrated query state into a QueryClient's cache.
 *
 * For each entry, creates a cache entry via \`cache.set()\` using
 * a minimal port-like object. Existing entries are not overwritten
 * (hydration fills in missing data, doesn't replace fresh data).
 */
export declare function hydrate(client: QueryClient, state: DehydratedState): void;
/**
 * Configure a callback for cross-container batch detection.
 *
 * When \`batch()\` is called with target A while target B is already in a batch,
 * alien-signals cannot isolate the two batches. The callback receives both
 * targets so diagnostics or warnings can be emitted.
 *
 * Pass \`null\` to disable detection.
 *
 * @param callback - The diagnostic callback, or \`null\` to disable.
 */
export declare function setBatchDiagnostics(callback: ((newTarget: object, existingTarget: object) => void) | null): void;
/**
 * Groups multiple state changes into a single notification cycle.
 *
 * Uses alien-signals batching primitives by default. When an explicit
 * \`system\` is provided, uses that system's isolated batching instead.
 *
 * When the batch function throws, deferred notifications are flushed
 * and a BatchExecutionError is returned as the Err variant of the Result.
 *
 * **Scope isolation:** When using an explicit \`ReactiveSystemInstance\`,
 * batching is fully isolated to that system's dependency graph.
 * Without a system, alien-signals' global batching is used and per-target
 * depth tracking via WeakMap provides diagnostic visibility.
 *
 * @param containerOrScope - The container or scope to associate batch depth with.
 *   Pass \`null\` for global-only mode (no per-target depth tracking).
 * @param fn - The function to execute within the batch.
 * @param system - Optional isolated reactive system to use for batching.
 */
export declare function batch(containerOrScope: object | null, fn: () => void, system?: ReactiveSystemInstance): Result<void, BatchExecutionFailed>;
/**
 * Returns \`true\` if the given target is currently inside a \`batch()\` call.
 *
 * @param target - The container or scope object to check.
 */
export declare function isInBatch(target: object): boolean;
/**
 * Returns the current batch nesting depth for the given target.
 *
 * Returns \`0\` when the target is not inside any batch.
 *
 * @param target - The container or scope object to check.
 */
export declare function getBatchDepth(target: object): number;
/**
 * Returns the set of targets currently in a batch.
 *
 * Useful for debugging and introspection. Since the internal tracking uses
 * a \`WeakMap\`, this function requires the caller to pass candidate targets
 * to check against.
 *
 * @param candidates - The targets to check for active batch membership.
 * @returns The subset of candidates that are currently inside a batch.
 */
export declare function batchTargets(candidates: readonly object[]): ReadonlySet<object>;
export interface PersistenceConfig {
	readonly persister: CachePersister;
	readonly buster: string;
	readonly maxAge: number;
}
export interface PersistenceManager {
	readonly restore: () => Promise<void>;
	readonly createListener: () => CacheListener;
	readonly dispose: () => void;
}
export declare function createPersistenceManager(config: PersistenceConfig, cache: QueryCache, clock: Clock): PersistenceManager;
export interface QuerySnapshot {
	readonly timestamp: number;
	readonly entries: ReadonlyArray<QueryEntrySnapshot>;
	readonly inFlight: ReadonlyArray<InFlightSnapshot>;
	readonly stats: CacheStats;
}
export interface QueryEntrySnapshot {
	readonly kind: "query-entry";
	readonly portName: string;
	readonly params: unknown;
	readonly cacheKey: CacheKey;
	readonly status: string;
	readonly fetchStatus: "fetching" | "idle";
	readonly dataUpdatedAt: number | undefined;
	readonly errorUpdatedAt: number | undefined;
	readonly fetchCount: number;
	readonly hasSubscribers: boolean;
	readonly isInvalidated: boolean;
	readonly isStale: boolean;
	readonly data: unknown | "[truncated]";
	readonly error: unknown | null;
	readonly staleTime: number;
	readonly cacheTime: number;
}
export interface InFlightSnapshot {
	readonly kind: "in-flight";
	readonly portName: string;
	readonly params: unknown;
	readonly cacheKey: string;
	readonly startedAt: number;
	readonly elapsedMs: number;
	readonly retryAttempt: number;
}
export type QueryInspectorEvent = {
	kind: "fetch-started";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "fetch-completed";
	portName: string;
	params: unknown;
	durationMs: number;
	timestamp: number;
} | {
	kind: "fetch-cancelled";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "cache-hit";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "deduplicated";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "invalidated";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "gc-collected";
	key: string;
	timestamp: number;
} | {
	kind: "subscriber-added";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "subscriber-removed";
	portName: string;
	params: unknown;
	timestamp: number;
} | {
	kind: "retry";
	portName: string;
	params: unknown;
	attempt: number;
	timestamp: number;
} | {
	kind: "dependency-cycle-detected";
	chain: readonly string[];
	timestamp: number;
} | {
	kind: "mutation-effect-applied";
	mutationPortName: string;
	targetPortName: string;
	effect: "invalidates" | "removes";
	entriesAffected: number;
	timestamp: number;
};
export interface FetchHistoryEntry {
	readonly portName: string;
	readonly params: unknown;
	readonly cacheKey: string;
	readonly timestamp: number;
	readonly durationMs: number;
	readonly result: "ok" | "error";
	readonly errorTag?: string;
	readonly cacheHit: boolean;
	readonly deduplicated: boolean;
	readonly retryAttempt: number;
	readonly trigger: FetchTrigger;
}
export interface FetchHistoryFilter {
	readonly portName?: string;
	readonly result?: "ok" | "error";
	readonly minDurationMs?: number;
	readonly limit?: number;
	readonly since?: number;
	readonly trigger?: FetchHistoryEntry["trigger"];
}
export interface RuntimeInvalidationEdge {
	readonly from: string;
	readonly to: string;
	readonly effect: "invalidates" | "removes";
	readonly count: number;
	readonly lastTriggered: number;
	readonly totalEntriesAffected: number;
}
export interface InvalidationGraph {
	readonly nodes: ReadonlyArray<string>;
	readonly edges: ReadonlyArray<{
		from: string;
		to: string;
		type: "invalidates" | "removes";
	}>;
	readonly runtimeEdges: ReadonlyArray<RuntimeInvalidationEdge>;
	readonly cycles: ReadonlyArray<ReadonlyArray<string>>;
	readonly maxCascadeDepth: number;
	readonly warnings: ReadonlyArray<string>;
}
export interface QueryDependencyGraph {
	readonly staticEdges: ReadonlyArray<{
		from: string;
		to: string;
	}>;
	readonly dynamicEdges: ReadonlyArray<{
		from: string;
		to: string;
	}>;
	readonly cycles: ReadonlyArray<ReadonlyArray<string>>;
}
export interface QueryDiagnosticSummary {
	readonly totalQueries: number;
	readonly activeQueries: number;
	readonly staleQueries: number;
	readonly errorQueries: number;
	readonly inFlightCount: number;
	readonly cacheHitRate: number;
	readonly avgFetchDurationMs: number;
	readonly gcEligibleCount: number;
	readonly totalFetches: number;
	readonly errorRate: number;
	readonly dedupSavings: number;
	readonly errorsByTag: ReadonlyMap<string, number>;
}
export interface QuerySuggestion {
	readonly type: "stale_query" | "invalidation_storm" | "high_error_rate" | "large_cache_entry" | "unused_subscriber" | "missing_adapter";
	readonly portName: string;
	readonly message: string;
	readonly action: string;
}
export interface CacheStats {
	readonly totalEntries: number;
	readonly activeEntries: number;
	readonly staleEntries: number;
	readonly errorEntries: number;
	readonly inFlightCount: number;
	readonly cacheHitRate: number;
	readonly avgFetchDurationMs: number;
	readonly gcEligibleCount: number;
}
export interface MutationPortInfo {
	readonly name: string;
	readonly effects?: {
		readonly invalidates?: ReadonlyArray<{
			readonly __portName: string;
		}>;
		readonly removes?: ReadonlyArray<{
			readonly __portName: string;
		}>;
	};
}
export interface QueryPortInfo {
	readonly name: string;
	readonly dependsOn?: ReadonlyArray<{
		readonly __portName: string;
	}>;
}
export interface QueryInspectorOptions {
	readonly historySize?: number;
	readonly defaultStaleTime?: number;
	/** Mutation port metadata for building the invalidation graph */
	readonly mutationPorts?: ReadonlyArray<MutationPortInfo>;
	/** Query port metadata for building the dependency graph */
	readonly queryPorts?: ReadonlyArray<QueryPortInfo>;
}
export interface QueryInspectorAPI {
	getSnapshot(): QuerySnapshot;
	getQuerySnapshot(port: {
		readonly __portName: string;
	}, params?: unknown): QueryEntrySnapshot | undefined;
	subscribe(listener: CacheListener): () => void;
	getCacheStats(): CacheStats;
	getFetchHistory(filter?: FetchHistoryFilter): ReadonlyArray<FetchHistoryEntry>;
	getInvalidationGraph(): InvalidationGraph;
	getQueryDependencyGraph(): QueryDependencyGraph;
	getDiagnosticSummary(): QueryDiagnosticSummary;
	getQuerySuggestions(): ReadonlyArray<QuerySuggestion>;
	listQueryPorts(): ReadonlyArray<{
		name: string;
		entryCount: number;
		subscriberCount: number;
	}>;
	dispose(): void;
}
export declare function createQueryInspector(client: QueryClient, options?: QueryInspectorOptions): QueryInspectorAPI;
/**
 * Port for the QueryInspector service.
 */
export declare const QueryInspectorPort: DirectedPort<"QueryInspector", QueryInspectorAPI, "outbound", "query">;
/**
 * Creates a LibraryInspector that bridges QueryInspectorAPI
 * into the container's unified Library Inspector Protocol.
 *
 * The returned inspector:
 * - Reports \`name: "query"\`
 * - Exposes frozen cache stats, diagnostics, suggestions, and query port listing via \`getSnapshot()\`
 * - Forwards CacheEvents from \`inspector.subscribe()\` as LibraryEvents
 * - \`dispose()\` delegates to \`inspector.dispose()\`
 *
 * @param inspector - The QueryInspectorAPI instance to bridge
 * @returns A LibraryInspector for the query library
 *
 * @example
 * \`\`\`typescript
 * const libraryInspector = createQueryLibraryInspector(queryInspector);
 * container.inspector.registerLibrary(libraryInspector);
 *
 * const snapshot = libraryInspector.getSnapshot();
 * // { stats: { totalEntries: 5, ... }, diagnostics: { ... }, suggestions: [...], queryPorts: [...] }
 * \`\`\`
 */
export declare function createQueryLibraryInspector(inspector: QueryInspectorAPI): LibraryInspector;
/**
 * Query Library Inspector Port Definition
 *
 * @packageDocumentation
 */
/**
 * Port definition for the query library inspector bridge.
 *
 * When registered in a graph, provides a LibraryInspector that bridges
 * query inspection into the container's unified Library Inspector Protocol.
 */
export declare const QueryLibraryInspectorPort: DirectedPort<"QueryLibraryInspector", LibraryInspector, "outbound", "library-inspector">;
/**
 * Pre-built frozen adapter for QueryLibraryInspectorPort.
 *
 * Depends on QueryInspectorPort and produces a LibraryInspector bridge
 * via createQueryLibraryInspector. Because QueryLibraryInspectorPort
 * has \`category: "library-inspector"\`, the container's afterResolve hook
 * will auto-register it with the unified inspection protocol.
 *
 * @example
 * \`\`\`typescript
 * const graph = GraphBuilder.create()
 *   .provide(QueryInspectorAdapter)
 *   .provide(QueryLibraryInspectorAdapter)
 *   .build();
 *
 * // Auto-registered on first resolve thanks to category: "library-inspector"
 * const inspector = container.resolve(QueryLibraryInspectorPort);
 * \`\`\`
 */
export declare const QueryLibraryInspectorAdapter: Adapter<typeof QueryLibraryInspectorPort, typeof QueryInspectorPort, "singleton", "sync", false, readonly [
	typeof QueryInspectorPort
]>;
/**
 * Creates a QueryTracingHook instance for distributed tracing integration.
 *
 * @param options - Hook configuration with tracer, optional filter, and traceMutations flag
 * @returns A new QueryTracingHook
 */
export declare function createQueryTracingHook(options: QueryTracingHookOptions): QueryTracingHook;
/**
 * Configuration for the Query tracing bridge.
 */
export interface QueryTracingBridgeConfig {
	/**
	 * A TracerLike implementation (from @hex-di/tracing or any compatible adapter).
	 */
	readonly tracer: TracerLike;
	/**
	 * Optional filter to control which ports are traced.
	 * Return true to trace the port, false to skip.
	 */
	readonly filter?: (portName: string) => boolean;
	/**
	 * Whether to create spans for mutation operations.
	 * Defaults to true.
	 */
	readonly traceMutations?: boolean;
	/**
	 * Optional scope ID to include in span attributes.
	 */
	readonly scopeId?: string;
	/**
	 * Optional trace context entries to include in span attributes.
	 */
	readonly traceContext?: Record<string, string>;
}
/**
 * Creates QueryTracingHookOptions from a TracerLike implementation.
 *
 * This bridge adapts the @hex-di/tracing \`TracerLike\` interface
 * (\`pushSpan\`/\`popSpan\`) to the \`QueryTracingHookOptions\` expected
 * by \`createQueryTracingHook\`.
 *
 * @param config - The bridge configuration
 * @returns QueryTracingHookOptions ready to pass to \`createQueryTracingHook\`
 *
 * @example
 * \`\`\`typescript
 * import { createQueryTracingBridge, createQueryTracingHook } from '@hex-di/query';
 *
 * const hookOptions = createQueryTracingBridge({
 *   tracer: myTracerImplementation,
 *   filter: (portName) => portName !== 'internal',
 *   traceMutations: true,
 * });
 *
 * const tracingHook = createQueryTracingHook(hookOptions);
 *
 * const client = createQueryClient({
 *   container,
 *   tracingHook,
 * });
 * \`\`\`
 */
export declare function createQueryTracingBridge(config: QueryTracingBridgeConfig): QueryTracingHookOptions;
/**
 * Extract the data type from a QueryPort.
 * Infers from the config property to avoid phantom brand variance.
 */
export type InferQueryData<T> = T extends {
	readonly config: QueryPortConfig<infer TData, unknown, unknown, string, ReadonlyArray<AnyQueryPort>>;
} ? TData : InferenceError<"InferQueryData", "Expected a QueryPort type. Use InferQueryData<typeof YourPort>.", T>;
/** Extract the params type from a QueryPort */
export type InferQueryParams<T> = T extends {
	readonly config: QueryPortConfig<unknown, infer TParams, unknown, string, ReadonlyArray<AnyQueryPort>>;
} ? TParams : InferenceError<"InferQueryParams", "Expected a QueryPort type. Use InferQueryParams<typeof YourPort>.", T>;
/** Extract the error type from a QueryPort */
export type InferQueryError<T> = T extends {
	readonly config: QueryPortConfig<unknown, unknown, infer TError, string, ReadonlyArray<AnyQueryPort>>;
} ? TError : InferenceError<"InferQueryError", "Expected a QueryPort type. Use InferQueryError<typeof YourPort>.", T>;
/** Extract the name literal type from a QueryPort */
export type InferQueryName<T> = T extends {
	readonly __portName: infer TName extends string;
} ? TName : InferenceError<"InferQueryName", "Expected a QueryPort type. Use InferQueryName<typeof YourPort>.", T>;
/** Extract the dependsOn tuple from a QueryPort */
export type InferQueryDependsOn<T> = T extends {
	readonly config: {
		readonly dependsOn?: infer TDeps;
	};
} ? TDeps extends ReadonlyArray<AnyQueryPort> ? TDeps : [
] : InferenceError<"InferQueryDependsOn", "Expected a QueryPort type. Use InferQueryDependsOn<typeof YourPort>.", T>;
/** Extract dependency port names as a union */
export type InferQueryDependencyNames<T> = T extends {
	readonly config: {
		readonly dependsOn?: infer TDeps;
	};
} ? TDeps extends ReadonlyArray<{
	readonly __portName: infer TName extends string;
}> ? TName : never : InferenceError<"InferQueryDependencyNames", "Expected a QueryPort type.", T>;
/** Extract all types from a QueryPort at once */
export type InferQueryTypes<T> = T extends {
	readonly __portName: infer TName extends string;
	readonly config: QueryPortConfig<infer TData, infer TParams, infer TError, string, infer TDependsOn>;
} ? TDependsOn extends ReadonlyArray<AnyQueryPort> ? {
	readonly name: TName;
	readonly data: TData;
	readonly params: TParams;
	readonly error: TError;
	readonly dependsOn: TDependsOn;
} : InferenceError<"InferQueryTypes", "Failed to infer dependsOn.", T> : InferenceError<"InferQueryTypes", "Expected a QueryPort type. Use InferQueryTypes<typeof YourPort>.", T>;
/** True if query port has non-void params */
export type HasParams<T> = [
	InferQueryParams<T>
] extends [
	void
] ? false : true;
/** Extract the data (return) type from a MutationPort */
export type InferMutationData<T> = T extends {
	readonly config: MutationPortConfig<infer TData, unknown, unknown, unknown, string>;
} ? TData : InferenceError<"InferMutationData", "Expected a MutationPort type. Use InferMutationData<typeof YourPort>.", T>;
/** Extract the input type from a MutationPort */
export type InferMutationInput<T> = T extends {
	readonly config: MutationPortConfig<unknown, infer TInput, unknown, unknown, string>;
} ? TInput : InferenceError<"InferMutationInput", "Expected a MutationPort type. Use InferMutationInput<typeof YourPort>.", T>;
/** Extract the error type from a MutationPort */
export type InferMutationError<T> = T extends {
	readonly config: MutationPortConfig<unknown, unknown, infer TError, unknown, string>;
} ? TError : InferenceError<"InferMutationError", "Expected a MutationPort type. Use InferMutationError<typeof YourPort>.", T>;
/** Extract the context type from a MutationPort */
export type InferMutationContext<T> = T extends {
	readonly config: MutationPortConfig<unknown, unknown, unknown, infer TContext, string>;
} ? TContext : InferenceError<"InferMutationContext", "Expected a MutationPort type. Use InferMutationContext<typeof YourPort>.", T>;
/** Extract the name literal type from a MutationPort */
export type InferMutationName<T> = T extends {
	readonly __portName: infer TName extends string;
} ? TName : InferenceError<"InferMutationName", "Expected a MutationPort type. Use InferMutationName<typeof YourPort>.", T>;
/** Extract all types from a MutationPort at once */
export type InferMutationTypes<T> = T extends {
	readonly __portName: infer TName extends string;
	readonly config: MutationPortConfig<infer TData, infer TInput, infer TError, infer TContext, string>;
} ? {
	readonly name: TName;
	readonly data: TData;
	readonly input: TInput;
	readonly error: TError;
	readonly context: TContext;
} : InferenceError<"InferMutationTypes", "Expected a MutationPort type. Use InferMutationTypes<typeof YourPort>.", T>;
/** Extract the list of invalidated port names from a MutationPort */
export type InferInvalidatedPorts<T> = T extends {
	readonly config: {
		readonly effects: {
			readonly invalidates: infer TPorts;
		};
	};
} ? TPorts extends ReadonlyArray<{
	readonly __portName: infer TName extends string;
}> ? TName : never : never;
/** Extract the list of removed port names from a MutationPort */
export type InferRemovedPorts<T> = T extends {
	readonly config: {
		readonly effects: {
			readonly removes: infer TPorts;
		};
	};
} ? TPorts extends ReadonlyArray<{
	readonly __portName: infer TName extends string;
}> ? TName : never : never;
/**
 * Maps a dependency tuple to a record of \`{ portName: dataType }\`.
 *
 * Given \`[QueryPort<"users", User[], ...>, QueryPort<"posts", Post[], ...>]\`,
 * produces \`{ readonly users: User[]; readonly posts: Post[] }\`.
 */
export type DependencyData<TDependsOn extends ReadonlyArray<AnyQueryPort>> = {
	readonly [K in TDependsOn[number] as InferQueryName<K>]: InferQueryData<K>;
};
/**
 * Maps a dependency tuple to a record of \`{ portName: paramsType }\`.
 *
 * Given \`[QueryPort<"users", ..., { role: string }, ...>]\`,
 * produces \`{ readonly users: { role: string } }\`.
 */
export type DependencyParamsMap<TDependsOn extends ReadonlyArray<AnyQueryPort>> = {
	readonly [K in TDependsOn[number] as InferQueryName<K>]: InferQueryParams<K>;
};
/**
 * Compile-time validators for query dependency graphs and mutation effects.
 *
 * These types produce human-readable error messages at compile time when
 * invalid configurations are detected.
 *
 * @packageDocumentation
 */
/**
 * Minimal structural type for any object that has a \`__portName\` property.
 * Used instead of \`AnyQueryPort\` to avoid variance issues with contravariant
 * type parameters (e.g. TParams in QueryFetcher).
 */
export type NamedPort = {
	readonly __portName: string;
};
/**
 * Validates that a query port's dependsOn array does not create circular deps.
 * Checks that the port itself is not in its dependency chain.
 *
 * Returns \`true\` if valid, or an error string literal if invalid.
 */
export type ValidateQueryDependencies<TPortName extends string, TDependsOn extends ReadonlyArray<NamedPort>> = [
	ExtractPortNames<TDependsOn>
] extends [
	never
] ? true : TPortName extends ExtractPortNames<TDependsOn> ? \`Error: Query port '\${TPortName}' has a circular dependency on itself.\` : true;
/**
 * Given a set of dependency port names and available port names,
 * returns the missing port names or \`never\` if all are available.
 */
export type FindMissingPorts<TDependsOn extends ReadonlyArray<NamedPort>, TAvailable extends string> = Exclude<ExtractPortNames<TDependsOn>, TAvailable>;
/**
 * Validates that mutation effects (invalidates/removes) reference
 * ports that exist in the available set.
 *
 * Returns \`true\` if valid, or an error string literal if invalid.
 */
export type ValidateMutationEffects<TEffects extends {
	readonly invalidates?: ReadonlyArray<NamedPort>;
	readonly removes?: ReadonlyArray<NamedPort>;
}, TAvailable extends string> = ValidateEffectList<ExtractInvalidateNames<TEffects>, TAvailable, "invalidates"> extends true ? ValidateEffectList<ExtractRemoveNames<TEffects>, TAvailable, "removes"> : ValidateEffectList<ExtractInvalidateNames<TEffects>, TAvailable, "invalidates">;
/**
 * Validates that an adapter's lifetime is not shorter than its dependencies'.
 * A singleton adapter must not depend on a scoped or transient adapter.
 *
 * Lifetime hierarchy: singleton > scoped > transient
 */
export type ValidateQueryAdapterLifetime<TAdapterLifetime extends "singleton" | "scoped" | "transient", TDepLifetimes extends ReadonlyArray<"singleton" | "scoped" | "transient">> = TAdapterLifetime extends "singleton" ? HasNonSingleton<TDepLifetimes> extends true ? \`Error: Singleton adapter cannot depend on scoped or transient dependencies (captive dependency).\` : true : TAdapterLifetime extends "scoped" ? HasTransient<TDepLifetimes> extends true ? \`Error: Scoped adapter cannot depend on transient dependencies (captive dependency).\` : true : true;
/** Extract port names from a ReadonlyArray of named ports */
export type ExtractPortNames<TPorts extends ReadonlyArray<NamedPort>> = TPorts extends readonly [
] ? never : TPorts[number]["__portName"];
/** Extract invalidation target names from mutation effects */
export type ExtractInvalidateNames<TEffects> = TEffects extends {
	readonly invalidates: readonly [
		NamedPort,
		...ReadonlyArray<NamedPort>
	];
} ? TEffects["invalidates"][number]["__portName"] : never;
/** Extract removal target names from mutation effects */
export type ExtractRemoveNames<TEffects> = TEffects extends {
	readonly removes: readonly [
		NamedPort,
		...ReadonlyArray<NamedPort>
	];
} ? TEffects["removes"][number]["__portName"] : never;
/** Validate an effect list against available ports */
export type ValidateEffectList<TEffectNames extends string, TAvailable extends string, TEffectType extends string> = [
	Exclude<TEffectNames, TAvailable>
] extends [
	never
] ? true : \`Error: Mutation \${TEffectType} references unknown ports: \${Exclude<TEffectNames, TAvailable>}\`;
/** Check if any lifetime in the array is non-singleton */
export type HasNonSingleton<T extends ReadonlyArray<string>> = T extends ReadonlyArray<infer U> ? "scoped" extends U ? true : "transient" extends U ? true : false : false;
/** Check if any lifetime in the array is transient */
export type HasTransient<T extends ReadonlyArray<string>> = T extends ReadonlyArray<infer U> ? ("transient" extends U ? true : false) : false;
/**
 * A type-level adjacency map. Maps node names to their neighbor names.
 * Example: \`{ A: "B" | "C"; B: "D"; C: never; D: never }\`
 */
export type AdjacencyMap = Record<string, string>;
/**
 * Adds a directed edge from TFrom to TTo in the graph.
 * Merges the new neighbor into the existing neighbor union for TFrom.
 */
export type AddEdge<TGraph extends AdjacencyMap, TFrom extends string, TTo extends string> = TGraph & {
	readonly [K in TFrom]: K extends keyof TGraph ? TGraph[K] | TTo : TTo;
};
/**
 * Checks if TTo is reachable from TFrom in TGraph.
 *
 * Uses a visited set (TVisited) to prevent infinite recursion on cycles.
 * Returns \`true\` if reachable, \`false\` otherwise.
 */
export type IsReachable<TGraph extends AdjacencyMap, TFrom extends string, TTo extends string, TVisited extends string = never> = TFrom extends TVisited ? false : TFrom extends keyof TGraph ? TTo extends TGraph[TFrom] ? true : IsReachableViaNeighbors<TGraph, TGraph[TFrom], TTo, TVisited | TFrom> : false;
/**
 * Distributes IsReachable over a union of neighbor nodes.
 * Returns \`true\` if any neighbor can reach TTo.
 *
 * Uses \`true extends X ? true : ...\` to short-circuit when distribution
 * produces \`true\` for any branch (union distributes, then we check if
 * \`true\` is in the resulting union).
 */
export type IsReachableViaNeighbors<TGraph extends AdjacencyMap, TNeighbors extends string, TTo extends string, TVisited extends string> = [
	TNeighbors
] extends [
	never
] ? false : true extends (TNeighbors extends infer N extends string ? IsReachable<TGraph, N, TTo, TVisited> : never) ? true : false;

export {
	MutationObserver\$1 as MutationObserver,
};

export {};
}`;
