/**
 * Default context key for storing the HexDI scope on the Hono Context.
 */
export const DEFAULT_SCOPE_KEY = "hexScope" as const;

/**
 * Default context key for storing the HexDI container on the Hono Context.
 */
export const DEFAULT_CONTAINER_KEY = "hexContainer" as const;
