# Raw Idea

Enhanced DevTools Architecture Visualization - Container Tree and Port/Adapter Graph

The user wants to:

- See the containers tree showing all containers in the application
- View a graph of all ports where each port can have multiple adapters
- Show adapter ownership per container: owned, inherited, or overridden by different adapter
- Display the complete application architecture
