# Plan 18-04: Tracer API Tests

```yaml
wave: 2
depends_on: []
files_modified:
  - packages/runtime/tests/tracer.test.ts [CREATE]
autonomous: true
```

## Goal

Create comprehensive test coverage for the TracingAPI with 16+ tests covering filtering, overhead measurement, cross-scope tracing, subscriptions, and statistics computation.

## Context

From research:

- TracingAPI exposed via `container.tracer` property (always available)
- TraceFilter supports 7 dimensions: portName, lifetime, isCacheHit, minDuration, maxDuration, scopeId, isPinned
- Methods: getTraces, getStats, pause/resume, clear, subscribe, pin/unpin
- Standalone functions: trace(), enableTracing()

From Phase 18 context:

- Full filter coverage: test each dimension independently
- Overhead test: verify disabled tracing has negligible cost
- Cross-scope: trace across parent/child boundaries
- Real-time subscriptions and stats computation

## Tasks

<task>
Create packages/runtime/tests/tracer.test.ts with comprehensive tracing tests

File structure:

- Filter coverage (7 tests, one per dimension)
  - Filter by portName (partial match, case-insensitive)
  - Filter by lifetime (singleton/transient/scoped)
  - Filter by isCacheHit (true/false)
  - Filter by minDuration (inclusive threshold)
  - Filter by maxDuration (inclusive threshold)
  - Filter by scopeId (specific scope or null for root)
  - Filter by isPinned (manually pinned traces)
- Combined filters (2 tests)
  - Multiple filter dimensions work together (AND logic)
  - Complex filter with 3+ dimensions
- Overhead test (1 test)
  - Measure resolution time with tracing paused vs resumed
  - Verify negligible overhead when paused
- Cross-scope tracing (2 tests)
  - Traces captured across parent->child resolution chain
  - Traces show correct containerId for each level
- Subscription tests (2 tests)
  - Real-time subscription receives new traces
  - Unsubscribe stops trace delivery
- Stats computation (2 tests)
  - getStats returns accurate aggregate metrics
  - Stats update as new traces are added
    </task>

<task>
Add tests for standalone tracing functions

Test the high-level API:

- trace() function captures resolution within callback
- enableTracing() returns functional unsubscribe
- Standalone functions work with any container
  </task>

<task>
Add tests for trace lifecycle management

Trace buffer management:

- pin() protects traces from eviction
- unpin() makes traces eligible for eviction
- clear() removes all non-pinned traces
- pause()/resume() control trace recording
  </task>

<task>
Create realistic test scenarios with timing

Set up adapters with delays:

- Fast singleton (< 1ms)
- Slow singleton (50ms delay)
- Variable transient (random 1-20ms)
- Async factory (100ms)

This provides realistic timing data for filter tests.
</task>

## Verification

Run tests and verify:

```bash
cd packages/runtime
pnpm test tracer.test.ts
```

Expected output:

- 16+ passing tests covering all tracer scenarios
- Each filter dimension tested independently
- Overhead measurement proves negligible cost
- Cross-scope tracing validated
- Stats and subscriptions working correctly

## must_haves

- [ ] 7 tests covering each TraceFilter dimension
- [ ] Overhead test measuring actual timing difference
- [ ] Cross-scope tests with parent/child containers
- [ ] Subscription tests with real-time updates
- [ ] Stats tests validating aggregation logic
- [ ] Realistic timing scenarios with actual delays
