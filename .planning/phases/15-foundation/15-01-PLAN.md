---
phase: 15-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/types/index.ts
  - packages/runtime/src/types/container.ts
  - packages/runtime/src/types/scope.ts
  - packages/runtime/src/types/lazy-container.ts
  - packages/runtime/src/types/options.ts
  - packages/runtime/src/types/inheritance.ts
  - packages/runtime/src/types/brands.ts
  - packages/runtime/src/types/inference.ts
  - packages/runtime/src/types.ts
  - packages/runtime/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Types are importable from types/ subdirectory without circular dependency errors"
    - "All existing type exports from @hex-di/runtime still work"
    - "Each type file is under 400 lines"
    - "Type guards stay with their types (not separate files)"
  artifacts:
    - path: "packages/runtime/src/types/index.ts"
      provides: "Central re-exports for all type files"
      contains: "export type { Container"
    - path: "packages/runtime/src/types/container.ts"
      provides: "Container and ContainerMembers types"
      contains: "export type Container"
    - path: "packages/runtime/src/types/scope.ts"
      provides: "Scope and ScopeMembers types"
      contains: "export type Scope"
    - path: "packages/runtime/src/types/brands.ts"
      provides: "ContainerBrand and ScopeBrand symbols"
      contains: "export const ContainerBrand"
  key_links:
    - from: "packages/runtime/src/types/container.ts"
      to: "packages/runtime/src/types/brands.ts"
      via: "import ContainerBrand"
      pattern: "import.*ContainerBrand.*from.*brands"
    - from: "packages/runtime/src/index.ts"
      to: "packages/runtime/src/types/index.ts"
      via: "re-export from types/"
      pattern: "from.*types/index"
---

<objective>
Split types.ts (1,271 lines) into focused type files in a types/ subdirectory.

Purpose: Improve maintainability by organizing types by entity (Container, Scope, LazyContainer, etc.) with each file under 400 lines. Enables easier navigation and prevents the monolithic types file from growing further.

Output: A types/ subdirectory with 8 focused files, all existing exports preserved via index.ts re-exports.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-foundation/15-CONTEXT.md
@.planning/phases/15-foundation/15-RESEARCH.md
@packages/runtime/src/types.ts
@packages/runtime/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create types/ subdirectory structure with leaf files first</name>
  <files>
    packages/runtime/src/types/brands.ts
    packages/runtime/src/types/options.ts
    packages/runtime/src/types/inheritance.ts
  </files>
  <action>
Create the types/ subdirectory and start with "leaf" files that have no internal dependencies (prevents circular imports):

**brands.ts** (~50 lines):

- Move `ContainerBrand` and `ScopeBrand` symbol declarations from types.ts
- These are leaf nodes - no imports from other type files
- Include JSDoc comments

**options.ts** (~120 lines):

- Move `ContainerPhase`, `ContainerKind`, `ContainerDevToolsOptions`, `CreateContainerOptions`, `CreateChildOptions` from types.ts
- Import from brands.ts if needed
- Include JSDoc comments

**inheritance.ts** (~80 lines):

- Move `InheritanceMode`, `InheritanceModeMap`, `InheritanceModeConfig` from types.ts
- Move `ExtractPortNames` internal utility type (needed for InheritanceModeConfig)
- Import from brands.ts only (leaf-level)
- Include JSDoc comments

Import hierarchy: brands.ts <- options.ts, inheritance.ts (no cycles possible)
</action>
<verify>
Run `pnpm --filter @hex-di/runtime typecheck` - should pass (types.ts still exists with remaining types)
</verify>
<done>
Three leaf type files exist in types/ subdirectory with proper JSDoc and no circular dependencies
</done>
</task>

<task type="auto">
  <name>Task 2: Create entity type files (Container, Scope, LazyContainer)</name>
  <files>
    packages/runtime/src/types/container.ts
    packages/runtime/src/types/scope.ts
    packages/runtime/src/types/lazy-container.ts
  </files>
  <action>
Create the main entity type files:

**container.ts** (~400 lines):

- Move `Container` type alias and `ContainerMembers` type from types.ts
- Import `ContainerBrand` from "./brands.js"
- Import `ContainerPhase`, `ContainerKind`, `CreateChildOptions` from "./options.js"
- Import `InheritanceModeConfig` from "./inheritance.js"
- Import external types from @hex-di/core, @hex-di/graph, inspection modules
- Preserve ALL JSDoc comments (they are extensive and valuable)
- Include `InferServiceByName` internal utility type (used in ContainerMembers.withOverrides)

**scope.ts** (~200 lines):

- Move `Scope` type alias and `ScopeMembers` type from types.ts
- Import `ScopeBrand` from "./brands.js"
- Import `ContainerPhase` from "./options.js"
- Import external types from @hex-di/core, inspection modules
- Preserve ALL JSDoc comments

**lazy-container.ts** (~150 lines):

- Move `LazyContainer` type alias and `LazyContainerMembers` type from types.ts
- Import `Container` from "./container.js"
- Import `ContainerPhase` from "./options.js"
- Preserve ALL JSDoc comments

Import hierarchy: brands.ts <- options.ts <- container.ts, scope.ts <- lazy-container.ts (DAG, no cycles)
</action>
<verify>
Run `pnpm --filter @hex-di/runtime typecheck` - should pass
</verify>
<done>
Container, Scope, and LazyContainer types exist in separate files with preserved JSDoc and correct import hierarchy
</done>
</task>

<task type="auto">
  <name>Task 3: Create inference types file and index.ts, update main types.ts</name>
  <files>
    packages/runtime/src/types/inference.ts
    packages/runtime/src/types/index.ts
    packages/runtime/src/types.ts
    packages/runtime/src/index.ts
  </files>
  <action>
Complete the split and wire up re-exports:

**inference.ts** (~150 lines):

- Move `InferContainerProvides`, `InferContainerEffectiveProvides`, `InferScopeProvides` from types.ts
- Move `IsResolvable`, `ServiceFromContainer` from types.ts
- Move `IsRootContainer`, `IsChildContainer` from types.ts
- Import `Container` from "./container.js"
- Import `Scope` from "./scope.js"
- Preserve ALL JSDoc comments

**index.ts** (~80 lines - re-exports only):

- Re-export ALL public types from each file:
  - `export type { Container, ContainerMembers } from "./container.js"`
  - `export type { Scope, ScopeMembers } from "./scope.js"`
  - `export type { LazyContainer } from "./lazy-container.js"`
  - `export type { ContainerPhase, ContainerKind, ContainerDevToolsOptions, CreateContainerOptions, CreateChildOptions } from "./options.js"`
  - `export type { InheritanceMode, InheritanceModeMap, InheritanceModeConfig } from "./inheritance.js"`
  - `export { ContainerBrand, ScopeBrand } from "./brands.js"` (value exports, not type-only)
  - `export type { InferContainerProvides, InferContainerEffectiveProvides, InferScopeProvides, IsResolvable, ServiceFromContainer, IsRootContainer, IsChildContainer } from "./inference.js"`
- NO logic, only re-exports

**Update types.ts**:

- Remove all moved types (Container, Scope, LazyContainer, options, inheritance, brands, inference)
- Replace with re-exports from types/index.ts:
  ```typescript
  // Re-export all types from types/ subdirectory
  export * from "./types/index.js";
  ```
- Keep any imports that are still needed by remaining code (INTERNAL_ACCESS import, etc.)
- File should be ~20 lines after this change

**Update index.ts** (main package export):

- Change imports from `"./types.js"` to `"./types/index.js"` where direct types/ access is cleaner
- OR keep importing from types.ts (which re-exports from types/) - either works
- Verify all existing exports still work
  </action>
  <verify>
  Run these commands:

1. `pnpm --filter @hex-di/runtime typecheck` - type checking passes
2. `pnpm --filter @hex-di/runtime build` - build succeeds
3. `pnpm test --filter @hex-di/runtime` - all tests pass
4. `wc -l packages/runtime/src/types/*.ts` - verify each file under 400 lines
   </verify>
   <done>
   types.ts is now a thin re-export file (~20 lines), all types live in types/ subdirectory, all existing exports preserved, no breaking changes to public API
   </done>
   </task>

</tasks>

<verification>
1. Run full typecheck: `pnpm typecheck`
2. Run full test suite: `pnpm test`
3. Verify file sizes: `wc -l packages/runtime/src/types/*.ts` (all under 400 lines)
4. Verify no circular dependencies: `pnpm --filter @hex-di/runtime build` succeeds
5. Verify existing imports work: Check that examples still compile
</verification>

<success_criteria>

1. types.ts reduced from 1,271 lines to ~20 lines (re-exports only)
2. 8 focused type files exist in types/ subdirectory
3. Each type file is under 400 lines
4. All existing @hex-di/runtime type exports still work (no breaking changes)
5. No circular dependency errors
6. All tests pass
   </success_criteria>

<output>
After completion, create `.planning/phases/15-foundation/15-01-SUMMARY.md`
</output>
