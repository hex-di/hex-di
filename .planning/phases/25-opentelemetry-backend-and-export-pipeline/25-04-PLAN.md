---
phase: 25-opentelemetry-backend-and-export-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["25-01", "25-03"]
files_modified:
  [
    packages/tracing-jaeger/package.json,
    packages/tracing-jaeger/tsconfig.json,
    packages/tracing-jaeger/eslint.config.js,
    packages/tracing-jaeger/src/index.ts,
    packages/tracing-jaeger/src/exporter.ts,
    packages/tracing-zipkin/package.json,
    packages/tracing-zipkin/tsconfig.json,
    packages/tracing-zipkin/eslint.config.js,
    packages/tracing-zipkin/src/index.ts,
    packages/tracing-zipkin/src/exporter.ts,
  ]
autonomous: true

must_haves:
  truths:
    - "Jaeger exporter sends HexDI spans to Jaeger backend"
    - "Zipkin exporter sends HexDI spans to Zipkin backend"
    - "Both exporters implement SpanExporterPort interface"
  artifacts:
    - path: "packages/tracing-jaeger/src/exporter.ts"
      provides: "Jaeger exporter adapter"
      exports: ["createJaegerExporter"]
    - path: "packages/tracing-zipkin/src/exporter.ts"
      provides: "Zipkin exporter adapter"
      exports: ["createZipkinExporter"]
  key_links:
    - from: "packages/tracing-jaeger/src/exporter.ts"
      to: "@opentelemetry/exporter-jaeger"
      via: "wraps Jaeger exporter"
      pattern: "JaegerExporter"
    - from: "packages/tracing-zipkin/src/exporter.ts"
      to: "@opentelemetry/exporter-zipkin"
      via: "wraps Zipkin exporter"
      pattern: "ZipkinExporter"
---

<objective>
Create Jaeger and Zipkin backend adapters as thin wrappers over OTel exporters

Purpose: Enable direct export to Jaeger and Zipkin backends using their native protocols via OpenTelemetry exporters
Output: Lightweight backend-specific packages (BACK-01, BACK-02, BACK-04)
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-RESEARCH.md
@.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-01-SUMMARY.md
@.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-03-SUMMARY.md

# SpanExporter port to implement

@packages/tracing/src/ports/exporter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jaeger package structure</name>
  <files>packages/tracing-jaeger/package.json, packages/tracing-jaeger/tsconfig.json, packages/tracing-jaeger/eslint.config.js</files>
  <action>
    Create the @hex-di/tracing-jaeger package.

    Create packages/tracing-jaeger/package.json:
    - name: "@hex-di/tracing-jaeger"
    - version: "0.1.0"
    - type: "module"
    - exports: "./dist/index.js"
    - types: "./dist/index.d.ts"
    - dependencies:
      - "@hex-di/tracing": "workspace:^"
      - "@hex-di/tracing-otel": "workspace:^"
      - "@opentelemetry/exporter-jaeger": "^2.5.0"
      - "@opentelemetry/resources": "^2.5.0"
    - devDependencies: typescript, vitest
    - scripts: build, test, typecheck, lint

    Create tsconfig.json extending ../../tsconfig.base.json:
    - compilerOptions.outDir: "./dist"
    - include: ["src/**/*"]
    - references to @hex-di/tracing and @hex-di/tracing-otel

    Create eslint.config.js extending base config

  </action>
  <verify>pnpm --filter @hex-di/tracing-jaeger install && pnpm --filter @hex-di/tracing-jaeger typecheck</verify>
  <done>Jaeger package structure created with dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Implement Jaeger exporter</name>
  <files>packages/tracing-jaeger/src/exporter.ts, packages/tracing-jaeger/src/index.ts</files>
  <action>
    Create Jaeger exporter adapter.

    In packages/tracing-jaeger/src/exporter.ts:
    - Import SpanExporter, SpanData from @hex-di/tracing
    - Import JaegerExporter from @opentelemetry/exporter-jaeger
    - Import convertToReadableSpan, createResource from @hex-di/tracing-otel
    - Create JaegerExporterOptions interface:
      - endpoint?: string (default: 'http://localhost:14268/api/traces')
      - serviceName: string (required)
      - serviceVersion?: string
      - deploymentEnvironment?: string
    - Create createJaegerExporter function:
      - Takes JaegerExporterOptions
      - Creates Resource using createResource from tracing-otel
      - Creates underlying JaegerExporter with endpoint
      - Returns SpanExporter implementation (BACK-04):
        - export(spans): Convert each SpanData using convertToReadableSpan, attach resource, delegate to Jaeger
        - forceFlush(): delegate to underlying exporter
        - shutdown(): delegate to underlying exporter
      - Handle errors gracefully (log but don't throw)

    In packages/tracing-jaeger/src/index.ts:
    - Export createJaegerExporter
    - Export JaegerExporterOptions
    - Add JSDoc explaining this uses Jaeger's native Thrift protocol

  </action>
  <verify>pnpm --filter @hex-di/tracing-jaeger build && pnpm --filter @hex-di/tracing-jaeger typecheck</verify>
  <done>Jaeger exporter implemented with SpanExporterPort interface</done>
</task>

<task type="auto">
  <name>Task 3: Create Zipkin package structure</name>
  <files>packages/tracing-zipkin/package.json, packages/tracing-zipkin/tsconfig.json, packages/tracing-zipkin/eslint.config.js</files>
  <action>
    Create the @hex-di/tracing-zipkin package.

    Create packages/tracing-zipkin/package.json:
    - name: "@hex-di/tracing-zipkin"
    - version: "0.1.0"
    - type: "module"
    - exports: "./dist/index.js"
    - types: "./dist/index.d.ts"
    - dependencies:
      - "@hex-di/tracing": "workspace:^"
      - "@hex-di/tracing-otel": "workspace:^"
      - "@opentelemetry/exporter-zipkin": "^2.5.0"
      - "@opentelemetry/resources": "^2.5.0"
    - devDependencies: typescript, vitest
    - scripts: build, test, typecheck, lint

    Create tsconfig.json and eslint.config.js similar to Jaeger package

  </action>
  <verify>pnpm --filter @hex-di/tracing-zipkin install && pnpm --filter @hex-di/tracing-zipkin typecheck</verify>
  <done>Zipkin package structure created with dependencies</done>
</task>

<task type="auto">
  <name>Task 4: Implement Zipkin exporter</name>
  <files>packages/tracing-zipkin/src/exporter.ts, packages/tracing-zipkin/src/index.ts</files>
  <action>
    Create Zipkin exporter adapter.

    In packages/tracing-zipkin/src/exporter.ts:
    - Import SpanExporter, SpanData from @hex-di/tracing
    - Import ZipkinExporter from @opentelemetry/exporter-zipkin
    - Import convertToReadableSpan, createResource from @hex-di/tracing-otel
    - Create ZipkinExporterOptions interface:
      - url?: string (default: 'http://localhost:9411/api/v2/spans')
      - serviceName: string (required)
      - serviceVersion?: string
      - deploymentEnvironment?: string
    - Create createZipkinExporter function:
      - Takes ZipkinExporterOptions
      - Creates Resource using createResource
      - Creates underlying ZipkinExporter with url
      - Returns SpanExporter implementation (BACK-04):
        - export(spans): Convert, attach resource, delegate
        - forceFlush(): delegate
        - shutdown(): delegate
      - Error handling as with Jaeger

    In packages/tracing-zipkin/src/index.ts:
    - Export createZipkinExporter
    - Export ZipkinExporterOptions
    - Add JSDoc explaining this uses Zipkin's JSON v2 API

  </action>
  <verify>pnpm --filter @hex-di/tracing-zipkin build && pnpm --filter @hex-di/tracing-zipkin typecheck</verify>
  <done>Zipkin exporter implemented with SpanExporterPort interface</done>
</task>

</tasks>

<verification>
# Build both packages
pnpm --filter @hex-di/tracing-jaeger build
pnpm --filter @hex-di/tracing-zipkin build

# Typecheck

pnpm --filter @hex-di/tracing-jaeger typecheck
pnpm --filter @hex-di/tracing-zipkin typecheck

# Verify exports

node -e "import('@hex-di/tracing-jaeger').then(m => console.log('Jaeger:', !!m.createJaegerExporter))"
node -e "import('@hex-di/tracing-zipkin').then(m => console.log('Zipkin:', !!m.createZipkinExporter))"

# Check no type casts

grep -r "as \|<.\*>" packages/tracing-jaeger/src/ packages/tracing-zipkin/src/ || echo "✓ No type casts"
</verification>

<success_criteria>

- [ ] Jaeger package created with OTel Jaeger exporter dependency
- [ ] Zipkin package created with OTel Zipkin exporter dependency
- [ ] Both exporters implement SpanExporterPort interface
- [ ] SpanData converted to ReadableSpan format
- [ ] Resource metadata attached to spans
- [ ] Error handling prevents export failures from throwing
      </success_criteria>

<output>
After completion, create `.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-04-SUMMARY.md`
</output>
