---
phase: 25-opentelemetry-backend-and-export-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  [
    packages/tracing-otel/src/exporters/otlp-http.ts,
    packages/tracing-otel/src/exporters/types.ts,
    packages/tracing-otel/src/resources/resource.ts,
    packages/tracing-otel/src/semantic-conventions/mapper.ts,
    packages/tracing-otel/src/index.ts,
  ]
autonomous: true

must_haves:
  truths:
    - "OTLP HTTP exporter sends spans to OpenTelemetry collectors"
    - "Resource metadata identifies service name, version, and environment"
    - "HexDI attributes are mapped to OTel semantic conventions"
  artifacts:
    - path: "packages/tracing-otel/src/exporters/otlp-http.ts"
      provides: "OTLP HTTP exporter adapter"
      exports: ["createOtlpHttpExporter"]
    - path: "packages/tracing-otel/src/resources/resource.ts"
      provides: "Resource metadata builder"
      exports: ["createResource"]
    - path: "packages/tracing-otel/src/semantic-conventions/mapper.ts"
      provides: "Attribute mapping utilities"
      exports: ["mapHexDiToOtelAttributes"]
  key_links:
    - from: "packages/tracing-otel/src/exporters/otlp-http.ts"
      to: "@opentelemetry/exporter-trace-otlp-http"
      via: "wraps OTel exporter"
      pattern: "OTLPTraceExporter"
---

<objective>
Implement OTLP HTTP exporter with resource metadata and semantic convention mapping

Purpose: Enable HexDI spans to be sent to any OTLP-compatible collector with proper service identification and standard attributes
Output: OTLP exporter adapter with resource and attribute mapping (OTEL-05, OTEL-06, OTEL-07)
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-RESEARCH.md
@.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-01-SUMMARY.md

# SpanExporter port we're implementing

@packages/tracing/src/ports/exporter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resource metadata builder</name>
  <files>packages/tracing-otel/src/resources/resource.ts</files>
  <action>
    Create a resource builder for service identification metadata.

    In packages/tracing-otel/src/resources/resource.ts:
    - Import Resource from @opentelemetry/resources
    - Import ATTR_SERVICE_NAME, ATTR_SERVICE_VERSION, ATTR_DEPLOYMENT_ENVIRONMENT from @opentelemetry/semantic-conventions
    - Create ResourceConfig interface:
      - serviceName: string (required)
      - serviceVersion?: string
      - deploymentEnvironment?: string
      - serviceNamespace?: string
      - attributes?: Record<string, string | number | boolean>
    - Create createResource function:
      - Takes ResourceConfig
      - Builds attributes object with semantic convention keys
      - ATTR_SERVICE_NAME from serviceName (OTEL-06 required)
      - ATTR_SERVICE_VERSION from serviceVersion if provided
      - ATTR_DEPLOYMENT_ENVIRONMENT from deploymentEnvironment if provided
      - Merge any additional attributes
      - Return new Resource(attributes)
    - Add JSDoc explaining standard attributes and their purpose

  </action>
  <verify>pnpm --filter @hex-di/tracing-otel typecheck</verify>
  <done>Resource builder created with standard service identification attributes</done>
</task>

<task type="auto">
  <name>Task 2: Create semantic convention mapper</name>
  <files>packages/tracing-otel/src/semantic-conventions/mapper.ts</files>
  <action>
    Map HexDI-specific attributes to OpenTelemetry semantic conventions.

    In packages/tracing-otel/src/semantic-conventions/mapper.ts:
    - Import Attributes from @hex-di/tracing
    - Import semantic convention constants from @opentelemetry/semantic-conventions
    - Create mapHexDiToOtelAttributes function:
      - Takes HexDI Attributes
      - Returns mapped Attributes with OTel conventions
    - Implement mappings (OTEL-07):
      - Keep all hex-di.* attributes as-is (for HexDI-specific tools)
      - ADD (don't replace) OTel standard equivalents:
        - hex-di.port.name → add ATTR_CODE_NAMESPACE
        - hex-di.resolution.cached → add custom.cache_hit (boolean)
        - hex-di.container.id → add custom.container_id
        - hex-di.resolution.depth → add custom.resolution_depth
      - Preserve all other attributes unchanged
    - Return merged attributes object with both hex-di.* and OTel conventions
    - Document why we keep both (compatibility with HexDI tools + standard backends)

  </action>
  <verify>pnpm --filter @hex-di/tracing-otel build && pnpm --filter @hex-di/tracing-otel typecheck</verify>
  <done>Semantic convention mapper preserves HexDI attributes while adding OTel standards</done>
</task>

<task type="auto">
  <name>Task 3: Implement OTLP HTTP exporter adapter</name>
  <files>packages/tracing-otel/src/exporters/otlp-http.ts, packages/tracing-otel/src/exporters/types.ts</files>
  <action>
    Create the OTLP HTTP exporter that bridges HexDI to OpenTelemetry collectors.

    In packages/tracing-otel/src/exporters/types.ts:
    - Create OtlpHttpExporterOptions interface:
      - url?: string (default: 'http://localhost:4318/v1/traces')
      - headers?: Record<string, string>
      - compression?: 'gzip' | 'none' (default: 'none')
      - timeout?: number (milliseconds, default: 10000)
      - resource?: ResourceConfig (from resource.ts)

    In packages/tracing-otel/src/exporters/otlp-http.ts:
    - Import SpanExporter, SpanData from @hex-di/tracing
    - Import OTLPTraceExporter from @opentelemetry/exporter-trace-otlp-http
    - Import convertToReadableSpan from ../adapters/span-adapter.js
    - Import createResource from ../resources/resource.js
    - Import mapHexDiToOtelAttributes from ../semantic-conventions/mapper.js
    - Create createOtlpHttpExporter function:
      - Takes OtlpHttpExporterOptions
      - Creates Resource using createResource if config provided
      - Creates underlying OTLPTraceExporter with mapped options
      - Returns SpanExporter implementation:
        - export(spans):
          - Convert each SpanData to ReadableSpan
          - Map attributes using mapHexDiToOtelAttributes
          - Attach resource if provided
          - Call underlying exporter's export
        - forceFlush(): delegate to underlying exporter
        - shutdown(): delegate to underlying exporter
    - Handle errors gracefully (log but don't throw)
    - Add JSDoc with example configuration

  </action>
  <verify>pnpm --filter @hex-di/tracing-otel build && pnpm --filter @hex-di/tracing-otel typecheck</verify>
  <done>OTLP HTTP exporter adapter created with resource and semantic convention support</done>
</task>

<task type="auto">
  <name>Task 4: Update package exports</name>
  <files>packages/tracing-otel/src/index.ts</files>
  <action>
    Add exporter and utility exports to the main index.

    Update packages/tracing-otel/src/index.ts:
    - Export createOtlpHttpExporter from './exporters/otlp-http.js'
    - Export OtlpHttpExporterOptions from './exporters/types.js'
    - Export createResource, ResourceConfig from './resources/resource.js'
    - Export mapHexDiToOtelAttributes from './semantic-conventions/mapper.js'
    - Keep all existing exports from 25-01 and 25-02
    - Add package-level JSDoc explaining this is the universal OTel adapter

  </action>
  <verify>pnpm --filter @hex-di/tracing-otel build && node -e "import('@hex-di/tracing-otel').then(m => console.log('OTLP exporter:', !!m.createOtlpHttpExporter))"</verify>
  <done>OTLP exporter and utilities exported from package</done>
</task>

</tasks>

<verification>
# Build and verify
pnpm --filter @hex-di/tracing-otel build
pnpm --filter @hex-di/tracing-otel typecheck

# Check exports

node -e "import('@hex-di/tracing-otel').then(m => console.log('Exports:', Object.keys(m).sort()))"

# Verify no type casts

grep -r "as \|<.\*>" packages/tracing-otel/src/ || echo "✓ No type casts found"
</verification>

<success_criteria>

- [ ] OTLP HTTP exporter sends converted spans to collectors
- [ ] Resource metadata includes service.name as required attribute
- [ ] HexDI attributes mapped to OTel semantic conventions
- [ ] Both hex-di.\* and OTel standard attributes preserved
- [ ] Exporter handles errors gracefully without throwing
- [ ] All components exported from package
      </success_criteria>

<output>
After completion, create `.planning/phases/25-opentelemetry-backend-and-export-pipeline/25-03-SUMMARY.md`
</output>
