---
phase: 17-type-safe-api
plan: 04
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - packages/runtime/src/container/override-context.ts
  - packages/runtime/src/container/child-impl.ts
  - packages/runtime/tests/override.test.ts
autonomous: true

must_haves:
  truths:
    - "String-based override API no longer exists"
    - "Only adapter-based overrides are possible"
    - "Compile-time validation prevents invalid overrides"
  artifacts:
    - path: "packages/runtime/src/container/override-context.ts"
      provides: "Removed or refactored for adapter-only use"
      contains: "adapter-based logic only"
  key_links:
    - from: "packages/runtime/src/container/child-impl.ts"
      to: "packages/runtime/src/container/override-builder.ts"
      via: "uses builder for overrides"
      pattern: "OverrideBuilder"
---

<objective>
Remove string-based override API completely

Purpose: Enforce type-safe adapter-based overrides only, removing legacy string-based API
Output: Clean override implementation using only adapters
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-type-safe-api/17-01-SUMMARY.md
@.planning/phases/17-type-safe-api/17-02-SUMMARY.md

# Existing override implementation

@packages/runtime/src/container/override-context.ts
@packages/runtime/src/container/child-impl.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove string-based override context</name>
  <files>packages/runtime/src/container/override-context.ts</files>
  <action>
Refactor or remove override-context.ts:

1. If the file only handles string-based overrides, delete it entirely
2. If it has reusable logic for adapter overrides:
   - Remove all string-based override handling
   - Remove OverrideMap type and related string lookups
   - Keep only adapter-based resolution logic if any
3. Remove createOverrideContext function if it only supports strings
4. Update any imports/exports as needed

The goal is to eliminate all code paths that allow string-based overrides.
</action>
<verify>Build succeeds: `pnpm --filter @hex-di/runtime build`</verify>
<done>String-based override context removed or refactored</done>
</task>

<task type="auto">
  <name>Task 2: Update child container implementation</name>
  <files>packages/runtime/src/container/child-impl.ts</files>
  <action>
Update ChildContainerImpl to remove string-based override support:

1. Remove any constructor parameters for string-based overrides
2. Remove override context handling if string-based
3. Update resolution logic to only check adapter-based overrides
4. Ensure child containers created via OverrideBuilder work correctly
5. Remove any methods that accept string port names for overrides

Child containers should only support overrides via the typed OverrideBuilder API.
</action>
<verify>Type check passes: `pnpm --filter @hex-di/runtime typecheck`</verify>
<done>Child container no longer supports string-based overrides</done>
</task>

<task type="auto">
  <name>Task 3: Update override tests</name>
  <files>packages/runtime/tests/override.test.ts</files>
  <action>
Update or rewrite override tests for adapter-only API:

1. Remove all tests using string-based overrides
2. Add tests using the new override builder:
   ```typescript
   const overridden = container.override(MockAdapter).override(AnotherMockAdapter).build();
   ```
3. Test that overrides actually replace original implementations
4. Test that override adapters can depend on each other
5. Test compile-time validation (via type tests if needed)

All tests should use the typed adapter-based API.
</action>
<verify>Tests pass: `pnpm --filter @hex-di/runtime test override`</verify>
<done>Override tests updated for adapter-only API</done>
</task>

</tasks>

<verification>
- No string-based override code remains
- Only adapter-based overrides possible
- Tests confirm adapter override behavior
- Compile-time validation prevents invalid overrides
</verification>

<success_criteria>

- String-based override API completely removed
- Override context simplified or removed
- Child containers only support adapter overrides
- Tests validate adapter-only override behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/17-type-safe-api/17-04-SUMMARY.md`
</output>
