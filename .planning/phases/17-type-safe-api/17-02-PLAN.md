---
phase: 17-type-safe-api
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/runtime/src/types/container.ts
  - packages/runtime/src/container/base-impl.ts
  - packages/runtime/src/container/child-impl.ts
  - packages/runtime/src/container/root-impl.ts
autonomous: true

must_haves:
  truths:
    - "User can call container.override(adapter) to create builder"
    - "Override method is available on all container types"
    - "Type validation happens at compile time"
  artifacts:
    - path: "packages/runtime/src/types/container.ts"
      provides: "Container interface with override method"
      contains: "override<A extends AdapterConstraint>"
    - path: "packages/runtime/src/container/base-impl.ts"
      provides: "Base implementation of override method"
      contains: "override(adapter)"
  key_links:
    - from: "packages/runtime/src/container/base-impl.ts"
      to: "packages/runtime/src/container/override-builder.ts"
      via: "import and instantiation"
      pattern: "new OverrideBuilder"
---

<objective>
Add override method to Container interface and implementations

Purpose: Enable users to create override builders directly from container instances
Output: Container.override() method that returns typed OverrideBuilder
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-type-safe-api/17-01-SUMMARY.md

# Container types and implementations

@packages/runtime/src/types/container.ts
@packages/runtime/src/container/base-impl.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add override method to Container interface</name>
  <files>packages/runtime/src/types/container.ts</files>
  <action>
Add override method to ContainerMembers type:

1. Import AdapterConstraint from @hex-di/core
2. Import OverrideBuilder from ./override-types
3. Add method signature:
   ```typescript
   override<A extends AdapterConstraint>(
     adapter: A
   ): OverrideBuilder<TProvides, InferAdapterProvides<A>>;
   ```
4. Add JSDoc explaining the method creates an override builder
5. Ensure type parameters flow correctly for compile-time validation

The method should accept any adapter and return a typed OverrideBuilder that tracks the override.
</action>
<verify>Type check passes: `pnpm --filter @hex-di/runtime typecheck`</verify>
<done>Container interface includes override method with proper typing</done>
</task>

<task type="auto">
  <name>Task 2: Implement override method in container classes</name>
  <files>packages/runtime/src/container/base-impl.ts, packages/runtime/src/container/child-impl.ts, packages/runtime/src/container/root-impl.ts</files>
  <action>
Implement override method in container classes:

1. In base-impl.ts:
   - Import OverrideBuilder from ./override-builder
   - Add override method that creates new OverrideBuilder:
     ```typescript
     override<A extends AdapterConstraint>(adapter: A) {
       return new OverrideBuilder(this, [adapter]);
     }
     ```

2. In child-impl.ts and root-impl.ts:
   - Verify they inherit the method from BaseContainerImpl
   - No additional implementation needed if properly inheriting

The method creates a new OverrideBuilder with the container instance and initial adapter.
</action>
<verify>Run container tests: `pnpm --filter @hex-di/runtime test container`</verify>
<done>Override method implemented in all container classes</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for override method</name>
  <files>packages/runtime/tests/override-builder.test.ts</files>
  <action>
Create tests for the container.override() method:

1. Test that override returns OverrideBuilder instance
2. Test that builder can chain multiple overrides
3. Test that build() creates child container with overrides
4. Test that overrides actually replace original adapters
5. Test immutability - each override() returns new builder

Use existing test fixtures and patterns from container tests.
</action>
<verify>Tests pass: `pnpm --filter @hex-di/runtime test override-builder`</verify>
<done>Override method has comprehensive test coverage</done>
</task>

</tasks>

<verification>
- Container.override() method exists and returns OverrideBuilder
- Method is available on all container instances
- Type validation happens at compile time
- Tests verify override behavior
</verification>

<success_criteria>

- Users can call container.override(adapter) on any container
- Method returns typed OverrideBuilder for chaining
- Compile-time validation of adapter ports
- Tests confirm override functionality
  </success_criteria>

<output>
After completion, create `.planning/phases/17-type-safe-api/17-02-SUMMARY.md`
</output>
