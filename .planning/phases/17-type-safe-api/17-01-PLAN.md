---
phase: 17-type-safe-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/types/override-types.ts
  - packages/runtime/src/types/validation-errors.ts
  - packages/runtime/src/container/override-builder.ts
  - packages/runtime/src/types/index.ts
autonomous: true

must_haves:
  truths:
    - "User can create override builder from container"
    - "Builder provides fluent API for chaining overrides"
    - "Each override returns new builder instance (immutable)"
  artifacts:
    - path: "packages/runtime/src/types/override-types.ts"
      provides: "Type definitions for override builder"
      exports: ["OverrideBuilder", "ValidateOverrideAdapter"]
    - path: "packages/runtime/src/types/validation-errors.ts"
      provides: "Error types for port validation"
      exports: ["PortNotInGraphError", "MissingDependenciesError"]
    - path: "packages/runtime/src/container/override-builder.ts"
      provides: "Override builder implementation"
      exports: ["OverrideBuilder"]
  key_links:
    - from: "packages/runtime/src/container/override-builder.ts"
      to: "packages/runtime/src/types/override-types.ts"
      via: "type imports"
      pattern: "import.*override-types"
---

<objective>
Create type-safe override builder with compile-time port validation

Purpose: Enable users to override container adapters with compile-time validation that ports exist in graph
Output: Override builder class with phantom type tracking and validation types
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-type-safe-api/17-RESEARCH.md

# GraphBuilder reference for patterns

@packages/graph/src/builder/builder.ts
@packages/graph/src/builder/types/override.ts
@packages/core/src/adapters/inference.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create override validation types</name>
  <files>packages/runtime/src/types/override-types.ts, packages/runtime/src/types/validation-errors.ts</files>
  <action>
Create type definitions for override builder following GraphBuilder patterns:

1. In `packages/runtime/src/types/validation-errors.ts`:
   - Create `PortNotInGraphError` template literal type with actionable message
   - Create `MissingDependenciesError` for unsatisfied adapter requires
   - Include fix suggestions in error messages
   - Use template literal types for readable errors

2. In `packages/runtime/src/types/override-types.ts`:
   - Create `ValidateOverrideAdapter` type that checks adapter's port exists in graph
   - Create `ValidateAdapterDependencies` type for checking required ports
   - Create `OverrideBuilderState` phantom type tracking accumulated overrides
   - Export types for builder implementation

Mirror GraphBuilder's conditional type patterns for validation. Use `InferAdapterProvides` and `InferAdapterRequires` from @hex-di/core for adapter inspection.
</action>
<verify>Check that types compile: `pnpm --filter @hex-di/runtime typecheck`</verify>
<done>Type definitions exist for override validation with error types</done>
</task>

<task type="auto">
  <name>Task 2: Implement override builder class</name>
  <files>packages/runtime/src/container/override-builder.ts</files>
  <action>
Create OverrideBuilder class with phantom type tracking:

1. Class with type parameters tracking TProvides, TOverrides
2. Constructor accepts base container reference and stores it:
   ```typescript
   constructor(private container: Container<TProvides, ...>) {
     this.overrides = [];
   }
   ```
3. `override(adapter)` method:
   - Returns new OverrideBuilder instance (immutable)
   - Type signature uses ValidateOverrideAdapter for compile-time checks
   - Accumulates adapter in internal array
4. `build()` method creates child container with overrides:
   - Access original graph via: `this.container.getGraph()` or similar container method
   - Create override graph fragment using GraphBuilder:
     ```typescript
     const overrideGraph = new GraphBuilder().add(...this.overrides).build();
     ```
   - Create child container:
     ```typescript
     return this.container.createChild({
       graph: overrideGraph,
       name: `${this.container.name}-override`,
     });
     ```
   - Returns typed Container instance with override adapters applied

Follow GraphBuilder's immutable builder pattern. Each method returns new instance with updated phantom types. The container reference provides access to the original graph for validation and child creation.
</action>
<verify>Check that builder compiles: `pnpm --filter @hex-di/runtime typecheck`</verify>
<done>Override builder class implements fluent API with type validation</done>
</task>

<task type="auto">
  <name>Task 3: Export override types from package</name>
  <files>packages/runtime/src/types/index.ts</files>
  <action>
Add exports to types/index.ts:
- Export OverrideBuilder type from override-types.ts
- Export validation error types from validation-errors.ts
- Ensure types are accessible to consumers

Do NOT export internal validation types, only public API types.
</action>
<verify>Check exports are available: `pnpm --filter @hex-di/runtime build`</verify>
<done>Override builder types are exported from runtime package</done>
</task>

</tasks>

<verification>
- Type definitions compile without errors
- Override builder creates child containers with overrides
- Phantom types track override state at compile time
- Error messages provide actionable guidance
</verification>

<success_criteria>

- OverrideBuilder class exists with fluent API
- Type validation happens at compile time via conditional types
- Error types use template literals for readable messages
- Builder follows immutable pattern (returns new instances)
  </success_criteria>

<output>
After completion, create `.planning/phases/17-type-safe-api/17-01-SUMMARY.md`
</output>
