---
phase: 03-scoped-overrides
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/adapters/types.ts
  - packages/core/src/index.ts
autonomous: true

must_haves:
  truths:
    - "'request' is a valid Lifetime option alongside 'singleton', 'scoped', 'transient'"
    - "Existing code using Lifetime type continues to work"
    - "Type exports are available from @hex-di/core package"
  artifacts:
    - path: "packages/core/src/adapters/types.ts"
      provides: "Lifetime type with 'request' option"
      exports: ["Lifetime"]
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/adapters/types.ts"
      via: "re-export"
      pattern: "export.*Lifetime"
---

<objective>
Add 'request' lifetime to the core Lifetime type union

Purpose: Enable request-scoped services as a first-class lifetime option, allowing services to be created fresh per request context while maintaining type safety.
Output: Updated Lifetime type with 'request' option available throughout the codebase
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/u1070457/Projects/Perso/hex-di/.planning/PROJECT.md
@/Users/u1070457/Projects/Perso/hex-di/.planning/ROADMAP.md
@/Users/u1070457/Projects/Perso/hex-di/.planning/STATE.md

@/Users/u1070457/Projects/Perso/hex-di/packages/core/src/adapters/types.ts
@/Users/u1070457/Projects/Perso/hex-di/packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'request' to Lifetime type union</name>
  <files>packages/core/src/adapters/types.ts</files>
  <action>
    Update the Lifetime type definition to include 'request' as a fourth option:
    - Find the line: `export type Lifetime = "singleton" | "scoped" | "transient";`
    - Change it to: `export type Lifetime = "singleton" | "scoped" | "transient" | "request";`

    This extends the lifetime options to support request-scoped services which will be created fresh per request context, similar to scoped but with request-level isolation.

    Also update the JSDoc comment above the type to document the new 'request' lifetime:
    - Add documentation explaining that 'request' lifetime creates a new instance per request context
    - Note that request-scoped services are isolated from the parent container and other request contexts
    - Explain that they are disposed when the request context ends

  </action>
  <verify>grep -n "export type Lifetime" packages/core/src/adapters/types.ts</verify>
  <done>Lifetime type includes "request" as a valid option</done>
</task>

<task type="auto">
  <name>Task 2: Verify type exports</name>
  <files>packages/core/src/index.ts</files>
  <action>
    Ensure the Lifetime type is properly exported from the package entry point:
    - Check that the export from adapters/types.ts includes Lifetime
    - The export should already be in place from the existing line: `export type { Lifetime, ... } from "./adapters/types.js";`
    - No changes needed if already exported, just verify it's there

    This ensures that consuming packages can import the updated Lifetime type with the new 'request' option.

  </action>
  <verify>grep "export.*Lifetime" packages/core/src/index.ts</verify>
  <done>Lifetime type is exported from @hex-di/core package</done>
</task>

<task type="auto">
  <name>Task 3: Run type checks</name>
  <files>packages/core</files>
  <action>
    Run TypeScript type checking to ensure the change doesn't break existing code:
    - Execute: `pnpm --filter @hex-di/core typecheck`
    - All type checks should pass since we're only adding a new union member
    - This is backward compatible - existing code using 'singleton', 'scoped', or 'transient' continues to work

    The type system will now accept 'request' as a valid lifetime option in adapter definitions.

  </action>
  <verify>cd /Users/u1070457/Projects/Perso/hex-di && pnpm --filter @hex-di/core typecheck</verify>
  <done>Type checking passes with the updated Lifetime type</done>
</task>

</tasks>

<verification>
1. Lifetime type includes 'request' as the fourth option
2. Type is properly exported from @hex-di/core
3. TypeScript compilation succeeds
4. Existing code using Lifetime type is not broken
</verification>

<success_criteria>

- [ ] Lifetime type updated to include 'request'
- [ ] Type properly exported from package entry point
- [ ] All type checks pass
- [ ] Change is backward compatible
      </success_criteria>

<output>
After completion, create `.planning/phases/03-scoped-overrides/03-01-SUMMARY.md`
</output>
