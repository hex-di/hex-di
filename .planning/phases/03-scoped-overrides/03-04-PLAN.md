---
phase: 03-scoped-overrides
plan: 04
type: tdd
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - packages/runtime/tests/override.test.ts
  - packages/runtime/tests/request-scope.test.ts
autonomous: true

must_haves:
  truths:
    - "Tests verify withOverrides() isolates instances"
    - "Tests verify request scope creates fresh instances"
    - "Tests verify lifetime hierarchy with 'request'"
    - "All new features have test coverage"
  artifacts:
    - path: "packages/runtime/tests/override.test.ts"
      provides: "Override context test coverage"
      min_lines: 150
    - path: "packages/runtime/tests/request-scope.test.ts"
      provides: "Request scope test coverage"
      min_lines: 150
  key_links:
    - from: "tests"
      to: "implementation"
      via: "import and test"
      pattern: "withOverrides|createRequestScope"
---

<objective>
Add comprehensive test coverage for scoped overrides and request lifetime

Purpose: Ensure the new withOverrides() and request scope features work correctly with proper isolation, validate lifetime hierarchy, and prevent regressions.
Output: Complete test suite covering all new functionality
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/u1070457/Projects/Perso/hex-di/.planning/PROJECT.md
@/Users/u1070457/Projects/Perso/hex-di/.planning/ROADMAP.md
@/Users/u1070457/Projects/Perso/hex-di/.planning/STATE.md

@/Users/u1070457/Projects/Perso/hex-di/.planning/phases/03-scoped-overrides/03-01-SUMMARY.md
@/Users/u1070457/Projects/Perso/hex-di/.planning/phases/03-scoped-overrides/03-02-SUMMARY.md
@/Users/u1070457/Projects/Perso/hex-di/.planning/phases/03-scoped-overrides/03-03-SUMMARY.md

@/Users/u1070457/Projects/Perso/hex-di/packages/runtime/tests/container.test.ts
@/Users/u1070457/Projects/Perso/hex-di/packages/runtime/tests/scope.test.ts
</context>

<feature>
  <name>Scoped Overrides and Request Lifetime Tests</name>
  <files>packages/runtime/tests/override.test.ts,packages/runtime/tests/request-scope.test.ts</files>
  <behavior>
    Test withOverrides():
    - Override context returns override adapter instances
    - Override instances are isolated from parent container
    - Nested dependencies use override context
    - Override context doesn't affect parent container
    - Type inference works with overrides

    Test createRequestScope():
    - Request-scoped services are created fresh per request
    - Request scope instances are isolated between requests
    - Request lifetime validation in captive dependencies
    - Request scope disposal cleans up instances
    - Mixing request/scoped/singleton lifetimes works correctly

    Test cases:
    - withOverrides({ port: mockAdapter }, fn) uses mock in fn
    - Two override contexts have separate instances
    - Request scope creates new instance per request
    - Request service can't depend on scoped service (captive dependency)
    - Singleton is shared across request scopes

  </behavior>
  <implementation>
    Use Vitest with the existing test setup. Create two test files:

    1. override.test.ts - Tests for withOverrides() functionality
    2. request-scope.test.ts - Tests for request lifetime and createRequestScope()

    Follow the existing test patterns in the runtime package. Use describe/it blocks
    and create test ports/adapters as needed for isolation.

  </implementation>
</feature>
