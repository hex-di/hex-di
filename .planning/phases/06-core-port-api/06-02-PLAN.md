---
phase: 06-core-port-api
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/core/src/ports/factory.ts
  - packages/core/src/ports/directed.ts
  - packages/core/src/index.ts
  - packages/core/tests/directed-ports.test.ts
  - packages/core/tests/from-class.test.ts
  - packages/core/tests/builder.test.ts
autonomous: true

must_haves:
  truths:
    - "createInboundPort() no longer exists in exports"
    - "createOutboundPort() no longer exists in exports"
    - "String-only createPort('Name') no longer exists"
    - "port<T>()('Name') curried function no longer exists"
    - "All @hex-di/core tests use new createPort({ name }) API"
    - "All @hex-di/core tests pass after migration"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Clean exports without deprecated APIs"
      not_contains: ["createInboundPort", "createOutboundPort", "port"]
    - path: "packages/core/src/ports/factory.ts"
      provides: "Only object-config createPort"
      not_contains:
        ["function port<", "createPort<const TName extends string, TService>(name: TName)"]
    - path: "packages/core/src/ports/directed.ts"
      provides: "Only utilities, no factory functions"
      not_contains: ["createInboundPort", "createOutboundPort"]
  key_links:
    - from: "packages/core/tests/*.ts"
      to: "packages/core/src/ports/factory.ts"
      via: "import { createPort }"
      pattern: "createPort\\(\\{"
---

<objective>
Remove deprecated port APIs from @hex-di/core and migrate all core tests to use the unified createPort({ name }) API.

Purpose: Complete the breaking change in core package by removing old APIs. This plan focuses ONLY on @hex-di/core - other packages are migrated in subsequent plans.

Output: Core package has clean API surface with only createPort({ name, direction? }), and all core tests pass.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-port-api/06-CONTEXT.md
@.planning/phases/06-core-port-api/06-01-SUMMARY.md

@packages/core/src/ports/factory.ts
@packages/core/src/ports/directed.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove old APIs from source files</name>
  <files>
packages/core/src/ports/factory.ts
packages/core/src/ports/directed.ts
packages/core/src/index.ts
  </files>
  <action>
1. In factory.ts:
   - Remove the `port<TService>()` curried function entirely
   - Remove the string-based `createPort<const TName extends string, TService>(name: TName)` overload
   - Keep only the object-config createPort() with the two overloads for direction handling
   - Update JSDoc to remove references to deprecated APIs

2. In directed.ts:
   - Remove `createInboundPort()` function entirely
   - Remove `createOutboundPort()` function entirely
   - Keep: runtime symbols (DIRECTION_BRAND, METADATA_KEY) - still exported
   - Keep: createDirectedPortImpl helper (used by factory.ts)
   - Keep: type guards (isDirectedPort, isInboundPort, isOutboundPort)
   - Keep: accessor functions (getPortDirection, getPortMetadata)
   - Keep: CreateDirectedPortOptions interface (may still be useful internally)
   - Keep: re-exports of types

3. In index.ts:
   - Remove `port` from exports
   - Remove `createInboundPort` and `createOutboundPort` from exports
   - Keep all other port-related exports
   - Verify createPort is still exported from factory.ts

After these changes, the only way to create a port is `createPort({ name })`.
</action>
<verify>
Run `pnpm --filter @hex-di/core typecheck` - will have errors from tests using old APIs.
This is expected - tests will be migrated in Task 2.
</verify>
<done>
Old APIs removed:

- port() curried function removed
- String-based createPort('Name') removed
- createInboundPort() removed
- createOutboundPort() removed
  Only createPort({ name, direction? }) remains.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Migrate @hex-di/core tests</name>
  <files>
packages/core/tests/directed-ports.test.ts
packages/core/tests/from-class.test.ts
packages/core/tests/builder.test.ts
  </files>
  <action>
Migrate each test file to use the new createPort({ name }) API:

**Migration patterns:**

1. `createInboundPort<'Name', Type>({ name: 'Name' })` becomes:
   `createPort<Type>({ name: 'Name', direction: 'inbound' })`

2. `createOutboundPort<'Name', Type>({ name: 'Name' })` becomes:
   `createPort<Type>({ name: 'Name', direction: 'outbound' })`
   OR just `createPort<Type>({ name: 'Name' })` if outbound is acceptable

3. `createPort<'Name', Type>('Name')` becomes:
   `createPort<Type>({ name: 'Name' })`

4. `port<Type>()('Name')` becomes:
   `createPort<Type>({ name: 'Name' })`

**For directed-ports.test.ts specifically:**

- Update all PORT-01 tests to use `createPort({ name, direction: 'inbound' })`
- Update all PORT-02 tests to use `createPort({ name, direction: 'outbound' })`
- Update backward compatibility tests to verify new API works like old undirected API did (but now has direction)
- Update "createPort() still works as before" test - it now returns DirectedPort with 'outbound'
- Remove/update any tests that specifically test the old string API
- isDirectedPort(port) will now return TRUE for all ports created with createPort()
- getPortDirection(port) will return 'outbound' for all ports created with createPort({ name })
- Update import statement to remove createInboundPort, createOutboundPort

**For from-class.test.ts and builder.test.ts:**

- Find all port creation calls and migrate to new API
- These likely use simple `createPort('Name')` patterns
  </action>
  <verify>
  Run `pnpm --filter @hex-di/core test` - all tests should pass after migration.
  Run `pnpm --filter @hex-di/core typecheck` - should pass.
  </verify>
  <done>
  All @hex-di/core test files migrated:
- directed-ports.test.ts uses createPort({ name, direction })
- from-class.test.ts uses createPort({ name })
- builder.test.ts uses createPort({ name })
  All core tests pass.
  </done>
  </task>

</tasks>

<verification>
After all tasks complete:

1. **Old APIs completely removed from core**:

```bash
# These should all return 0 results
grep -r "createInboundPort" packages/core/src/ --include="*.ts"
grep -r "createOutboundPort" packages/core/src/ --include="*.ts"
grep -r "function port<" packages/core/src/ --include="*.ts"
```

2. **All core tests pass**:

```bash
pnpm --filter @hex-di/core test
pnpm --filter @hex-di/core typecheck
```

3. **Export verification**:

```typescript
// This should fail to compile
import { createInboundPort } from "@hex-di/core"; // Error: not exported
import { createOutboundPort } from "@hex-di/core"; // Error: not exported
import { port } from "@hex-di/core"; // Error: not exported

// This should work
import { createPort } from "@hex-di/core"; // OK
```

</verification>

<success_criteria>

- [ ] createInboundPort() removed from source and exports
- [ ] createOutboundPort() removed from source and exports
- [ ] port() curried function removed from source and exports
- [ ] String-only createPort('Name') overload removed
- [ ] All @hex-di/core tests pass
- [ ] pnpm --filter @hex-di/core typecheck passes
- [ ] No grep matches for old API patterns in packages/core/
      </success_criteria>

<output>
After completion, create `.planning/phases/06-core-port-api/06-02-SUMMARY.md`
</output>
