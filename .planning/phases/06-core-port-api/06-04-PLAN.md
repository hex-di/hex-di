---
phase: 06-core-port-api
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/runtime/tests/**/*.ts
  - packages/runtime/src/**/*.ts
autonomous: true

must_haves:
  truths:
    - "All @hex-di/runtime tests use new createPort({ name }) API"
    - "All @hex-di/runtime tests pass after migration"
    - "No old API patterns remain in packages/runtime/"
  artifacts:
    - path: "packages/runtime/tests/support/fixtures.ts"
      provides: "Shared runtime test fixtures using new API"
      pattern: "createPort\\(\\{"
  key_links:
    - from: "packages/runtime/tests/*.ts"
      to: "packages/core/src/ports/factory.ts"
      via: "import { createPort }"
      pattern: "createPort\\(\\{"
---

<objective>
Migrate all @hex-di/runtime tests and source files to use the unified createPort({ name }) API.

Purpose: Runtime package has ~50 test files that need migration. This includes both test files and any source files that reference old port APIs.

Output: All runtime package tests and source files use new API and pass.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-port-api/06-CONTEXT.md
@.planning/phases/06-core-port-api/06-01-SUMMARY.md

@packages/runtime/tests/support/fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate runtime shared fixtures and source files</name>
  <files>
packages/runtime/tests/support/fixtures.ts
packages/runtime/src/types.ts
packages/runtime/src/types/type-guards.ts
  </files>
  <action>
First migrate shared fixtures and any source files that reference old port APIs.

**Check source files for old API usage:**

```bash
grep -r "createPort\|createInboundPort\|createOutboundPort" packages/runtime/src/ --include="*.ts"
```

**Migration patterns:**

1. `createInboundPort<'Name', Type>({ name: 'Name' })` ->
   `createPort<Type>({ name: 'Name', direction: 'inbound' })`

2. `createOutboundPort<'Name', Type>({ name: 'Name' })` ->
   `createPort<Type>({ name: 'Name', direction: 'outbound' })`

3. `createPort<'Name', Type>('Name')` ->
   `createPort<Type>({ name: 'Name' })`

**Update imports:**

- Remove: createInboundPort, createOutboundPort from imports
- Keep: createPort

Note: Source files may only have type references, not actual createPort calls.
</action>
<verify>
Grep for old patterns in source and fixture files - should return 0 results.
Run `pnpm --filter @hex-di/runtime typecheck` to identify remaining issues.
</verify>
<done>
Runtime shared fixtures and source files migrated.
</done>
</task>

<task type="auto">
  <name>Task 2: Migrate runtime test files</name>
  <files>packages/runtime/tests/**/*.ts</files>
  <action>
Migrate all test files in packages/runtime/tests/.

**Key test files (~50 files):**

- packages/runtime/tests/\*.test.ts
- packages/runtime/tests/\*.test-d.ts
- packages/runtime/tests/plugins/\*.test.ts

**Find files needing migration:**

```bash
grep -rl "createPort<.*>('[^']*')\|createInboundPort\|createOutboundPort" packages/runtime/tests/ --include="*.ts"
```

**Apply same migration patterns as Task 1.**
</action>
<verify>
Run grep commands to confirm no old patterns remain:

```bash
grep -r "createInboundPort" packages/runtime/ --include="*.ts"
grep -r "createOutboundPort" packages/runtime/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/runtime/ --include="*.ts"
```

Run `pnpm --filter @hex-di/runtime typecheck` - should pass.
Run `pnpm --filter @hex-di/runtime test` - all tests should pass.
</verify>
<done>
All @hex-di/runtime files migrated to use createPort({ name }).
All runtime tests pass.
</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **No old API patterns in runtime**:

```bash
grep -r "createInboundPort" packages/runtime/ --include="*.ts"
grep -r "createOutboundPort" packages/runtime/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/runtime/ --include="*.ts"
```

All should return 0 results.

2. **All runtime tests pass**:

```bash
pnpm --filter @hex-di/runtime typecheck
pnpm --filter @hex-di/runtime test
```

</verification>

<success_criteria>

- [ ] All source files in packages/runtime/src/ migrated (if any)
- [ ] All test files in packages/runtime/tests/ migrated
- [ ] No grep matches for old API patterns
- [ ] pnpm --filter @hex-di/runtime typecheck passes
- [ ] pnpm --filter @hex-di/runtime test passes
      </success_criteria>

<output>
After completion, create `.planning/phases/06-core-port-api/06-04-SUMMARY.md`
</output>
