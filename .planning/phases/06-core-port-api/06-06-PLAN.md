---
phase: 06-core-port-api
plan: 06
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/react/tests/**/*.ts
  - packages/react/src/**/*.ts
  - packages/hono/tests/**/*.ts
  - packages/visualization/tests/**/*.ts
autonomous: true

must_haves:
  truths:
    - "All @hex-di/react tests use new createPort({ name }) API"
    - "All @hex-di/hono tests use new createPort({ name }) API"
    - "All @hex-di/visualization tests use new createPort({ name }) API"
    - "All tests pass after migration"
  artifacts:
    - path: "packages/react/src/index.ts"
      provides: "React bindings using new port API"
  key_links:
    - from: "packages/react/tests/*.ts"
      to: "packages/core/src/ports/factory.ts"
      via: "import { createPort }"
      pattern: "createPort\\(\\{"
---

<objective>
Migrate @hex-di/react (~8 files), @hex-di/hono (~6 files), and @hex-di/visualization (~2 files) to use the unified createPort({ name }) API.

Purpose: These are the remaining smaller packages. Combining them into one plan keeps total plan count at 6.

Output: All react, hono, and visualization package tests use new API and pass.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-port-api/06-CONTEXT.md
@.planning/phases/06-core-port-api/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate @hex-di/react</name>
  <files>
packages/react/src/index.ts
packages/react/tests/**/*.ts
  </files>
  <action>
Migrate react package source and test files.

**Source files to check:**

- packages/react/src/index.ts

**Test files (~8 files):**

- packages/react/tests/\*.test.ts
- packages/react/tests/\*.test-d.ts

**Migration patterns:**

1. `createInboundPort<'Name', Type>({ name: 'Name' })` ->
   `createPort<Type>({ name: 'Name', direction: 'inbound' })`

2. `createOutboundPort<'Name', Type>({ name: 'Name' })` ->
   `createPort<Type>({ name: 'Name', direction: 'outbound' })`

3. `createPort<'Name', Type>('Name')` ->
   `createPort<Type>({ name: 'Name' })`
   </action>
   <verify>

```bash
grep -r "createInboundPort\|createOutboundPort" packages/react/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/react/ --include="*.ts"
```

Run `pnpm --filter @hex-di/react typecheck`
Run `pnpm --filter @hex-di/react test`
</verify>
<done>
All @hex-di/react files migrated.
</done>
</task>

<task type="auto">
  <name>Task 2: Migrate @hex-di/hono and @hex-di/visualization</name>
  <files>
packages/hono/tests/**/*.ts
packages/visualization/tests/**/*.ts
  </files>
  <action>
Migrate hono and visualization packages.

**@hex-di/hono (~6 files):**

- packages/hono/tests/\*.test.ts
- packages/hono/tests/\*.test-d.ts

**@hex-di/visualization (~2 files):**

- packages/visualization/tests/\*.test.ts

**Apply same migration patterns.**
</action>
<verify>

```bash
grep -r "createInboundPort\|createOutboundPort" packages/hono/ packages/visualization/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/hono/ packages/visualization/ --include="*.ts"
```

Run `pnpm --filter @hex-di/hono typecheck && pnpm --filter @hex-di/hono test`
Run `pnpm --filter @hex-di/visualization typecheck && pnpm --filter @hex-di/visualization test`
</verify>
<done>
All @hex-di/hono and @hex-di/visualization files migrated.
</done>
</task>

<task type="auto">
  <name>Task 3: Final verification - all packages</name>
  <files>none (verification only)</files>
  <action>
Verify the entire monorepo has no remaining old API patterns and all tests pass.

**Full grep verification:**

```bash
grep -r "createInboundPort" packages/ --include="*.ts" | grep -v node_modules
grep -r "createOutboundPort" packages/ --include="*.ts" | grep -v node_modules
grep -r "createPort<.*>('[^']*')" packages/ --include="*.ts" | grep -v node_modules
```

**Full test suite:**

```bash
pnpm typecheck
pnpm test
```

  </action>
  <verify>
All grep commands return 0 results.
`pnpm typecheck` passes for all packages.
`pnpm test` passes for all packages.
  </verify>
  <done>
Complete migration verified:
- No old API patterns anywhere in packages/
- All packages typecheck
- All tests pass
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **No old API patterns anywhere**:

```bash
grep -r "createInboundPort" packages/ --include="*.ts"
grep -r "createOutboundPort" packages/ --include="*.ts"
grep -r "function port<" packages/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/ --include="*.ts"
```

All should return 0 results.

2. **Full monorepo passes**:

```bash
pnpm typecheck
pnpm test
```

</verification>

<success_criteria>

- [ ] All @hex-di/react files migrated
- [ ] All @hex-di/hono files migrated
- [ ] All @hex-di/visualization files migrated
- [ ] No grep matches for old API patterns in entire packages/ directory
- [ ] pnpm typecheck passes
- [ ] pnpm test passes
      </success_criteria>

<output>
After completion, create `.planning/phases/06-core-port-api/06-06-SUMMARY.md`
</output>
