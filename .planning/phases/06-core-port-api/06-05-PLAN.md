---
phase: 06-core-port-api
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/testing/tests/**/*.ts
  - packages/testing/src/**/*.ts
  - packages/flow/tests/**/*.ts
  - packages/flow/src/**/*.ts
autonomous: true

must_haves:
  truths:
    - "All @hex-di/testing tests use new createPort({ name }) API"
    - "All @hex-di/flow tests use new createPort({ name }) API"
    - "All tests pass after migration"
  artifacts:
    - path: "packages/testing/src/mock-adapter.ts"
      provides: "Mock adapter using new port API"
      pattern: "createPort\\(\\{"
  key_links:
    - from: "packages/testing/tests/*.ts"
      to: "packages/core/src/ports/factory.ts"
      via: "import { createPort }"
      pattern: "createPort\\(\\{"
    - from: "packages/flow/tests/*.ts"
      to: "packages/core/src/ports/factory.ts"
      via: "import { createPort }"
      pattern: "createPort\\(\\{"
---

<objective>
Migrate @hex-di/testing (~18 files) and @hex-di/flow (~8 files) to use the unified createPort({ name }) API.

Purpose: These two packages are mid-sized and related (testing often used with flow). Combining them keeps plan count reasonable.

Output: All testing and flow package tests use new API and pass.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-core-port-api/06-CONTEXT.md
@.planning/phases/06-core-port-api/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate @hex-di/testing</name>
  <files>
packages/testing/src/mock-adapter.ts
packages/testing/src/index.ts
packages/testing/tests/**/*.ts
  </files>
  <action>
Migrate testing package source and test files.

**Source files to check:**

- packages/testing/src/mock-adapter.ts
- packages/testing/src/index.ts

**Test files (~18 files):**

- packages/testing/tests/\*.test.ts
- packages/testing/tests/\*.test-d.ts

**Migration patterns:**

1. `createInboundPort<'Name', Type>({ name: 'Name' })` ->
   `createPort<Type>({ name: 'Name', direction: 'inbound' })`

2. `createOutboundPort<'Name', Type>({ name: 'Name' })` ->
   `createPort<Type>({ name: 'Name', direction: 'outbound' })`

3. `createPort<'Name', Type>('Name')` ->
   `createPort<Type>({ name: 'Name' })`

**Update imports in each file.**
</action>
<verify>

```bash
grep -r "createInboundPort\|createOutboundPort" packages/testing/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/testing/ --include="*.ts"
```

Should return 0 results.

Run `pnpm --filter @hex-di/testing typecheck`
Run `pnpm --filter @hex-di/testing test`
</verify>
<done>
All @hex-di/testing files migrated.
All testing tests pass.
</done>
</task>

<task type="auto">
  <name>Task 2: Migrate @hex-di/flow</name>
  <files>
packages/flow/src/effects/constructors.ts
packages/flow/src/integration/port.ts
packages/flow/tests/**/*.ts
  </files>
  <action>
Migrate flow package source and test files.

**Source files to check:**

- packages/flow/src/effects/constructors.ts
- packages/flow/src/integration/port.ts

**Test files (~8 files):**

- packages/flow/tests/\*.test.ts
- packages/flow/tests/\*.test-d.ts

**Apply same migration patterns as Task 1.**
</action>
<verify>

```bash
grep -r "createInboundPort\|createOutboundPort" packages/flow/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/flow/ --include="*.ts"
```

Should return 0 results.

Run `pnpm --filter @hex-di/flow typecheck`
Run `pnpm --filter @hex-di/flow test`
</verify>
<done>
All @hex-di/flow files migrated.
All flow tests pass.
</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **No old API patterns**:

```bash
grep -r "createInboundPort\|createOutboundPort" packages/testing/ packages/flow/ --include="*.ts"
grep -r "createPort<.*>('[^']*')" packages/testing/ packages/flow/ --include="*.ts"
```

2. **All tests pass**:

```bash
pnpm --filter @hex-di/testing typecheck && pnpm --filter @hex-di/testing test
pnpm --filter @hex-di/flow typecheck && pnpm --filter @hex-di/flow test
```

</verification>

<success_criteria>

- [ ] All @hex-di/testing source and test files migrated
- [ ] All @hex-di/flow source and test files migrated
- [ ] No grep matches for old API patterns
- [ ] Both packages typecheck and test pass
      </success_criteria>

<output>
After completion, create `.planning/phases/06-core-port-api/06-05-SUMMARY.md`
</output>
