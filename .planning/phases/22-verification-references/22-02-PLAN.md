---
phase: 22-verification-references
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "pnpm install completes with up-to-date lockfile"
    - "All packages build successfully"
    - "Type checking passes for all packages"
    - "Test suite runs with 1,816+ tests passing"
    - "Lint passes for moved packages (pre-existing failures acceptable)"
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "Resolved workspace dependencies"
      min_lines: 1000
    - path: "test-output.txt"
      provides: "Test execution results"
      contains: "122 test files.*1816 tests"
  key_links:
    - from: "pnpm-workspace.yaml"
      to: "all package directories"
      via: "glob patterns"
      pattern: "integrations/.*|tooling/.*|libs/"
---

<objective>
Run full build pipeline verification to ensure zero regressions from the monorepo reorganization, identifying and fixing any remaining issues.

Purpose: Achieve complete verification that the v6.0 reorganization is successful with no functional regressions
Output: Fully verified monorepo with all tests passing and documented verification results
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-references/22-RESEARCH.md

# Verification baselines from Phase 21:

# - 122 test files

# - 1,816+ tests

# - Pre-existing lint issues: packages/graph (11 errors, 12 warnings)

# - Pre-existing lint warnings: libs/flow (3000+, only surfaced during git mv)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build pipeline verification</name>
  <files>pnpm-lock.yaml</files>
  <action>
    Execute the complete build pipeline in order and capture results:

    1. Install and verify lockfile:
       ```bash
       pnpm install
       # Should output "Lockfile is up to date" if no changes needed
       ```

    2. Build all packages:
       ```bash
       pnpm build
       # All packages should build successfully
       ```

    3. Type-check all packages:
       ```bash
       pnpm typecheck
       # All packages should pass type checking
       ```

    4. Run test suite and verify count:
       ```bash
       pnpm test 2>&1 | tee test-output.txt
       # Verify output shows 122 test files, 1816+ tests
       grep "Test Files" test-output.txt
       grep "Tests" test-output.txt
       ```

    5. Run lint and verify only pre-existing issues:
       ```bash
       pnpm lint 2>&1 | tee lint-output.txt || true
       # Expect packages/graph to fail (pre-existing)
       # All moved packages should pass
       ```

    Record any failures for fixing in next task.

  </action>
  <verify>pnpm test 2>&1 | grep "1816 passed"</verify>
  <done>Full pipeline executed with results captured, test count verified at 1,816+</done>
</task>

<task type="auto">
  <name>Task 2: Fix any issues found</name>
  <files>Any files with issues</files>
  <action>
    Based on Task 1 results, fix any issues found:

    1. If install shows outdated lockfile:
       - Run `pnpm install` to update
       - Commit the updated pnpm-lock.yaml

    2. If build fails for any package:
       - Investigate error messages
       - Most likely: stale tsconfig references or missing dependencies
       - Fix and re-run build

    3. If typecheck fails:
       - Check for stale path references in tsconfig files
       - Fix any type errors introduced by moves

    4. If test count is below 1,816:
       - Check vitest.config.ts includes patterns
       - Ensure tooling/** and libs/** are included
       - Fix test discovery issues

    5. If lint fails for moved packages (not packages/graph):
       - Fix any new lint issues in moved packages
       - Pre-existing packages/graph issues are acceptable

    Note: Based on research, minimal to no fixes expected since workspace protocol handles resolution.

  </action>
  <verify>pnpm build && pnpm typecheck</verify>
  <done>All identified issues fixed, pipeline commands succeed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete v6.0 monorepo reorganization with all packages moved to new structure</what-built>
  <how-to-verify>
    Run final verification to confirm success:

    1. Run the complete pipeline:
       ```bash
       pnpm install && pnpm build && pnpm typecheck && pnpm test && pnpm lint
       ```

    2. Verify test output shows:
       - 122 test files
       - 1,816+ tests passing

    3. Verify lint output shows:
       - Only packages/graph failures (pre-existing)
       - All moved packages pass

    4. Verify examples work:
       ```bash
       pnpm --filter @hex-di/hono-todo build
       pnpm --filter @hex-di/react-showcase build
       pnpm --filter @hex-di/react-showcase test
       ```

    5. Review the new structure:
       - packages/ contains only core, graph, runtime
       - integrations/ contains react, hono
       - tooling/ contains testing, visualization, graph-viz
       - libs/flow/ contains core, react

    The v6.0 reorganization should be complete with zero functional regressions.

  </how-to-verify>
  <resume-signal>Type "approved" if all verification passes, or describe any remaining issues</resume-signal>
</task>

</tasks>

<verification>
1. pnpm install shows "Lockfile is up to date"
2. pnpm build succeeds for all packages
3. pnpm typecheck passes for all packages
4. pnpm test shows 122 test files, 1,816+ tests passing
5. pnpm lint shows only pre-existing packages/graph failures
6. Human verification confirms reorganization complete
</verification>

<success_criteria>

- [ ] VER-01: pnpm install resolves all workspace dependencies
- [ ] VER-02: pnpm build succeeds for all packages
- [ ] VER-03: pnpm typecheck passes across all packages
- [ ] VER-04: pnpm test passes with 1,816+ tests
- [ ] VER-05: pnpm lint passes for all moved packages (packages/graph pre-existing failures acceptable)
- [ ] Human verification confirms v6.0 reorganization complete
      </success_criteria>

<output>
After completion, create `.planning/phases/22-verification-references/22-02-SUMMARY.md`
</output>
