---
phase: 22-verification-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "hono-todo example builds successfully"
    - "react-showcase example builds and tests pass"
    - "Website builds successfully"
    - "All workspace dependencies resolve to correct locations"
  artifacts:
    - path: "examples/hono-todo/package.json"
      provides: "Workspace dependencies using workspace:* protocol"
      contains: "@hex-di/hono.*workspace"
    - path: "examples/react-showcase/package.json"
      provides: "Workspace dependencies using workspace:* protocol"
      contains: "@hex-di/react.*workspace"
  key_links:
    - from: "examples/hono-todo"
      to: "integrations/hono"
      via: "workspace:* resolution"
      pattern: "@hex-di/hono.*workspace"
    - from: "examples/react-showcase"
      to: "integrations/react"
      via: "workspace:* resolution"
      pattern: "@hex-di/react.*workspace"
    - from: "examples/react-showcase"
      to: "libs/flow/core"
      via: "workspace:* resolution"
      pattern: "@hex-di/flow.*workspace"
---

<objective>
Verify examples and website work correctly with the reorganized monorepo structure, ensuring all workspace dependencies resolve to the new package locations.

Purpose: Confirm downstream consumers (examples and website) are not broken by the Phase 20-21 package relocations
Output: Verified examples and website with any necessary reference updates
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verification-references/22-RESEARCH.md

# Key package locations after Phase 20-21 moves:

# - integrations/react (was packages/react)

# - integrations/hono (was packages/hono)

# - tooling/testing (was packages/testing)

# - libs/flow/core (was packages/flow)

# - libs/flow/react (was packages/flow-react)

</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify example dependency resolution</name>
  <files>examples/hono-todo/package.json, examples/react-showcase/package.json</files>
  <action>
    Run dependency resolution verification for both examples:

    1. For hono-todo example:
       - Run `pnpm list --filter @hex-di/hono-todo --depth=1`
       - Verify @hex-di/hono resolves to integrations/hono (not packages/hono)
       - Run `pnpm --filter @hex-di/hono-todo build`
       - Run `pnpm --filter @hex-di/hono-todo typecheck`

    2. For react-showcase example:
       - Run `pnpm list --filter @hex-di/react-showcase --depth=1`
       - Verify @hex-di/react resolves to integrations/react
       - Verify @hex-di/flow resolves to libs/flow/core
       - Verify @hex-di/flow-react resolves to libs/flow/react
       - Verify @hex-di/testing resolves to tooling/testing
       - Run `pnpm --filter @hex-di/react-showcase build`
       - Run `pnpm --filter @hex-di/react-showcase typecheck`
       - Run `pnpm --filter @hex-di/react-showcase test`

    The workspace:* protocol should automatically resolve to the new locations.
    If any dependencies resolve incorrectly, note them for fixing.

  </action>
  <verify>pnpm list --filter @hex-di/hono-todo --depth=1 | grep integrations/hono</verify>
  <done>Both examples build successfully and their workspace dependencies resolve to the correct new locations</done>
</task>

<task type="auto">
  <name>Task 2: Verify website build</name>
  <files>website/package.json</files>
  <action>
    Verify the Docusaurus website builds correctly:

    1. Run `pnpm --filter website typecheck`
    2. Run `pnpm --filter website build`
    3. Check if website has any workspace dependencies (it shouldn't based on current setup)
    4. If website build fails, investigate and fix any workspace configuration issues

    Note: The website doesn't directly import hex-di packages, so this should pass without changes.

  </action>
  <verify>pnpm --filter website build</verify>
  <done>Website builds successfully with no errors</done>
</task>

<task type="auto">
  <name>Task 3: Search and fix stale path references</name>
  <files>examples/**, website/**</files>
  <action>
    Search for any stale references to old package paths and fix if found:

    1. Search for Phase 20 moved packages:
       ```bash
       grep -r "packages/react\|packages/hono" \
         --include="*.ts" --include="*.tsx" --include="*.json" \
         --include="*.md" --include="*.yaml" --include="*.yml" \
         examples/ website/ \
         | grep -v node_modules | grep -v dist | grep -v .vite-temp
       ```

    2. Search for Phase 21 moved packages:
       ```bash
       grep -r "packages/testing\|packages/visualization\|packages/graph-viz" \
         --include="*.ts" --include="*.tsx" --include="*.json" \
         --include="*.md" --include="*.yaml" --include="*.yml" \
         examples/ website/ \
         | grep -v node_modules | grep -v dist | grep -v .vite-temp

       grep -r "packages/flow\|packages/flow-react" \
         --include="*.ts" --include="*.tsx" --include="*.json" \
         --include="*.md" --include="*.yaml" --include="*.yml" \
         examples/ website/ \
         | grep -v node_modules | grep -v dist | grep -v .vite-temp
       ```

    3. If any stale references found (excluding .planning/ directory which is historical):
       - Update import paths if needed (shouldn't be needed with workspace:*)
       - Update documentation links if they point to old paths
       - Update any config files with hardcoded paths

    Expected: No changes needed since workspace:* protocol handles resolution by name.

  </action>
  <verify>grep -r "packages/react\|packages/hono" examples/ website/ | grep -v node_modules | wc -l</verify>
  <done>No stale path references exist in examples or website directories</done>
</task>

</tasks>

<verification>
1. hono-todo example builds and runs correctly
2. react-showcase example builds, tests pass, and runs correctly
3. Website builds without errors
4. All workspace dependencies resolve to correct new locations
5. No stale path references in source code or documentation
</verification>

<success_criteria>

- [ ] REF-01: hono-todo workspace dependencies resolve correctly (@hex-di/hono → integrations/hono)
- [ ] REF-02: react-showcase workspace dependencies resolve correctly (all moved packages found)
- [ ] REF-03: website builds successfully
- [ ] No stale references to packages/\* for moved packages in examples or website
      </success_criteria>

<output>
After completion, create `.planning/phases/22-verification-references/22-01-SUMMARY.md`
</output>
