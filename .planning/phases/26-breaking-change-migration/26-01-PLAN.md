---
phase: 26-breaking-change-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/collectors/composite.ts
  - packages/core/src/collectors/index.ts
  - packages/core/src/collectors/memory.ts
  - packages/core/src/collectors/noop.ts
  - packages/core/src/collectors/types.ts
  - packages/core/src/span/index.ts
  - packages/core/src/span/types.ts
  - packages/core/src/index.ts
autonomous: true

must_haves:
  truths:
    - "@hex-di/core no longer exports TraceCollector, TracingAPI, ResolutionSpan"
    - "@hex-di/core no longer exports MemoryCollector, NoOpCollector, CompositeCollector"
    - "TypeScript build of @hex-di/core succeeds without old tracing types"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Core exports without old tracing"
      excludes:
        [
          "TraceCollector",
          "TracingAPI",
          "ResolutionSpan",
          "MemoryCollector",
          "NoOpCollector",
          "CompositeCollector",
        ]
  key_links:
    - from: "packages/core/src/index.ts"
      to: "./collectors"
      via: "no export"
      pattern: "!./collectors"
    - from: "packages/core/src/index.ts"
      to: "./span"
      via: "no export"
      pattern: "!./span"
---

<objective>
Remove all old tracing types and implementations from @hex-di/core package

Purpose: Begin the breaking change migration by removing the foundation types that the old tracing system was built on
Output: @hex-di/core package with collectors/ and span/ directories deleted, exports cleaned
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-breaking-change-migration/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old tracing directories from core</name>
  <files>packages/core/src/collectors/, packages/core/src/span/</files>
  <action>
    Delete the entire collectors/ and span/ directories from packages/core/src/:
    - Remove packages/core/src/collectors/ (contains composite.ts, index.ts, memory.ts, noop.ts, types.ts)
    - Remove packages/core/src/span/ (contains index.ts, types.ts if exists)

    These directories contain the old TraceCollector, TracingAPI, ResolutionSpan, MemoryCollector, NoOpCollector, and CompositeCollector types that are being replaced by the new @hex-di/tracing system.

    Per project rules: No backward compatibility, break and change freely, delete over deprecate.

  </action>
  <verify>ls packages/core/src/ | grep -E "collectors|span" returns empty</verify>
  <done>collectors/ and span/ directories no longer exist in packages/core/src/</done>
</task>

<task type="auto">
  <name>Task 2: Remove old tracing exports from core index</name>
  <files>packages/core/src/index.ts</files>
  <action>
    Edit packages/core/src/index.ts to remove all exports related to old tracing:
    - Remove any export lines containing "./collectors" path
    - Remove any export lines containing "./span" path
    - Remove any re-exports of TraceCollector, TracingAPI, ResolutionSpan
    - Remove any re-exports of MemoryCollector, NoOpCollector, CompositeCollector

    Keep all other exports intact (adapters, context, errors, inspection, ports, utils).

    After removal, the file should have no references to the deleted directories.

  </action>
  <verify>grep -E "collectors|span|TraceCollector|TracingAPI|ResolutionSpan|MemoryCollector|NoOpCollector|CompositeCollector" packages/core/src/index.ts returns empty</verify>
  <done>packages/core/src/index.ts contains no old tracing exports</done>
</task>

<task type="auto">
  <name>Task 3: Verify core package builds without old tracing</name>
  <files>packages/core/</files>
  <action>
    Run TypeScript build for the core package to ensure it compiles without the old tracing types:
    - Run: pnpm --filter @hex-di/core typecheck
    - Run: pnpm --filter @hex-di/core build

    Both commands should succeed. If there are any errors about missing types within core itself (unlikely since we're removing self-contained modules), fix them.

    Note: This may cause errors in dependent packages (runtime, integrations) but those will be fixed in subsequent plans.

  </action>
  <verify>pnpm --filter @hex-di/core typecheck && echo "SUCCESS"</verify>
  <done>@hex-di/core builds successfully without old tracing types</done>
</task>

</tasks>

<verification>
- No collectors/ directory exists in packages/core/src/
- No span/ directory exists in packages/core/src/
- packages/core/src/index.ts has no tracing-related exports
- pnpm --filter @hex-di/core typecheck passes
- pnpm --filter @hex-di/core build succeeds
</verification>

<success_criteria>

- MIGR-01 complete: TraceCollector, TracingAPI, ResolutionSpan types removed from @hex-di/core
- MIGR-04 partially complete: MemoryCollector, NoOpCollector, CompositeCollector removed from @hex-di/core
- @hex-di/core package builds independently without old tracing
  </success_criteria>

<output>
After completion, create `.planning/phases/26-breaking-change-migration/26-01-SUMMARY.md`
</output>
