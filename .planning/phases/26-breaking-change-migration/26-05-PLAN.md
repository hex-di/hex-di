---
phase: 26-breaking-change-migration
plan: 05
type: execute
wave: 3
depends_on: ["26-01", "26-02", "26-03", "26-04"]
files_modified:
  - README.md
  - docs/
  - libs/flow/core/
autonomous: true

must_haves:
  truths:
    - "Full monorepo typecheck passes with zero tracing-related errors"
    - "Full monorepo test suite passes (minus pre-existing failures)"
    - "No references to old tracing types anywhere in codebase"
  artifacts:
    - path: "."
      provides: "Clean monorepo without old tracing"
      excludes: ["TraceCollector", "TracingAPI", "ResolutionSpan", "container.tracer"]
  key_links:
    - from: "pnpm -r typecheck"
      to: "success"
      via: "command execution"
      pattern: "exit 0"
---

<objective>
Final verification and cleanup of the entire monorepo after breaking change migration

Purpose: Ensure the complete removal of old tracing is successful across all packages and documentation
Output: Fully migrated monorepo that builds, typechecks, and tests clean
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-breaking-change-migration/26-RESEARCH.md
@.planning/phases/26-breaking-change-migration/26-01-SUMMARY.md
@.planning/phases/26-breaking-change-migration/26-02-SUMMARY.md
@.planning/phases/26-breaking-change-migration/26-03-SUMMARY.md
@.planning/phases/26-breaking-change-migration/26-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check libs/flow/core for tracing dependencies</name>
  <files>libs/flow/core/</files>
  <action>
    Verify if libs/flow/core needs migration or is independent:

    1. Check if flow's tracing imports from @hex-di/core old types:
       grep -r "from.*@hex-di/core.*TraceCollector\|from.*@hex-di/core.*TracingAPI" libs/flow/core/src/ --include="*.ts" || echo "No imports from DI tracing"

    2. Check flow's own tracing module:
       ls libs/flow/core/src/tracing/ 2>/dev/null || echo "No flow tracing module"

    3. If flow has its own collectors that are independent (FlowCollector, etc.):
       - Leave them as-is if they don't import from @hex-di/core old types
       - If they do import old DI types, remove those imports and fix any type errors

    4. Run typecheck for flow:
       pnpm --filter "@libs/flow/core" typecheck || echo "Flow typecheck status"

    Note: Per research, flow may have its own independent tracing that doesn't need migration.

  </action>
  <verify>! grep -r "from.*@hex-di/core.*TraceCollector\|from.*@hex-di/runtime.*trace(" libs/flow/core/src/</verify>
  <done>libs/flow/core verified - either migrated or independent of DI tracing</done>
</task>

<task type="auto">
  <name>Task 2: Update documentation</name>
  <files>README.md, docs/</files>
  <action>
    Remove references to old tracing from documentation:

    1. Update root README.md:
       - Search for "container.tracer", "Built-in Tracing", "trace(", "enableTracing("
       - Replace with references to @hex-di/tracing and instrumentContainer pattern
       - Update any code examples showing old tracing

    2. Check for docs directory:
       test -d docs && find docs -name "*.md" -exec grep -l "container\.tracer\|trace(\|enableTracing(" {} \; || echo "No docs with old tracing"

    3. For any documentation files found:
       - Update to show new @hex-di/tracing approach
       - Replace old API references with instrumentContainer pattern
       - Update any migration guides

    Key changes:
    - OLD: "Built-in Tracing & Inspection"
    - NEW: "Distributed Tracing via @hex-di/tracing"
    - OLD: container.tracer examples
    - NEW: instrumentContainer() examples

  </action>
  <verify>! grep -r "container\.tracer\|Built-in Tracing.*Inspection" README.md docs/ 2>/dev/null</verify>
  <done>Documentation updated to reflect new tracing approach</done>
</task>

<task type="auto">
  <name>Task 3: Full monorepo verification</name>
  <files>.</files>
  <action>
    Run comprehensive verification of the entire monorepo:

    1. Full typecheck across all packages:
       pnpm -r typecheck 2>&1 | tee typecheck-results.txt

       Expected pre-existing issues (ignore these):
       - packages/graph: 11 errors, 12 warnings
       - libs/flow/core: 31 warnings

    2. Search for any remaining old tracing references:
       grep -r "TraceCollector\|TracingAPI\|ResolutionSpan\|MemoryCollector\|NoOpCollector\|CompositeCollector\|container\.tracer\|trace(\|enableTracing(" \
         --include="*.ts" --include="*.tsx" --include="*.js" \
         --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.git \
         packages/ integrations/ examples/ libs/ 2>/dev/null || echo "No old tracing found"

    3. Run full test suite:
       pnpm -r test 2>&1 | tee test-results.txt

       Expected pre-existing failures (ignore these):
       - examples/react-showcase: 12 test failures

    4. Verify build:
       pnpm -r build

    Success criteria: No NEW errors related to missing tracing types. Pre-existing issues are acceptable.

  </action>
  <verify>pnpm -r typecheck 2>&1 | grep -v "packages/graph\|libs/flow/core" | grep -c "error" | grep "^0$" || echo "Check for non-pre-existing errors"</verify>
  <done>Full monorepo builds, typechecks, and tests successfully (minus pre-existing issues)</done>
</task>

</tasks>

<verification>
- No references to old tracing types in any package
- pnpm -r typecheck passes (ignoring pre-existing graph/flow issues)
- pnpm -r test passes (ignoring pre-existing react-showcase failures)
- Documentation updated to reflect new tracing
- libs/flow/core either migrated or confirmed independent
</verification>

<success_criteria>

- MIGR-09 complete: Full pnpm -r typecheck and pnpm -r test pass after migration
- All old tracing completely removed from codebase
- Monorepo ready for v7.0 release
- Migration serves as clean break - no backward compatibility
  </success_criteria>

<output>
After completion, create `.planning/phases/26-breaking-change-migration/26-05-SUMMARY.md`
</output>
