---
phase: 26-breaking-change-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/trace.ts
  - packages/runtime/src/tracing/index.ts
  - packages/runtime/src/index.ts
  - packages/runtime/src/types/container.ts
  - packages/runtime/src/container/factory.ts
  - packages/runtime/src/container/wrappers.ts
  - packages/runtime/src/container/wrapper-utils.ts
autonomous: true

must_haves:
  truths:
    - "@hex-di/runtime no longer exports trace() or enableTracing() functions"
    - "Container type no longer has tracer property"
    - "@hex-di/runtime builds without dependency on old tracing types"
  artifacts:
    - path: "packages/runtime/src/index.ts"
      provides: "Runtime exports without trace functions"
      excludes: ["trace", "enableTracing"]
    - path: "packages/runtime/src/types/container.ts"
      provides: "Container type without tracer property"
      excludes: ["tracer: TracingAPI"]
  key_links:
    - from: "packages/runtime/src/index.ts"
      to: "./trace"
      via: "no export"
      pattern: "!./trace"
    - from: "packages/runtime/src/types/container.ts"
      to: "TracingAPI"
      via: "no import"
      pattern: "!TracingAPI"
---

<objective>
Remove old tracing APIs and container.tracer property from @hex-di/runtime package

Purpose: Remove the runtime-level tracing integration that coupled tracing directly to containers
Output: @hex-di/runtime without trace(), enableTracing(), or container.tracer property
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-breaking-change-migration/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove trace.ts and tracing re-exports</name>
  <files>packages/runtime/src/trace.ts, packages/runtime/src/tracing/index.ts, packages/runtime/src/index.ts</files>
  <action>
    Remove the old tracing API files and exports:
    1. Delete packages/runtime/src/trace.ts (contains trace() and enableTracing() functions)
    2. Edit packages/runtime/src/tracing/index.ts to remove any re-exports of old core tracing types
       - Remove imports/exports of TraceCollector, TracingAPI, ResolutionSpan
       - Remove imports/exports of MemoryCollector, NoOpCollector, CompositeCollector
       - If the file becomes empty or only has comments, delete it entirely
    3. Edit packages/runtime/src/index.ts:
       - Remove the line: export { trace, enableTracing } from "./trace.js"
       - Remove any exports from "./tracing" if they exist
       - Keep all other exports intact

    Per project rules: Delete completely, no commented-out code, no compatibility wrappers.

  </action>
  <verify>test ! -f packages/runtime/src/trace.ts && grep -v "trace\|enableTracing" packages/runtime/src/index.ts > /dev/null</verify>
  <done>trace.ts deleted and trace/enableTracing no longer exported from @hex-di/runtime</done>
</task>

<task type="auto">
  <name>Task 2: Remove container.tracer property</name>
  <files>packages/runtime/src/types/container.ts, packages/runtime/src/container/factory.ts, packages/runtime/src/container/wrappers.ts, packages/runtime/src/container/wrapper-utils.ts</files>
  <action>
    Remove the tracer property from Container type and all implementations:

    1. Edit packages/runtime/src/types/container.ts:
       - Remove the line: readonly tracer: TracingAPI;
       - Remove any import of TracingAPI type from @hex-di/core

    2. Edit packages/runtime/src/container/factory.ts:
       - Remove tracer?: TracingAPI; from any option types
       - Remove any tracer property initialization in createContainer
       - Remove any tracer-related imports

    3. Edit packages/runtime/src/container/wrappers.ts:
       - Remove tracer?: TracingAPI; from wrapper options
       - Remove any tracer property forwarding

    4. Edit packages/runtime/src/container/wrapper-utils.ts:
       - Remove tracer?: TracingAPI; from any utility types
       - Remove readonly tracer: TracingAPI; from any interfaces
       - Remove any tracer-related logic

    The container will no longer have built-in tracing. Users will use instrumentContainer() from @hex-di/tracing instead.

  </action>
  <verify>grep -r "tracer.*TracingAPI\|TracingAPI.*tracer" packages/runtime/src/ returns empty</verify>
  <done>Container type and implementations no longer have tracer property</done>
</task>

<task type="auto">
  <name>Task 3: Verify runtime package builds</name>
  <files>packages/runtime/</files>
  <action>
    Run TypeScript build for the runtime package:
    - Run: pnpm --filter @hex-di/runtime typecheck
    - Run: pnpm --filter @hex-di/runtime build

    Fix any internal type errors (there shouldn't be many since we're removing self-contained features).

    Note: This will likely cause errors in integration packages and examples that depend on these APIs, but those will be fixed in subsequent plans.

    Expected: Build should succeed since runtime no longer depends on deleted core types.

  </action>
  <verify>pnpm --filter @hex-di/runtime typecheck && echo "SUCCESS"</verify>
  <done>@hex-di/runtime builds successfully without old tracing</done>
</task>

</tasks>

<verification>
- packages/runtime/src/trace.ts no longer exists
- packages/runtime/src/index.ts doesn't export trace or enableTracing
- No container.tracer property in any runtime types or implementations
- pnpm --filter @hex-di/runtime typecheck passes
- pnpm --filter @hex-di/runtime build succeeds
</verification>

<success_criteria>

- MIGR-02 complete: trace(), enableTracing() removed from @hex-di/runtime
- MIGR-03 complete: container.tracer property removed from @hex-di/runtime
- MIGR-04 complete: Runtime no longer re-exports old collectors
- @hex-di/runtime package builds independently
  </success_criteria>

<output>
After completion, create `.planning/phases/26-breaking-change-migration/26-02-SUMMARY.md`
</output>
