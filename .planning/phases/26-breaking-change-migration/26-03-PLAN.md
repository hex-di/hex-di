---
phase: 26-breaking-change-migration
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - integrations/hono/src/
  - integrations/react/src/
  - packages/testing/src/
autonomous: true

must_haves:
  truths:
    - "@hex-di/hono integration uses new @hex-di/tracing system"
    - "@hex-di/react integration uses new @hex-di/tracing system"
    - "@hex-di/testing has no references to old tracing types"
  artifacts:
    - path: "integrations/hono/src/"
      provides: "Hono integration without old tracing"
      excludes: ["TraceCollector", "TracingAPI", "container.tracer"]
    - path: "integrations/react/src/"
      provides: "React integration without old tracing"
      excludes: ["TraceCollector", "TracingAPI", "container.tracer"]
  key_links:
    - from: "integrations/*/src/"
      to: "@hex-di/tracing"
      via: "import if needed"
      pattern: "instrumentContainer|TracerPort"
---

<objective>
Update integration packages to remove dependencies on old tracing system

Purpose: Migrate @hex-di/hono, @hex-di/react, and @hex-di/testing packages to work without the removed tracing APIs
Output: Integration packages that build and test successfully without old tracing
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-breaking-change-migration/26-RESEARCH.md
@.planning/phases/26-breaking-change-migration/26-01-SUMMARY.md
@.planning/phases/26-breaking-change-migration/26-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update @hex-di/hono integration</name>
  <files>integrations/hono/src/</files>
  <action>
    Search for and fix any references to old tracing in the Hono integration:

    1. Run grep to find old tracing usage:
       grep -r "TraceCollector\|TracingAPI\|ResolutionSpan\|MemoryCollector\|NoOpCollector\|CompositeCollector\|container\.tracer\|trace(\|enableTracing(" integrations/hono/src/ --include="*.ts"

    2. For each file with matches:
       - Remove imports of old tracing types from @hex-di/core or @hex-di/runtime
       - Remove any usage of container.tracer property
       - Remove any calls to trace() or enableTracing()
       - If tracing functionality is needed, import from @hex-di/tracing instead:
         import { instrumentContainer, TracerPort } from "@hex-di/tracing";

    3. Run typecheck to verify: pnpm --filter @hex-di/hono typecheck

    Note: Based on the research, integrations likely have minimal tracing usage. Most changes will be removing imports that are no longer needed.

  </action>
  <verify>grep -r "TraceCollector\|TracingAPI\|container\.tracer" integrations/hono/src/ returns empty</verify>
  <done>@hex-di/hono has no references to old tracing system</done>
</task>

<task type="auto">
  <name>Task 2: Update @hex-di/react integration</name>
  <files>integrations/react/src/</files>
  <action>
    Search for and fix any references to old tracing in the React integration:

    1. Run grep to find old tracing usage:
       grep -r "TraceCollector\|TracingAPI\|ResolutionSpan\|MemoryCollector\|NoOpCollector\|CompositeCollector\|container\.tracer\|trace(\|enableTracing(" integrations/react/src/ --include="*.ts" --include="*.tsx"

    2. For each file with matches:
       - Remove imports of old tracing types from @hex-di/core or @hex-di/runtime
       - Remove any usage of container.tracer property
       - Remove any calls to trace() or enableTracing()
       - If the React hooks/providers were using container.tracer for any reason, remove that logic
       - If tracing is needed, use @hex-di/tracing imports instead

    3. Run typecheck to verify: pnpm --filter @hex-di/react typecheck

    Note: React integration is UI-focused and likely has minimal tracing interaction.

  </action>
  <verify>grep -r "TraceCollector\|TracingAPI\|container\.tracer" integrations/react/src/ returns empty</verify>
  <done>@hex-di/react has no references to old tracing system</done>
</task>

<task type="auto">
  <name>Task 3: Update @hex-di/testing if needed</name>
  <files>packages/testing/src/</files>
  <action>
    Check and update the testing package for any old tracing references:

    1. Check if @hex-di/testing exists and uses old tracing:
       test -d packages/testing && grep -r "TraceCollector\|TracingAPI\|ResolutionSpan\|MemoryCollector\|NoOpCollector\|CompositeCollector" packages/testing/src/ --include="*.ts" || echo "No testing package or no tracing usage"

    2. If matches found:
       - Remove imports of old tracing types
       - If testing utilities were creating mock tracers, remove them
       - If test helpers were using container.tracer, update to use instrumentContainer() pattern
       - Consider if new test helpers are needed for @hex-di/tracing (but that's Phase 27 work)

    3. If testing package exists, verify it builds:
       test -d packages/testing && pnpm --filter @hex-di/testing typecheck || echo "No testing package"

    Note: MIGR-07 says "if it references old tracing types" - it may not need any changes.

  </action>
  <verify>test ! -d packages/testing || ! grep -r "TraceCollector\|TracingAPI" packages/testing/src/</verify>
  <done>@hex-di/testing has no old tracing references or doesn't exist</done>
</task>

</tasks>

<verification>
- No references to TraceCollector, TracingAPI, ResolutionSpan in integrations/
- No references to MemoryCollector, NoOpCollector, CompositeCollector in integrations/
- No usage of container.tracer in integration packages
- pnpm --filter @hex-di/hono typecheck passes
- pnpm --filter @hex-di/react typecheck passes
- @hex-di/testing (if exists) has no old tracing references
</verification>

<success_criteria>

- MIGR-05 complete: @hex-di/hono updated to use new tracing system
- MIGR-06 complete: @hex-di/react updated to use new tracing system
- MIGR-07 complete: @hex-di/testing checked and updated if needed
- All integration packages build successfully
  </success_criteria>

<output>
After completion, create `.planning/phases/26-breaking-change-migration/26-03-SUMMARY.md`
</output>
