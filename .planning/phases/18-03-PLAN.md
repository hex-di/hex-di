# Plan 18-03: Inspector API Tests

```yaml
wave: 2
depends_on: []
files_modified:
  - packages/runtime/tests/inspector.test.ts [MODIFY]
autonomous: true
```

## Goal

Expand existing inspector.test.ts with 8+ new tests covering lifecycle stage inspection and cross-scope hierarchy reporting.

## Context

From research:

- Inspector API has 14 methods for container introspection
- Existing inspector.test.ts has basic coverage but missing lifecycle/hierarchy tests
- Inspector is now built into containers via `container.inspector` property
- Methods include: getSnapshot, getScopeTree, listPorts, isResolved, subscribe, getPhase, getChildContainers

From Phase 18 context:

- Test all lifecycle stages: pre-resolve, mid-resolve, post-dispose
- Cross-scope inspection: parent/child, overrides
- Inspector must report accurate data at all stages

## Tasks

<task>
Add lifecycle stage tests to packages/runtime/tests/inspector.test.ts

New test sections:

- Lifecycle stage tests (4 tests)
  - Inspector returns accurate data before any resolutions
  - Inspector captures mid-resolution state correctly
  - Inspector reflects disposed container state
  - Inspector tracks phase transitions (uninitialized -> initialized)
    </task>

<task>
Add cross-scope hierarchy tests

New test sections:

- Cross-scope hierarchy (4 tests)
  - getScopeTree returns complete hierarchy with nested scopes
  - getChildContainers lists all direct child containers
  - Inspector correctly reports override container relationships
  - Parent/child inspector coordination during disposal
    </task>

<task>
Enhance existing test utilities

Add helpers:

- createComplexHierarchy() - Build multi-level container/scope tree
- snapshotAllLevels() - Capture state at each hierarchy level
- verifyHierarchyIntegrity() - Validate parent/child relationships
- trackPhaseTransitions() - Monitor container phase changes
  </task>

## Verification

Run tests and verify:

```bash
cd packages/runtime
pnpm test inspector.test.ts
```

Expected changes:

- 8+ new tests added to existing file
- Total inspector tests increase from ~20 to ~28
- Lifecycle and hierarchy coverage comprehensive
- All 14 InspectorAPI methods have test coverage

## must_haves

- [ ] 4 lifecycle stage tests covering all container phases
- [ ] 4 cross-scope hierarchy tests with multi-level structures
- [ ] Tests validate inspector accuracy at each lifecycle stage
- [ ] Tests verify parent/child/override relationships reported correctly
- [ ] Existing tests remain intact and passing
