---
phase: 13-runtime-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph/src/graph/types/inspection.ts
  - packages/graph/src/graph/inspection/inspector.ts
  - packages/graph/src/builder/builder.ts
  - packages/graph/tests/inspection-summary.test.ts
autonomous: true

must_haves:
  truths:
    - "Developer can call GraphBuilder.inspect({ summary: true }) and get a GraphSummary"
    - "Summary contains exactly 7 fields as specified"
    - "Default inspect() behavior returns full GraphInspection unchanged"
  artifacts:
    - path: "packages/graph/src/graph/types/inspection.ts"
      provides: "GraphSummary type with 7 required fields"
      exports: ["GraphSummary", "InspectOptions"]
    - path: "packages/graph/src/graph/inspection/inspector.ts"
      provides: "inspectGraph with summary mode support"
      exports: ["inspectGraph"]
    - path: "packages/graph/tests/inspection-summary.test.ts"
      provides: "Tests for summary mode"
      min_lines: 100
  key_links:
    - from: "packages/graph/src/builder/builder.ts"
      to: "inspectGraph"
      via: "passes options parameter"
      pattern: "inspectGraph.*options"
    - from: "packages/graph/src/graph/inspection/inspector.ts"
      to: "GraphSummary"
      via: "returns summary when options.summary is true"
      pattern: "if.*options\\.summary"
---

<objective>
Implement inspection summary mode for GraphBuilder per RUN-01 requirement

Purpose: Enable quick health checks without processing full inspection data (15+ fields vs 7 fields)
Output: GraphBuilder.inspect() supports summary mode via options parameter
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v4.0-REQUIREMENTS.md
@.planning/phases/13-runtime-features/13-RESEARCH.md

# Key files from research

@packages/graph/src/graph/types/inspection.ts
@packages/graph/src/graph/inspection/inspector.ts
@packages/graph/src/builder/builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GraphSummary type and InspectOptions</name>
  <files>packages/graph/src/graph/types/inspection.ts</files>
  <action>
    Add GraphSummary interface with the 7 fields specified in RUN-01:
    - adapterCount: number
    - asyncAdapterCount: number
    - isComplete: boolean (no missing ports)
    - missingPorts: string[] (same as unsatisfiedRequirements)
    - isValid: boolean (isComplete && errors.length === 0)
    - errors: string[] (validation errors only, not inspection metadata)
    - provides: string[] (list of provided port names)

    Add InspectOptions interface:
    - summary?: boolean (optional flag for summary mode)
    - seed?: string (existing option)
    - generator?: CorrelationIdGenerator (existing option)

    Export both new types alongside existing GraphInspection.

  </action>
  <verify>grep -A 7 "interface GraphSummary" packages/graph/src/graph/types/inspection.ts</verify>
  <done>GraphSummary type exists with all 7 required fields and InspectOptions includes summary flag</done>
</task>

<task type="auto">
  <name>Task 2: Extend inspectGraph to support summary mode</name>
  <files>packages/graph/src/graph/inspection/inspector.ts</files>
  <action>
    Update inspectGraph function:
    1. Add options parameter with default empty object (backward compatible)
    2. Add overload signatures for type safety:
       - inspectGraph(graph: InspectableGraph): GraphInspection
       - inspectGraph(graph: InspectableGraph, options: { summary: true }): GraphSummary
       - inspectGraph(graph: InspectableGraph, options?: InspectOptions): GraphInspection | GraphSummary
    3. In implementation, check options.summary:
       - If true, build and return GraphSummary from the graph data
       - Extract adapter count, async adapter count, missing ports, errors, and provides list
       - Derive isComplete (missingPorts.length === 0) and isValid (isComplete && errors.length === 0)
    4. If false/undefined, use existing full inspection logic

    Ensure GraphSummary objects are deeply frozen like GraphInspection objects.

  </action>
  <verify>grep -A 3 "options\\.summary" packages/graph/src/graph/inspection/inspector.ts</verify>
  <done>inspectGraph supports summary mode and returns appropriate type based on options</done>
</task>

<task type="auto">
  <name>Task 3: Update GraphBuilder.inspect() and add tests</name>
  <files>packages/graph/src/builder/builder.ts, packages/graph/tests/inspection-summary.test.ts</files>
  <action>
    In builder.ts:
    1. Update inspect() method signature to accept optional InspectOptions parameter
    2. Add overloads matching inspectGraph for type safety
    3. Pass options through to inspectGraph call

    Create inspection-summary.test.ts:
    1. Test default behavior (no options) returns full GraphInspection
    2. Test { summary: true } returns GraphSummary with 7 fields
    3. Test summary field values are correct:
       - adapterCount matches graph.adapters.length
       - asyncAdapterCount counts Promise-returning factories
       - isComplete is false when ports are missing
       - missingPorts lists unregistered required ports
       - isValid is true only when complete with no errors
       - errors contains validation errors (empty for valid graphs)
       - provides lists all provided port names
    4. Test that summary objects are deeply frozen
    5. Test backward compatibility (existing code without options works)

  </action>
  <verify>pnpm --filter @hex-di/graph test inspection-summary</verify>
  <done>GraphBuilder.inspect() supports summary mode and all tests pass</done>
</task>

</tasks>

<verification>
1. Run: `pnpm --filter @hex-di/graph typecheck`
2. Run: `pnpm --filter @hex-di/graph test inspection`
3. Verify GraphBuilder.inspect({ summary: true }) returns 7-field summary
4. Verify default inspect() still returns full GraphInspection
</verification>

<success_criteria>

- [ ] GraphSummary type defined with 7 required fields
- [ ] inspectGraph() accepts options parameter with summary flag
- [ ] GraphBuilder.inspect() passes options to inspectGraph
- [ ] Summary mode returns correct field values
- [ ] Backward compatibility maintained (no breaking changes)
- [ ] All graph package tests pass
      </success_criteria>

<output>
After completion, create `.planning/phases/13-runtime-features/13-01-SUMMARY.md`
</output>
