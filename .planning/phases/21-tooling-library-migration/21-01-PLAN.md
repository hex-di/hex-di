---
phase: 21-tooling-library-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [tooling/testing, tooling/visualization, tooling/graph-viz, libs/flow/core, libs/flow/react]
autonomous: true

must_haves:
  truths:
    - "All five packages are physically moved to their new locations"
    - "Git history is preserved for all moved packages"
    - "pnpm workspace recognizes all packages at new locations"
  artifacts:
    - path: "tooling/testing/package.json"
      provides: "Testing utilities package"
      contains: '"name": "@hex-di/testing"'
    - path: "tooling/visualization/package.json"
      provides: "Visualization package"
      contains: '"name": "@hex-di/visualization"'
    - path: "tooling/graph-viz/package.json"
      provides: "Graph visualization package"
      contains: '"name": "@hex-di/graph-viz"'
    - path: "libs/flow/core/package.json"
      provides: "Flow core package"
      contains: '"name": "@hex-di/flow"'
    - path: "libs/flow/react/package.json"
      provides: "Flow React package"
      contains: '"name": "@hex-di/flow-react"'
  key_links:
    - from: "pnpm workspace"
      to: "tooling/*"
      via: "pnpm-workspace.yaml glob"
      pattern: "tooling/\\*"
    - from: "pnpm workspace"
      to: "libs/*/*"
      via: "pnpm-workspace.yaml glob"
      pattern: "libs/\\*/\\*"
---

<objective>
Move all tooling and library packages to their semantic locations using git mv to preserve history, following the proven Phase 20 migration pattern.

Purpose: Complete the first wave of Phase 21's package migration, physically relocating packages while maintaining workspace integrity.
Output: All five packages moved to their new locations with pnpm workspace recognition working.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tooling-library-migration/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move packages with git mv and clean artifacts</name>
  <files>tooling/testing, tooling/visualization, tooling/graph-viz, libs/flow/core, libs/flow/react</files>
  <action>
    Create target directories and move all five packages using git mv to preserve history:
    1. Create tooling/ and libs/flow/ directories
    2. Move packages/testing → tooling/testing
    3. Move packages/visualization → tooling/visualization
    4. Move packages/graph-viz → tooling/graph-viz
    5. Move packages/flow → libs/flow/core
    6. Move packages/flow-react → libs/flow/react

    Clean all build artifacts from moved packages (node_modules, dist, .turbo) to prevent stale references.

    Note: pnpm-workspace.yaml already includes tooling/* and libs/*/* globs (added in Phase 20), so no workspace config update needed.

  </action>
  <verify>ls -la tooling/ libs/flow/ && git status --short | grep -E "^R"</verify>
  <done>All five packages moved to new locations with git showing rename operations</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate lockfile and verify workspace recognition</name>
  <files>pnpm-lock.yaml</files>
  <action>
    Run pnpm install to regenerate the lockfile with new package paths. The workspace:* protocol dependencies will automatically resolve to the new locations since pnpm resolves by package name, not path.

    After install, verify that:
    1. pnpm list shows all moved packages at their new locations
    2. No workspace resolution errors occur
    3. The lockfile regenerates successfully

    If any errors occur, they're likely due to leftover build artifacts - clean and retry.

  </action>
  <verify>pnpm list --depth 0 --json | grep -E '"@hex-di/(testing|visualization|graph-viz|flow|flow-react)"' | wc -l</verify>
  <done>pnpm install succeeds and all 5 moved packages are recognized in workspace</done>
</task>

</tasks>

<verification>
- Run `ls tooling/` and verify three packages present
- Run `ls libs/flow/` and verify two packages present
- Run `pnpm list --depth 0` and verify all packages listed
- Check git status shows rename operations (R flag)
</verification>

<success_criteria>
All five packages physically moved to their semantic locations with git history preserved. pnpm workspace recognizes all packages at new locations without any configuration changes (workspace globs already in place from Phase 20).
</success_criteria>

<output>
After completion, create `.planning/phases/21-tooling-library-migration/21-01-SUMMARY.md`
</output>
