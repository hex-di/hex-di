---
phase: 23-core-tracing-foundation
plan: 04
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - packages/tracing/src/adapters/memory/tracer.ts
  - packages/tracing/src/adapters/memory/span.ts
  - packages/tracing/src/adapters/memory/adapter.ts
  - packages/tracing/src/adapters/memory/index.ts
autonomous: true

must_haves:
  truths:
    - "MemoryTracer collects spans for testing"
    - "MemoryTracer tracks parent-child relationships"
    - "MemoryTracer has 10k span limit"
  artifacts:
    - path: "packages/tracing/src/adapters/memory/tracer.ts"
      provides: "MemoryTracer implementation"
      exports: ["MemoryTracer", "createMemoryTracer"]
    - path: "packages/tracing/src/adapters/memory/span.ts"
      provides: "MemorySpan class"
      exports: ["MemorySpan"]
    - path: "packages/tracing/src/adapters/memory/adapter.ts"
      provides: "MemoryTracerAdapter"
      exports: ["MemoryTracerAdapter"]
  key_links:
    - from: "packages/tracing/src/adapters/memory/span.ts"
      to: "packages/tracing/src/types/span.ts"
      via: "implements Span"
      pattern: "implements Span"
---

<objective>
Implement MemoryTracer adapter that collects SpanData in memory for testing, with getCollectedSpans() and clear() methods.

Purpose: Provide a tracer for testing that captures spans for assertions
Output: MemoryTracerAdapter with span collection capabilities
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-core-tracing-foundation/23-RESEARCH.md
@.planning/phases/23-core-tracing-foundation/23-01-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MemorySpan class</name>
  <files>packages/tracing/src/adapters/memory/span.ts</files>
  <action>
    Create MemorySpan class implementing Span interface:

    Constructor accepts:
    - name: string
    - parentContext?: SpanContext
    - options?: SpanOptions

    Properties:
    - context: SpanContext (generate IDs via utils)
    - startTime: number (performance.timeOrigin + performance.now())
    - attributes: Map for storing attributes
    - events: Array for storing events
    - status: SpanStatus (default 'unset')
    - recording: boolean (true until ended)

    Methods:
    - setAttribute/setAttributes: Store in Map
    - addEvent: Push to events array
    - recordException: Create exception event
    - setStatus: Update status
    - end(): Set recording=false, calculate duration
    - toSpanData(): Convert to SpanData for storage

    Use proper timing (performance.timeOrigin + performance.now()).

  </action>
  <verify>grep -l "implements Span" packages/tracing/src/adapters/memory/span.ts</verify>
  <done>MemorySpan class created with full Span interface</done>
</task>

<task type="auto">
  <name>Task 2: Create MemoryTracer class</name>
  <files>packages/tracing/src/adapters/memory/tracer.ts</files>
  <action>
    Create MemoryTracer class implementing Tracer interface:

    Private properties:
    - spans: SpanData[] (flat storage)
    - spanStack: Span[] (active span tracking)
    - maxSpans: number = 10000

    Methods:
    - startSpan: Create MemorySpan, push to stack
    - withSpan/withSpanAsync: Push/pop spanStack, handle exceptions
    - getActiveSpan: Return top of spanStack
    - getSpanContext: Return active span's context
    - withAttributes: Return new tracer with default attributes

    Additional methods:
    - getCollectedSpans(): Return copy of spans array
    - clear(): Empty spans and spanStack arrays
    - onSpanEnd(span): Convert to SpanData, add to spans (check limit)

    When limit hit, drop oldest spans (FIFO).
    Store parentSpanId for relationships.

  </action>
  <verify>grep -l "getCollectedSpans" packages/tracing/src/adapters/memory/tracer.ts</verify>
  <done>MemoryTracer created with collection methods</done>
</task>

<task type="auto">
  <name>Task 3: Create MemoryTracerAdapter</name>
  <files>packages/tracing/src/adapters/memory/adapter.ts, packages/tracing/src/adapters/memory/index.ts</files>
  <action>
    Create adapter.ts:

    MemoryTracerAdapter using createAdapter:
    - provides: TracerPort
    - requires: []
    - lifetime: 'transient' (new instance each time)
    - factory: () => new MemoryTracer()

    Create convenience factory:
    - createMemoryTracer(): Returns new MemoryTracer

    Create index.ts barrel export:
    - Export MemoryTracerAdapter
    - Export MemoryTracer, createMemoryTracer
    - Export MemorySpan for testing

    Add JSDoc explaining testing usage.

  </action>
  <verify>grep -l "export function createMemoryTracer" packages/tracing/src/adapters/memory/index.ts</verify>
  <done>MemoryTracerAdapter registered with factory function</done>
</task>

</tasks>

<verification>
- MemoryTracer collects spans: create spans, check getCollectedSpans()
- Parent-child tracking: nested spans have parentSpanId
- 10k limit enforced: add 10001 spans, verify oldest dropped
- clear() empties collection
- TypeScript validates: `pnpm --filter @hex-di/tracing typecheck`
</verification>

<success_criteria>

- Spans collected in flat array
- parentSpanId tracks relationships
- 10k span limit with FIFO eviction
- getCollectedSpans() returns copy
- clear() method works
- Exception auto-capture in withSpan
  </success_criteria>

<output>
After completion, create `.planning/phases/23-core-tracing-foundation/23-04-SUMMARY.md`
</output>
