---
phase: 23-core-tracing-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tracing/src/types/span.ts
  - packages/tracing/src/types/attributes.ts
  - packages/tracing/src/types/status.ts
  - packages/tracing/src/types/index.ts
autonomous: true

must_haves:
  truths:
    - "All tracing types are defined without type casts"
    - "AttributeValue follows OTel spec exactly"
    - "SpanContext has W3C compliant fields"
  artifacts:
    - path: "packages/tracing/src/types/span.ts"
      provides: "Span, SpanContext, SpanData, SpanOptions interfaces"
      exports: ["Span", "SpanContext", "SpanData", "SpanOptions", "SpanEvent"]
    - path: "packages/tracing/src/types/attributes.ts"
      provides: "AttributeValue type and Attributes map"
      exports: ["AttributeValue", "Attributes"]
    - path: "packages/tracing/src/types/status.ts"
      provides: "SpanKind and SpanStatus types"
      exports: ["SpanKind", "SpanStatus"]
  key_links:
    - from: "packages/tracing/src/types/span.ts"
      to: "packages/tracing/src/types/attributes.ts"
      via: "import Attributes"
      pattern: "import.*Attributes.*from.*attributes"
---

<objective>
Define all core types and interfaces for the tracing system following OpenTelemetry and W3C Trace Context specifications.

Purpose: Establish the type system foundation for distributed tracing
Output: Complete type definitions ready for adapter implementations
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-core-tracing-foundation/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define span types and interfaces</name>
  <files>packages/tracing/src/types/span.ts</files>
  <action>
    Create span.ts with all span-related types:

    SpanContext interface:
    - traceId: string (32 hex chars)
    - spanId: string (16 hex chars)
    - traceFlags: number (1 byte)
    - traceState?: string (optional W3C tracestate)

    SpanOptions interface:
    - kind?: SpanKind
    - attributes?: Attributes
    - links?: ReadonlyArray<SpanContext>
    - startTime?: number
    - root?: boolean

    SpanEvent interface:
    - name: string
    - time: number
    - attributes?: Attributes

    Span interface with methods:
    - setAttribute, setAttributes, addEvent, setStatus, recordException, end, isRecording

    SpanData interface for completed spans with all fields.

    Import types from attributes.ts and status.ts.
    Add comprehensive JSDoc per PERF-04 requirement.

  </action>
  <verify>grep -l "export interface Span" packages/tracing/src/types/span.ts</verify>
  <done>Span types defined following OTel spec</done>
</task>

<task type="auto">
  <name>Task 2: Define attribute types</name>
  <files>packages/tracing/src/types/attributes.ts</files>
  <action>
    Create attributes.ts with:

    AttributeValue type (union):
    - string | number | boolean | string[] | number[] | boolean[]

    Attributes type:
    - Readonly<Record<string, AttributeValue>>

    NO type casts allowed - this is a pure type definition.
    Document that arrays must be homogeneous (all same type).
    Add JSDoc explaining OTel attribute conventions.

  </action>
  <verify>grep -l "export type AttributeValue" packages/tracing/src/types/attributes.ts</verify>
  <done>Attribute types defined per OTel spec</done>
</task>

<task type="auto">
  <name>Task 3: Define status types and create barrel export</name>
  <files>packages/tracing/src/types/status.ts, packages/tracing/src/types/index.ts</files>
  <action>
    Create status.ts with:

    SpanKind type (literal union):
    - 'internal' | 'server' | 'client' | 'producer' | 'consumer'

    SpanStatus type (literal union):
    - 'unset' | 'ok' | 'error'

    Create index.ts barrel export:
    - Export all types from span.ts, attributes.ts, status.ts
    - Maintain clean public API surface

    Add JSDoc explaining when each kind/status is used.

  </action>
  <verify>ls packages/tracing/src/types/*.ts | wc -l | grep 4</verify>
  <done>All core types defined and exported</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `pnpm --filter @hex-di/tracing typecheck`
- No type casts in any file: `! grep -r "as \w" packages/tracing/src/types/`
- All types exported from index
</verification>

<success_criteria>

- All types follow OTel/W3C specs exactly
- No type casts used anywhere
- Comprehensive JSDoc on all public types
- Clean separation of concerns
  </success_criteria>

<output>
After completion, create `.planning/phases/23-core-tracing-foundation/23-02-SUMMARY.md`
</output>
