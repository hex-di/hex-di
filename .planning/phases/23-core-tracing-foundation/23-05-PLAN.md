---
phase: 23-core-tracing-foundation
plan: 05
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - packages/tracing/src/adapters/console/tracer.ts
  - packages/tracing/src/adapters/console/formatter.ts
  - packages/tracing/src/adapters/console/adapter.ts
  - packages/tracing/src/adapters/console/index.ts
autonomous: true

must_haves:
  truths:
    - "ConsoleTracer outputs human-readable logs"
    - "ConsoleTracer supports colorization and filtering"
    - "ConsoleTracer shows span hierarchy"
  artifacts:
    - path: "packages/tracing/src/adapters/console/tracer.ts"
      provides: "ConsoleTracer implementation"
      exports: ["ConsoleTracer", "ConsoleTracerOptions"]
    - path: "packages/tracing/src/adapters/console/formatter.ts"
      provides: "Output formatting utilities"
      exports: ["formatSpan", "colorize"]
    - path: "packages/tracing/src/adapters/console/adapter.ts"
      provides: "ConsoleTracerAdapter"
      exports: ["ConsoleTracerAdapter"]
  key_links:
    - from: "packages/tracing/src/adapters/console/tracer.ts"
      to: "console.log"
      via: "output on span.end()"
      pattern: "console.log"
---

<objective>
Implement ConsoleTracer adapter for development debugging with colorized output, timestamps, and minimum duration filtering.

Purpose: Provide human-readable tracing output for development
Output: ConsoleTracerAdapter with formatted console logging
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-core-tracing-foundation/23-RESEARCH.md
@.planning/phases/23-core-tracing-foundation/23-01-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create output formatter</name>
  <files>packages/tracing/src/adapters/console/formatter.ts</files>
  <action>
    Create formatter utilities:

    ConsoleTracerOptions interface:
    - colorize?: boolean (default: process.stdout.isTTY)
    - includeTimestamps?: boolean (default: true)
    - minDurationMs?: number (default: 0)
    - indent?: boolean (default: true)

    colorize(text, color) function:
    - Use ANSI escape codes if colorize enabled
    - Colors: cyan (name), yellow (duration), green (✓), red (✗), gray (attributes)
    - Return plain text if colorize disabled

    formatDuration(ms) function:
    - Format as "12.3ms" or "1.5s" based on magnitude

    formatSpan(spanData, depth, options) function:
    - Build output string with optional indentation
    - Format: [TRACE] name (duration) status timestamp
    - Include error message if status='error'
    - Skip if duration < minDurationMs

    Add JSDoc explaining formatting options.

  </action>
  <verify>grep -l "export function formatSpan" packages/tracing/src/adapters/console/formatter.ts</verify>
  <done>Formatter utilities created</done>
</task>

<task type="auto">
  <name>Task 2: Create ConsoleTracer class</name>
  <files>packages/tracing/src/adapters/console/tracer.ts</files>
  <action>
    Create ConsoleTracer implementing Tracer:

    Constructor accepts ConsoleTracerOptions.

    Private properties:
    - options: ConsoleTracerOptions
    - spanStack: Span[] (for hierarchy tracking)
    - depth: number (current nesting level)

    Methods (similar to MemoryTracer):
    - startSpan: Create span, track in stack
    - withSpan/withSpanAsync: Push/pop stack, adjust depth
    - getActiveSpan/getSpanContext: From stack
    - withAttributes: Return new tracer instance

    onSpanEnd(spanData) method:
    - Format span using formatter
    - Output to console.log if not filtered
    - Adjust depth based on stack

    Use ConsoleSpan class (minimal, just tracks data).
    Output immediately on span.end().

  </action>
  <verify>grep -l "export class ConsoleTracer" packages/tracing/src/adapters/console/tracer.ts</verify>
  <done>ConsoleTracer with formatted output</done>
</task>

<task type="auto">
  <name>Task 3: Create ConsoleTracerAdapter</name>
  <files>packages/tracing/src/adapters/console/adapter.ts, packages/tracing/src/adapters/console/index.ts</files>
  <action>
    Create adapter.ts:

    ConsoleTracerAdapter using createAdapter:
    - provides: TracerPort
    - requires: []
    - lifetime: 'singleton'
    - factory: (deps, options?: ConsoleTracerOptions) => new ConsoleTracer(options)

    Create convenience factory:
    - createConsoleTracer(options?): Returns new ConsoleTracer

    Create index.ts barrel export:
    - Export ConsoleTracerAdapter
    - Export ConsoleTracer, createConsoleTracer
    - Export ConsoleTracerOptions

    Add JSDoc with example output.

  </action>
  <verify>grep -l "export function createConsoleTracer" packages/tracing/src/adapters/console/index.ts</verify>
  <done>ConsoleTracerAdapter with factory</done>
</task>

</tasks>

<verification>
- Console output appears when spans end
- Colorization works if TTY detected
- minDurationMs filters short spans
- Indentation shows hierarchy
- TypeScript validates: `pnpm --filter @hex-di/tracing typecheck`
</verification>

<success_criteria>

- Human-readable console output
- Optional ANSI colors
- Duration filtering works
- Hierarchy shown via indentation
- Error status highlighted
- Timestamps included
  </success_criteria>

<output>
After completion, create `.planning/phases/23-core-tracing-foundation/23-05-SUMMARY.md`
</output>
