---
phase: 23-core-tracing-foundation
plan: 08
type: execute
wave: 4
depends_on: ["23-01", "23-02", "23-03", "23-04", "23-05", "23-06", "23-07"]
files_modified:
  - packages/tracing/src/index.ts
  - packages/tracing/src/adapters/index.ts
  - packages/tracing/tests/unit/noop.test.ts
  - packages/tracing/tests/unit/memory.test.ts
  - packages/tracing/tests/unit/console.test.ts
  - packages/tracing/tests/unit/propagation.test.ts
  - packages/tracing/tests/unit/id-generation.test.ts
  - packages/tracing/tests/integration/tracing.test.ts
  - packages/tracing/README.md
autonomous: true

must_haves:
  truths:
    - "Package exports all public APIs"
    - "All adapters have test coverage"
    - "W3C compliance is verified"
  artifacts:
    - path: "packages/tracing/src/index.ts"
      provides: "Main package exports"
      exports:
        [
          "TracerPort",
          "SpanExporterPort",
          "SpanProcessorPort",
          "NoOpTracerAdapter",
          "MemoryTracerAdapter",
          "ConsoleTracerAdapter",
        ]
    - path: "packages/tracing/tests/unit/noop.test.ts"
      provides: "NoOp adapter tests"
      min_lines: 50
    - path: "packages/tracing/tests/unit/memory.test.ts"
      provides: "Memory adapter tests"
      min_lines: 100
    - path: "packages/tracing/README.md"
      provides: "Package documentation"
      contains: "# @hex-di/tracing"
  key_links:
    - from: "packages/tracing/src/index.ts"
      to: "packages/tracing/src/ports/index.ts"
      via: "export all ports"
      pattern: "export.*from.*ports"
---

<objective>
Create comprehensive test suite, main exports, and documentation to complete the @hex-di/tracing package.

Purpose: Validate implementation and provide complete public API
Output: Fully tested and documented tracing package
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-core-tracing-foundation/23-RESEARCH.md
@.planning/phases/23-core-tracing-foundation/23-01-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-02-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-03-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-04-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-05-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-06-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create main exports</name>
  <files>packages/tracing/src/index.ts, packages/tracing/src/adapters/index.ts</files>
  <action>
    Create src/adapters/index.ts:
    - Export from './noop'
    - Export from './memory'
    - Export from './console'

    Create src/index.ts main exports:
    - Export all from './ports'
    - Export all from './types'
    - Export all from './adapters'
    - Export all from './context'
    - Export all from './utils'

    Organize exports by category with comments.
    Ensure clean public API surface.

  </action>
  <verify>grep -l "export.*from.*ports" packages/tracing/src/index.ts</verify>
  <done>Main package exports configured</done>
</task>

<task type="auto">
  <name>Task 2: Write adapter unit tests</name>
  <files>packages/tracing/tests/unit/noop.test.ts, packages/tracing/tests/unit/memory.test.ts, packages/tracing/tests/unit/console.test.ts</files>
  <action>
    Create noop.test.ts:
    - Test singleton span reuse
    - Verify no allocations (same instance)
    - Check isRecording() returns false
    - Verify withAttributes returns same tracer

    Create memory.test.ts:
    - Test span collection
    - Verify parent-child relationships
    - Test 10k limit with FIFO eviction
    - Test clear() method
    - Test exception capture in withSpan

    Create console.test.ts:
    - Mock console.log
    - Test output format
    - Test minDurationMs filtering
    - Test colorization (check for ANSI codes)

    Use vitest with describe/it/expect.

  </action>
  <verify>ls packages/tracing/tests/unit/*.test.ts | wc -l | grep 5</verify>
  <done>Adapter unit tests written</done>
</task>

<task type="auto">
  <name>Task 3: Write integration tests and documentation</name>
  <files>packages/tracing/tests/unit/propagation.test.ts, packages/tracing/tests/unit/id-generation.test.ts, packages/tracing/tests/integration/tracing.test.ts, packages/tracing/README.md</files>
  <action>
    Create propagation.test.ts:
    - Test W3C traceparent parsing (valid/invalid)
    - Test header extraction (case-insensitive)
    - Test context injection
    - Test all-zeros rejection

    Create id-generation.test.ts:
    - Test ID lengths (32/16 chars)
    - Test hex format
    - Test uniqueness (generate 1000, check no collisions)

    Create integration/tracing.test.ts:
    - Test adapter with DI container
    - Test context propagation round-trip
    - Test W3C compliance end-to-end

    Create README.md:
    - Package description
    - Installation: `pnpm add @hex-di/tracing`
    - Usage examples (NoOp, Memory, Console)
    - Port/adapter pattern explanation
    - W3C Trace Context support
    - Zero dependencies note

  </action>
  <verify>test -f packages/tracing/README.md && grep -l "# @hex-di/tracing" packages/tracing/README.md</verify>
  <done>Integration tests and documentation complete</done>
</task>

</tasks>

<verification>
- All tests pass: `pnpm --filter @hex-di/tracing test`
- Package builds: `pnpm --filter @hex-di/tracing build`
- TypeScript valid: `pnpm --filter @hex-di/tracing typecheck`
- README exists with examples
</verification>

<success_criteria>

- 100% of public API exported
- All adapters have test coverage
- W3C compliance verified in tests
- No type casts in test files
- Documentation includes usage examples
- Package ready for use
  </success_criteria>

<output>
After completion, create `.planning/phases/23-core-tracing-foundation/23-08-SUMMARY.md`
</output>
