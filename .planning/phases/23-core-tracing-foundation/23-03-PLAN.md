---
phase: 23-core-tracing-foundation
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified:
  - packages/tracing/src/adapters/noop/tracer.ts
  - packages/tracing/src/adapters/noop/adapter.ts
  - packages/tracing/src/adapters/noop/index.ts
autonomous: true

must_haves:
  truths:
    - "NoOpTracer has zero runtime overhead"
    - "NoOpTracer returns singleton frozen span"
    - "NoOpTracer makes no timing calls"
  artifacts:
    - path: "packages/tracing/src/adapters/noop/tracer.ts"
      provides: "NoOp tracer implementation"
      exports: ["NOOP_SPAN", "NOOP_TRACER"]
    - path: "packages/tracing/src/adapters/noop/adapter.ts"
      provides: "NoOpTracerAdapter via createAdapter"
      exports: ["NoOpTracerAdapter"]
  key_links:
    - from: "packages/tracing/src/adapters/noop/adapter.ts"
      to: "@hex-di/core"
      via: "createAdapter"
      pattern: "createAdapter.*TracerPort"
---

<objective>
Implement NoOpTracer adapter with zero runtime overhead - singleton frozen span, no allocations, no timing calls.

Purpose: Provide a zero-cost tracer for production when tracing is disabled
Output: NoOpTracerAdapter ready for use with TracerPort
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-core-tracing-foundation/23-RESEARCH.md
@.planning/phases/23-core-tracing-foundation/23-01-SUMMARY.md
@.planning/phases/23-core-tracing-foundation/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NoOp span and tracer singletons</name>
  <files>packages/tracing/src/adapters/noop/tracer.ts</files>
  <action>
    Create frozen singleton instances:

    NOOP_SPAN_CONTEXT with:
    - traceId: '00000000000000000000000000000000'
    - spanId: '0000000000000000'
    - traceFlags: 0

    NOOP_SPAN with:
    - context: NOOP_SPAN_CONTEXT
    - All methods as empty functions: () => {}
    - isRecording: () => false

    NOOP_TRACER with:
    - startSpan: () => NOOP_SPAN
    - withSpan: (_, fn) => fn(NOOP_SPAN)
    - withSpanAsync: async (_, fn) => fn(NOOP_SPAN)
    - getActiveSpan: () => undefined
    - getSpanContext: () => undefined
    - withAttributes: () => NOOP_TRACER (returns self)

    Use Object.freeze() on all objects.
    NO allocations, NO Date.now(), NO performance.now().

  </action>
  <verify>grep -l "Object.freeze" packages/tracing/src/adapters/noop/tracer.ts</verify>
  <done>NoOp singletons created with zero overhead</done>
</task>

<task type="auto">
  <name>Task 2: Create NoOpTracerAdapter</name>
  <files>packages/tracing/src/adapters/noop/adapter.ts, packages/tracing/src/adapters/noop/index.ts</files>
  <action>
    Create adapter.ts with:

    NoOpTracerAdapter using createAdapter:
    - provides: TracerPort
    - requires: []
    - lifetime: 'singleton'
    - factory: () => NOOP_TRACER

    Create index.ts barrel export:
    - Export NoOpTracerAdapter
    - Export NOOP_TRACER, NOOP_SPAN for testing

    Add JSDoc explaining zero-overhead design.

  </action>
  <verify>grep -l "export const NoOpTracerAdapter" packages/tracing/src/adapters/noop/adapter.ts</verify>
  <done>NoOpTracerAdapter registered with TracerPort</done>
</task>

</tasks>

<verification>
- NoOp tracer imports successfully
- startSpan returns same instance every time
- No timing function calls in implementation
- TypeScript validates: `pnpm --filter @hex-di/tracing typecheck`
</verification>

<success_criteria>

- Zero allocations after initialization
- No timing calls (Date.now, performance.now)
- Singleton span reused for all operations
- isRecording() returns false
- Object.freeze() used on all singletons
  </success_criteria>

<output>
After completion, create `.planning/phases/23-core-tracing-foundation/23-03-SUMMARY.md`
</output>
