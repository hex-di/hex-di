---
phase: 23-core-tracing-foundation
plan: 07
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - packages/tracing/src/utils/id-generation.ts
  - packages/tracing/src/utils/timing.ts
  - packages/tracing/src/utils/type-guards.ts
  - packages/tracing/src/utils/index.ts
autonomous: true

must_haves:
  truths:
    - "IDs are generated with proper randomness"
    - "Type guards validate without casts"
    - "Timing uses high-resolution timestamps"
  artifacts:
    - path: "packages/tracing/src/utils/id-generation.ts"
      provides: "Trace and span ID generation"
      exports: ["generateTraceId", "generateSpanId"]
    - path: "packages/tracing/src/utils/type-guards.ts"
      provides: "Type validation utilities"
      exports: ["isAttributeValue", "isSpanKind", "isSpanStatus"]
    - path: "packages/tracing/src/utils/timing.ts"
      provides: "High-resolution timing"
      exports: ["getHighResTimestamp"]
  key_links:
    - from: "packages/tracing/src/utils/id-generation.ts"
      to: "crypto.getRandomValues"
      via: "secure random generation"
      pattern: "crypto.getRandomValues"
---

<objective>
Implement trace/span ID generation using crypto.getRandomValues and utility functions for the tracing system.

Purpose: Provide core utilities for ID generation, timing, and type validation
Output: Utility functions ready for use by adapters
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-core-tracing-foundation/23-RESEARCH.md
@.planning/phases/23-core-tracing-foundation/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ID generation</name>
  <files>packages/tracing/src/utils/id-generation.ts</files>
  <action>
    Create ID generation functions:

    generateTraceId() function:
    - If crypto.getRandomValues available:
      - Create Uint8Array(16)
      - Fill with crypto.getRandomValues
      - Convert to 32 hex chars
    - Else fallback to Math.random():
      - Generate 32 random hex chars
    - Ensure not all zeros

    generateSpanId() function:
    - If crypto.getRandomValues available:
      - Create Uint8Array(8)
      - Fill with crypto.getRandomValues
      - Convert to 16 hex chars
    - Else fallback to Math.random():
      - Generate 16 random hex chars
    - Ensure not all zeros

    Helper bytesToHex(bytes: Uint8Array) function.
    Add JSDoc noting crypto preferred, Math.random for dev/test.

  </action>
  <verify>grep -l "export function generateTraceId" packages/tracing/src/utils/id-generation.ts</verify>
  <done>ID generation with crypto/fallback implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement type guards</name>
  <files>packages/tracing/src/utils/type-guards.ts</files>
  <action>
    Create type guard functions WITHOUT type casts:

    isAttributeValue(value: unknown): value is AttributeValue
    - Check string, number, boolean
    - For arrays: check all elements same type
    - Reject NaN, null, undefined, objects

    isSpanKind(value: unknown): value is SpanKind
    - Check literal values: 'internal', 'server', etc.

    isSpanStatus(value: unknown): value is SpanStatus
    - Check literal values: 'unset', 'ok', 'error'

    isValidTraceId(id: string): boolean
    - Check 32 hex chars pattern
    - Check not all zeros

    isValidSpanId(id: string): boolean
    - Check 16 hex chars pattern
    - Check not all zeros

    NO "as" casts anywhere. Use proper type narrowing.
    Add JSDoc explaining validation rules.

  </action>
  <verify>grep -l "export function isAttributeValue" packages/tracing/src/utils/type-guards.ts</verify>
  <done>Type guards implemented without casts</done>
</task>

<task type="auto">
  <name>Task 3: Implement timing and create barrel export</name>
  <files>packages/tracing/src/utils/timing.ts, packages/tracing/src/utils/index.ts</files>
  <action>
    Create timing.ts:

    getHighResTimestamp() function:
    - If performance.timeOrigin && performance.now():
      - Return performance.timeOrigin + performance.now()
    - Else fallback:
      - Return Date.now()
    - Returns milliseconds with microsecond precision

    formatDuration(ms: number): string
    - Format as "12.3ms" or "1.5s"

    Create index.ts barrel export:
    - Export all from id-generation.ts
    - Export all from type-guards.ts
    - Export all from timing.ts

    Add JSDoc explaining precision levels.

  </action>
  <verify>ls packages/tracing/src/utils/*.ts | wc -l | grep 4</verify>
  <done>Timing utilities and exports created</done>
</task>

</tasks>

<verification>
- IDs generated are correct length
- IDs are not all zeros
- Type guards work without casts
- Timing returns high-res timestamps
- TypeScript validates: `pnpm --filter @hex-di/tracing typecheck`
</verification>

<success_criteria>

- crypto.getRandomValues used when available
- Math.random fallback works
- Type guards use proper narrowing
- No type casts in implementation
- High-resolution timestamps
- All utilities exported
  </success_criteria>

<output>
After completion, create `.planning/phases/23-core-tracing-foundation/23-07-SUMMARY.md`
</output>
