---
phase: 31-tracing-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tracing/src/ports/tracer.ts
  - packages/tracing/src/adapters/noop/tracer.ts
  - packages/tracing/src/adapters/memory/tracer.ts
  - packages/tracing/src/adapters/console/tracer.ts
  - packages/tracing/src/instrumentation/hooks.ts
  - packages/tracing/tests/benchmarks/noop-overhead.bench.ts
autonomous: true

must_haves:
  truths:
    - "NoOp tracer overhead is reduced from ~34% to <20%"
    - "Early bailout prevents attribute object construction for NoOp tracer"
    - "Inline optimizations reduce function call overhead"
    - "All existing tests pass without modification"
  artifacts:
    - path: "packages/tracing/src/ports/tracer.ts"
      provides: "Tracer interface with isEnabled() method"
      contains: "isEnabled(): boolean"
    - path: "packages/tracing/src/adapters/noop/tracer.ts"
      provides: "NoOp tracer returning false for isEnabled()"
      contains: "isEnabled(): boolean { return false; }"
    - path: "packages/tracing/src/instrumentation/hooks.ts"
      provides: "Optimized hooks with early bailout"
      contains: "if (!tracer.isEnabled())"
  key_links:
    - from: "packages/tracing/src/instrumentation/hooks.ts"
      to: "tracer.isEnabled()"
      via: "Early bailout check"
      pattern: "if \\(!tracer\\.isEnabled\\(\\)\\)"
    - from: "packages/tracing/src/adapters/memory/tracer.ts"
      to: "isEnabled()"
      via: "Memory tracer enablement"
      pattern: "isEnabled\\(\\): boolean.*return true"
---

<objective>
Optimize NoOp tracer hot path to reduce overhead from ~34% to <20% through early bailout detection and inline optimizations.

Purpose: Address performance gap identified in v7.0 milestone audit where NoOp tracer overhead exceeds target by 3.4x.
Output: Optimized instrumentation hooks with minimal overhead when tracing is disabled.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-tracing-performance/31-RESEARCH.md

# Critical files from research

@packages/tracing/src/instrumentation/hooks.ts
@packages/tracing/src/ports/tracer.ts
@packages/tracing/src/adapters/noop/tracer.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add isEnabled() method to Tracer interface and implementations</name>
  <files>
    packages/tracing/src/ports/tracer.ts
    packages/tracing/src/adapters/noop/tracer.ts
    packages/tracing/src/adapters/memory/tracer.ts
    packages/tracing/src/adapters/console/tracer.ts
  </files>
  <action>
    Add isEnabled() method to the Tracer interface to allow early detection of NoOp tracers:

    1. In packages/tracing/src/ports/tracer.ts:
       - Add method signature: `isEnabled(): boolean;`
       - Add JSDoc explaining it returns false for NoOp, true for recording tracers

    2. In packages/tracing/src/adapters/noop/tracer.ts:
       - Add `isEnabled(): boolean { return false; }` to NOOP_TRACER frozen object
       - Add `isEnabled(): boolean { return false; }` to NOOP_SPAN frozen object (for completeness)

    3. In packages/tracing/src/adapters/memory/tracer.ts:
       - Add `isEnabled(): boolean { return true; }` to MemoryTracer class

    4. In packages/tracing/src/adapters/console/tracer.ts:
       - Add `isEnabled(): boolean { return true; }` to ConsoleTracer class

    This enables the hook machinery to detect NoOp tracers without building attribute objects.

  </action>
  <verify>pnpm --filter @hex-di/tracing typecheck</verify>
  <done>All tracer implementations have isEnabled() method returning appropriate values</done>
</task>

<task type="auto">
  <name>Task 2: Optimize instrumentation hooks with early bailout and inline optimizations</name>
  <files>
    packages/tracing/src/instrumentation/hooks.ts
  </files>
  <action>
    Optimize the beforeResolve and afterResolve hooks to minimize overhead:

    1. Add early bailout after shouldTrace check:
       ```typescript
       if (!tracer.isEnabled()) {
         return; // Skip attribute construction for NoOp
       }
       ```

    2. Extract attribute building to separate function:
       - Create `buildAttributes(ctx, opts)` function
       - Move all attribute object construction into this function
       - Only call it after isEnabled() check passes

    3. Inline shouldTrace function (if it's a simple check):
       - Replace function call with direct condition check
       - This reduces call overhead on hot path

    4. Pre-compute span name prefix:
       - Store "resolve:" as constant
       - Use template literal: `${RESOLVE_PREFIX}${ctx.portName}`

    5. Optimize conditional attribute spreading:
       - Build attributes conditionally without multiple spread operations
       - Use direct property assignment where possible

    Important: Preserve all existing functionality and error handling.

  </action>
  <verify>pnpm --filter @hex-di/tracing test -- hooks</verify>
  <done>Hooks optimized with early bailout, reduced function calls, and efficient attribute building</done>
</task>

<task type="auto">
  <name>Task 3: Verify performance improvement with benchmarks</name>
  <files>
    packages/tracing/tests/benchmarks/noop-overhead.bench.ts
  </files>
  <action>
    Run benchmarks to verify the performance improvements:

    1. Run the baseline benchmark before changes (if not already captured)
    2. Run the benchmark after optimizations: `pnpm --filter @hex-di/tracing test:bench -- noop-overhead`
    3. Verify overhead reduction from ~34% toward <20% target
    4. Add comment to benchmark file documenting the optimization results

    Expected improvements:
    - Early bailout should reduce ~20% overhead (skip attribute construction)
    - Inline optimizations should reduce ~10% overhead (fewer function calls)
    - Combined effect should bring overhead to ~17-20% range

    Note: The research indicates <10% may not be achievable without runtime changes,
    so <20% is the realistic target while maintaining hook machinery.

  </action>
  <verify>pnpm --filter @hex-di/tracing test:bench -- noop-overhead</verify>
  <done>NoOp tracer overhead reduced to <20% as verified by benchmarks</done>
</task>

</tasks>

<verification>
- All tracer implementations have isEnabled() method
- Hooks use early bailout to skip attribute construction for NoOp
- Benchmarks show overhead reduction from ~34% to <20%
- All existing tests pass
- Type checking passes
</verification>

<success_criteria>

- NoOp tracer overhead reduced to <20% (from ~34%)
- No breaking changes to public API (only additions)
- All tests continue to pass
- Type safety maintained throughout
  </success_criteria>

<output>
After completion, create `.planning/phases/31-tracing-performance/31-01-SUMMARY.md`
</output>
