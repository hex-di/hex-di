---
phase: 31-tracing-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tracing/src/instrumentation/container.ts
  - packages/tracing/src/instrumentation/tree.ts
  - packages/tracing/tests/integration/tree-instrumentation.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "NoOp tracer overhead reduced to less than 5% when hooks are not registered"
    - "Hooks are not registered when tracer.isEnabled() returns false"
    - "Containers with NoOp tracer have no tracing hooks installed"
  artifacts:
    - path: "packages/tracing/src/instrumentation/container.ts"
      provides: "Conditional hook registration based on tracer state"
      min_lines: 150
    - path: "packages/tracing/src/instrumentation/tree.ts"
      provides: "Smart tree instrumentation that checks tracer.isEnabled()"
      min_lines: 300
  key_links:
    - from: "packages/tracing/src/instrumentation/container.ts"
      to: "tracer.isEnabled()"
      via: "Check before registering hooks"
      pattern: "tracer\\.isEnabled\\(\\)"
    - from: "packages/tracing/src/instrumentation/tree.ts"
      to: "tracer.isEnabled()"
      via: "Check before instrumenting tree"
      pattern: "tracer\\.isEnabled\\(\\)"
---

<objective>
Implement conditional hook registration to eliminate NoOp tracer overhead

Purpose: Reduce NoOp tracer overhead from 37% to <5% by skipping hook registration entirely when tracer is disabled
Output: Instrumentation that returns early without registering hooks for NoOp tracer
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-tracing-performance/31-VERIFICATION.md
@.planning/phases/31-tracing-performance/31-01-SUMMARY.md
@.planning/phases/31-tracing-performance/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify instrumentContainer to skip hooks for NoOp tracer</name>
  <files>packages/tracing/src/instrumentation/container.ts</files>
  <action>
    Update instrumentContainer to check tracer state before registering hooks:
    - Check `tracer.isEnabled()` at the beginning of the function
    - If false (NoOp tracer), return immediately with a no-op cleanup function
    - This prevents ANY hooks from being registered with the container
    - Add comment explaining that NoOp tracer has zero overhead (no hooks = no invocations)

    The approach is simple: when tracer.isEnabled() returns false, don't register hooks at all. The container will have no tracing hooks and therefore zero overhead.

  </action>
  <verify>pnpm --filter @hex-di/tracing test -- container</verify>
  <done>instrumentContainer returns early with no-op cleanup when tracer.isEnabled() is false</done>
</task>

<task type="auto">
  <name>Task 2: Modify instrumentContainerTree to skip hooks for NoOp tracer</name>
  <files>packages/tracing/src/instrumentation/tree.ts</files>
  <action>
    Update instrumentContainerTree to check tracer state before instrumenting:
    - Check `tracer.isEnabled()` at the beginning of the function
    - If false (NoOp tracer), return immediately with a no-op cleanup function
    - This prevents instrumentContainer from being called at all for the tree
    - Ensure child containers also skip instrumentation when parent has NoOp tracer

    This ensures entire container trees with NoOp tracer have zero hook overhead.

  </action>
  <verify>pnpm --filter @hex-di/tracing test -- tree</verify>
  <done>instrumentContainerTree returns early with no-op cleanup when tracer.isEnabled() is false</done>
</task>

<task type="auto">
  <name>Task 3: Add integration test for zero-overhead NoOp tracer</name>
  <files>packages/tracing/tests/integration/tree-instrumentation.test.ts</files>
  <action>
    Add test cases that verify NoOp tracer has near-zero overhead:
    - Create container with NoOp tracer via instrumentContainer
    - Verify that the cleanup function is returned (no-op cleanup)
    - Call instrumentContainer with NoOp tracer and verify container resolves with no hook overhead
    - Use inspector API to verify no beforeResolve/afterResolve hooks are registered
    - Test instrumentContainerTree with NoOp tracer returns early without walking the tree
    - Test that child containers also have no hooks when parent uses NoOp tracer

    This test ensures hooks are completely skipped, not just disabled. Verify by resolving ports
    and checking that no span-related side effects occur (no spans collected, no callbacks invoked).

  </action>
  <verify>pnpm --filter @hex-di/tracing test -- tree-instrumentation</verify>
  <done>Integration test confirms NoOp tracer results in NO hooks being registered</done>
</task>

</tasks>

<verification>
Run benchmarks to verify NoOp overhead elimination:
```bash
pnpm --filter @hex-di/tracing bench
```

Expected: NoOp tracer overhead should drop from 37% to <5%
</verification>

<success_criteria>

- NoOp tracer overhead reduced to <5% (from 37%)
- instrumentContainer returns early when tracer.isEnabled() is false
- instrumentContainerTree returns early when tracer.isEnabled() is false
- Containers with NoOp tracer have NO hooks registered
- All existing tests pass
- Benchmark confirms zero overhead
  </success_criteria>

<output>
After completion, create `.planning/phases/31-tracing-performance/31-03-SUMMARY.md`
</output>
