---
phase: 31-tracing-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/container/factory.ts
  - packages/runtime/src/container/root-impl.ts
  - packages/runtime/src/container/child-impl.ts
  - packages/runtime/src/resolution/hooks-runner.ts
  - packages/tracing/src/instrumentation/tree.ts
  - packages/tracing/tests/integration/tree-instrumentation.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "NoOp tracer overhead reduced to near-zero when disabled"
    - "Hooks are not registered when tracer.isEnabled() returns false"
    - "Dynamic hook registration/deregistration works correctly"
  artifacts:
    - path: "packages/runtime/src/resolution/hooks-runner.ts"
      provides: "Conditional hook execution based on enabled state"
      min_lines: 250
    - path: "packages/tracing/src/instrumentation/tree.ts"
      provides: "Smart hook registration that checks tracer.isEnabled()"
      min_lines: 300
  key_links:
    - from: "packages/tracing/src/instrumentation/tree.ts"
      to: "tracer.isEnabled()"
      via: "Check before registering hooks"
      pattern: "tracer\\.isEnabled\\(\\)"
    - from: "packages/runtime/src/resolution/hooks-runner.ts"
      to: "hook enabled check"
      via: "Skip hook invocation when disabled"
      pattern: "if.*enabled"
---

<objective>
Implement conditional hook registration to eliminate NoOp tracer overhead

Purpose: Reduce NoOp tracer overhead from 37% to near-zero by only registering hooks when tracer is actually enabled
Output: Runtime that skips hook machinery entirely when tracing is disabled
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-tracing-performance/31-VERIFICATION.md
@.planning/phases/31-tracing-performance/31-01-SUMMARY.md
@.planning/phases/31-tracing-performance/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dynamic hook management to HooksRunner</name>
  <files>packages/runtime/src/resolution/hooks-runner.ts</files>
  <action>
    Modify HooksRunner to support dynamic enabling/disabling of hooks:
    - Add private `enabled` flag (default true for backward compatibility)
    - Add public `setEnabled(enabled: boolean)` method to control hook execution
    - Modify `runSync()` and `runAsync()` to check enabled flag before hook invocation
    - When disabled, skip both beforeResolve and afterResolve calls entirely
    - Ensure context object creation is also skipped when disabled for maximum performance

    Key optimization: When enabled is false, the resolution should bypass all hook-related code paths including context object allocation.

  </action>
  <verify>pnpm --filter @hex-di/runtime test</verify>
  <done>HooksRunner has setEnabled() method that completely bypasses hook machinery when disabled</done>
</task>

<task type="auto">
  <name>Task 2: Implement smart hook registration in tree instrumentation</name>
  <files>packages/tracing/src/instrumentation/tree.ts</files>
  <action>
    Modify instrumentContainer to conditionally register hooks based on tracer state:
    - Check `tracer.isEnabled()` before creating hooks
    - If NoOp tracer (isEnabled() returns false), pass null hooks to container options
    - Store reference to HooksRunner (if available) for potential dynamic control
    - Add comment explaining that NoOp tracer bypasses all hook overhead

    This ensures containers created with NoOp tracer have zero hook overhead from the start.

  </action>
  <verify>pnpm --filter @hex-di/tracing test</verify>
  <done>instrumentContainer skips hook registration entirely for NoOp tracer</done>
</task>

<task type="auto">
  <name>Task 3: Add integration test for zero-overhead NoOp tracer</name>
  <files>packages/tracing/tests/integration/tree-instrumentation.test.ts</files>
  <action>
    Add test case that verifies NoOp tracer has near-zero overhead:
    - Create container with NoOp tracer via instrumentContainer
    - Measure resolution performance vs uninstrumented container
    - Assert overhead is less than 5% (accounting for measurement noise)
    - Add test that verifies HooksRunner is null or disabled for NoOp tracer

    This test ensures the optimization actually eliminates overhead in practice.

  </action>
  <verify>pnpm --filter @hex-di/tracing test -- tree-instrumentation</verify>
  <done>Integration test confirms NoOp tracer overhead is less than 5%</done>
</task>

</tasks>

<verification>
Run benchmarks to verify NoOp overhead reduction:
```bash
pnpm --filter @hex-di/tracing bench
```

Expected: NoOp tracer overhead should drop from 37% to less than 5%
</verification>

<success_criteria>

- NoOp tracer overhead reduced to less than 5% (from 37%)
- HooksRunner can be dynamically enabled/disabled
- Containers with NoOp tracer skip hook registration entirely
- All existing tests pass
- Benchmark confirms performance improvement
  </success_criteria>

<output>
After completion, create `.planning/phases/31-tracing-performance/31-03-SUMMARY.md`
</output>
