---
phase: 12-api-cleanup
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - packages/graph/tests/*.test.ts
  - packages/graph/tests/*.test-d.ts
  - packages/graph/tests/edge-cases/*.test.ts
autonomous: true
must_haves:
  truths:
    - "All graph package tests use provide() instead of deprecated methods"
    - "All graph package tests use withExtendedDepth() instead of withUnsafeDepthOverride()"
    - "Tests pass with updated method names"
  artifacts:
    - path: "packages/graph/tests/**/*.test.ts"
      provides: "Updated test files using new API"
  key_links:
    - from: "test files"
      to: "GraphBuilder.provide()"
      via: "method calls"
---

<objective>
Update all tests in the graph package to use the new API

Purpose: Migrate all test files in the graph package from deprecated methods (provideAsync, provideFirstError, provideUnchecked, mergeWith) to the unified provide() method, and update withUnsafeDepthOverride calls to withExtendedDepth
Output: All graph package tests updated and passing with the new API
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v4.0-ROADMAP.md
@.planning/phases/12-api-cleanup/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace provideAsync with provide in test files</name>
  <files>packages/graph/tests/**/*.test.ts, packages/graph/tests/**/*.test-d.ts</files>
  <action>
    Find and replace all instances of `provideAsync()` with `provide()`:

    Key files to update (based on grep results):
    - packages/graph/tests/async-adapter.test.ts
    - packages/graph/tests/async-adapter-batch-validation.test-d.ts
    - packages/graph/tests/edge-cases/async-adapters.test.ts
    - packages/graph/tests/concurrent.test.ts

    The provide() method now automatically detects async adapters via their return type, so no special method is needed. Simply replace:
    - `.provideAsync(adapter)` → `.provide(adapter)`
    - `builder.provideAsync(` → `builder.provide(`

    Ensure the adapters being provided are properly typed with Promise-returning factories so the async detection works correctly.

  </action>
  <verify>grep -r "provideAsync" packages/graph/tests/ should return 0 matches</verify>
  <done>All provideAsync calls replaced with provide in graph tests</done>
</task>

<task type="auto">
  <name>Task 2: Replace other deprecated methods in test files</name>
  <files>packages/graph/tests/**/*.test.ts, packages/graph/tests/**/*.test-d.ts</files>
  <action>
    Replace usage of other deprecated methods:

    1. Replace `provideFirstError()` with `provide()`:
       - The provide() method now returns all errors by default
       - Simply replace the method name, no other changes needed

    2. Replace `provideUnchecked()` with `provide()`:
       - Files like unchecked-usage-marker.test-d.ts may need special attention
       - If tests were specifically testing unchecked behavior, they may need to be removed or rewritten

    3. Replace `mergeWith()` with `merge()`:
       - The merge() method now always uses max depth automatically
       - Remove the options parameter: `.mergeWith(other, { maxDepth: 'max' })` → `.merge(other)`
       - Files to check: merge-options.test.ts, merge-maxdepth-symmetry.test-d.ts

    For each replacement, ensure the test logic still makes sense with the new API behavior.

  </action>
  <verify>grep -rE "provideFirstError|provideUnchecked|mergeWith" packages/graph/tests/ should return 0 matches</verify>
  <done>All deprecated method calls replaced in graph tests</done>
</task>

<task type="auto">
  <name>Task 3: Rename withUnsafeDepthOverride to withExtendedDepth in tests</name>
  <files>packages/graph/tests/**/*.test.ts, packages/graph/tests/**/*.test-d.ts</files>
  <action>
    Update all occurrences of `withUnsafeDepthOverride` to `withExtendedDepth`:

    Key files that likely contain this method (based on naming patterns):
    - packages/graph/tests/depth-*.test-d.ts files
    - packages/graph/tests/merge-unsafe-override-preservation.test-d.ts
    - packages/graph/tests/depth-soundness.test.ts
    - packages/graph/tests/fixtures.ts

    Replace:
    - `GraphBuilder.withUnsafeDepthOverride()` → `GraphBuilder.withExtendedDepth()`
    - `.withUnsafeDepthOverride()` → `.withExtendedDepth()`

    Also update any type references:
    - `UnsafeDepthOverrideFactory` → `ExtendedDepthFactory`

    Update test descriptions and comments that mention "unsafe depth override" to use "extended depth" terminology.

  </action>
  <verify>grep -r "withExtendedDepth" packages/graph/tests/ should return multiple matches && grep -r "withUnsafeDepthOverride" packages/graph/tests/ should return 0 matches</verify>
  <done>All withUnsafeDepthOverride references updated to withExtendedDepth in tests</done>
</task>

</tasks>

<verification>
Run the graph package tests to ensure all updates work correctly:

```bash
# Type check the graph package
pnpm --filter @hex-di/graph typecheck

# Run all graph tests
pnpm --filter @hex-di/graph test

# Run type-level tests
pnpm --filter @hex-di/graph test:types
```

</verification>

<success_criteria>

1. No references to provideAsync, provideFirstError, provideUnchecked, or mergeWith in graph tests
2. All references to withUnsafeDepthOverride replaced with withExtendedDepth
3. All graph package tests pass with the updated API
4. Type checking succeeds for the graph package
   </success_criteria>

<output>
After completion, create `.planning/phases/12-api-cleanup/12-02-SUMMARY.md`
</output>
