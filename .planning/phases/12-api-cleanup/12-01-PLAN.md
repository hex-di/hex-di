---
phase: 12-api-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/graph/src/builder/builder.ts
  - packages/graph/src/builder/types/index.ts
  - packages/graph/src/index.ts
autonomous: true
must_haves:
  truths:
    - "Developer cannot use provideAsync(), provideFirstError(), provideUnchecked(), or mergeWith() methods"
    - "Developer calls withExtendedDepth() instead of withUnsafeDepthOverride()"
    - "TypeScript shows 'not exported' error for removed methods"
  artifacts:
    - path: "packages/graph/src/builder/builder.ts"
      provides: "GraphBuilder class without deprecated methods"
      contains: "withExtendedDepth"
    - path: "packages/graph/src/builder/types/index.ts"
      provides: "Updated type exports"
  key_links:
    - from: "packages/graph/src/builder/builder.ts"
      to: "packages/graph/src/builder/types/index.ts"
      via: "type exports"
---

<objective>
Remove deprecated GraphBuilder methods and rename withUnsafeDepthOverride to withExtendedDepth

Purpose: Clean up the GraphBuilder API surface by removing redundant methods (provideAsync, provideFirstError, provideUnchecked, mergeWith) and improving naming clarity (withUnsafeDepthOverride → withExtendedDepth)
Output: GraphBuilder class with only the essential methods and clearer naming
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v4.0-ROADMAP.md
@.planning/milestones/v4.0-REQUIREMENTS.md
@.planning/phases/12-api-cleanup/12-CONTEXT.md
@.planning/phases/12-api-cleanup/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated methods from GraphBuilder class</name>
  <files>packages/graph/src/builder/builder.ts</files>
  <action>
    Remove the following deprecated methods from the GraphBuilder class:

    1. Remove `provideFirstError()` method (lines 586-592):
       - Delete the method definition and its implementation
       - This method was for first-error-only reporting, now all errors are reported by default

    2. Remove `provideUnchecked()` method (lines 599-605):
       - Delete the method definition and its implementation
       - This escape hatch is no longer needed with improved type system

    3. Remove `provideAsync()` method (lines 612-620):
       - Delete the method definition and its implementation
       - The `provide()` method now auto-detects async via type-level Promise detection

    4. Remove `mergeWith()` method (lines 683-709):
       - Delete the method definition and its implementation
       - The `merge()` method now always uses max depth automatically

    Keep the main `provide()` method as it already detects async adapters via type-level Promise detection.

  </action>
  <verify>grep -c "provideAsync\|provideFirstError\|provideUnchecked\|mergeWith" packages/graph/src/builder/builder.ts should return a lower count than before</verify>
  <done>All four deprecated methods removed from GraphBuilder class</done>
</task>

<task type="auto">
  <name>Task 2: Rename withUnsafeDepthOverride to withExtendedDepth</name>
  <files>packages/graph/src/builder/builder.ts</files>
  <action>
    Rename the `withUnsafeDepthOverride` method to `withExtendedDepth`:

    1. In the `GraphBuilderFactory` interface (line 192):
       - Change method name from `withUnsafeDepthOverride()` to `withExtendedDepth()`
       - Update JSDoc to reflect new name and clearer purpose

    2. In the static method on GraphBuilder class (line 528):
       - Change method name from `static withUnsafeDepthOverride()` to `static withExtendedDepth()`
       - Update JSDoc comment to use "extended depth" terminology instead of "unsafe override"
       - Update the return type name from `UnsafeDepthOverrideFactory` to `ExtendedDepthFactory`

    3. In the factory implementations (lines 504, 532):
       - Update the method name in the factory object returned
       - Change `withUnsafeDepthOverride: () => ...` to `withExtendedDepth: () => ...`

    Also rename the associated type `UnsafeDepthOverrideFactory` to `ExtendedDepthFactory` (around line 219).

  </action>
  <verify>grep -c "withExtendedDepth" packages/graph/src/builder/builder.ts should return at least 3 matches</verify>
  <done>Method renamed from withUnsafeDepthOverride to withExtendedDepth throughout the file</done>
</task>

<task type="auto">
  <name>Task 3: Update type exports and clean up unused types</name>
  <files>packages/graph/src/builder/types/index.ts, packages/graph/src/index.ts</files>
  <action>
    Update type exports to reflect the removed methods:

    1. In packages/graph/src/builder/types/index.ts:
       - Remove exports for `ProvideAsyncResult` (no longer needed)
       - Remove exports for `ProvideUncheckedResult` (no longer needed)
       - Remove exports for `MergeWithResult` (no longer needed)
       - Keep `ProvideResult` and `ProvideResultAllErrors` as they're still used

    2. In packages/graph/src/index.ts:
       - Ensure the GraphBuilder export reflects the updated API
       - Remove any re-exports of deprecated method types
       - Verify `ExtendedDepthFactory` is exported if it was previously exported as `UnsafeDepthOverrideFactory`

    3. Check and update any related type definitions that reference the old method names.

  </action>
  <verify>grep -E "ProvideAsyncResult|ProvideUncheckedResult|MergeWithResult|UnsafeDepthOverride" packages/graph/src/builder/types/index.ts packages/graph/src/index.ts should return 0 matches</verify>
  <done>Type exports updated to remove deprecated method types and reflect new naming</done>
</task>

</tasks>

<verification>
Run tests to ensure the removed methods are no longer accessible and the renamed method works correctly:

```bash
# Verify that the deprecated methods are removed
pnpm --filter @hex-di/graph typecheck

# Run graph package tests to catch any internal usage
pnpm --filter @hex-di/graph test
```

</verification>

<success_criteria>

1. GraphBuilder class no longer exports provideAsync, provideFirstError, provideUnchecked, or mergeWith methods
2. withUnsafeDepthOverride renamed to withExtendedDepth throughout the codebase
3. Type exports updated to remove deprecated method types
4. Tests pass with the updated API
   </success_criteria>

<output>
After completion, create `.planning/phases/12-api-cleanup/12-01-SUMMARY.md`
</output>
