---
phase: 12-api-cleanup
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - packages/runtime/tests/**/*.test.ts
  - packages/react/tests/**/*.test.tsx
  - packages/flow/tests/**/*.test.ts
  - packages/hono/tests/**/*.test.ts
  - packages/testing/tests/**/*.test.ts
autonomous: true
must_haves:
  truths:
    - "All package tests use provide() instead of deprecated methods"
    - "All package tests use withExtendedDepth() instead of withUnsafeDepthOverride()"
    - "Tests across all packages pass with updated API"
  artifacts:
    - path: "packages/*/tests/**/*.test.ts"
      provides: "Updated test files using new GraphBuilder API"
  key_links:
    - from: "package test files"
      to: "GraphBuilder.provide()"
      via: "method calls"
---

<objective>
Update tests in all other packages to use the new GraphBuilder API

Purpose: Migrate test files in runtime, react, flow, hono, and testing packages from deprecated GraphBuilder methods to the new API
Output: All package tests updated and passing with the new GraphBuilder API
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v4.0-ROADMAP.md
@.planning/phases/12-api-cleanup/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update runtime package tests</name>
  <files>packages/runtime/tests/**/*.test.ts</files>
  <action>
    Update all GraphBuilder usage in runtime package tests:

    1. Replace deprecated method calls:
       - `.provideAsync(adapter)` → `.provide(adapter)`
       - `.provideFirstError(adapter)` → `.provide(adapter)`
       - `.provideUnchecked(adapter)` → `.provide(adapter)`
       - `.mergeWith(other, options)` → `.merge(other)`

    2. Rename method calls:
       - `GraphBuilder.withUnsafeDepthOverride()` → `GraphBuilder.withExtendedDepth()`
       - `.withUnsafeDepthOverride()` → `.withExtendedDepth()`

    The runtime package likely uses GraphBuilder for container construction tests, so ensure all builder instances use the updated API.

  </action>
  <verify>grep -rE "provideAsync|provideFirstError|provideUnchecked|mergeWith|withUnsafeDepthOverride" packages/runtime/tests/ should return 0 matches</verify>
  <done>Runtime package tests updated to use new GraphBuilder API</done>
</task>

<task type="auto">
  <name>Task 2: Update React and Flow package tests</name>
  <files>packages/react/tests/**/*.test.tsx, packages/flow/tests/**/*.test.ts</files>
  <action>
    Update GraphBuilder usage in React and Flow packages:

    For React package (packages/react/tests/):
    - Update any GraphBuilder instances in provider tests
    - Replace deprecated methods with provide()
    - Update withUnsafeDepthOverride to withExtendedDepth

    For Flow package (packages/flow/tests/):
    - Update GraphBuilder usage in state machine tests
    - Replace all deprecated method calls
    - Ensure async adapters work correctly with the auto-detection

    Common replacements:
    - `.provideAsync(` → `.provide(`
    - `.mergeWith(` → `.merge(`
    - `withUnsafeDepthOverride` → `withExtendedDepth`

  </action>
  <verify>grep -rE "provideAsync|mergeWith|withUnsafeDepthOverride" packages/react/tests/ packages/flow/tests/ should return 0 matches</verify>
  <done>React and Flow package tests updated to use new GraphBuilder API</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining packages and examples</name>
  <files>packages/hono/tests/**/*.test.ts, packages/testing/tests/**/*.test.ts, examples/**/*.ts</files>
  <action>
    Update GraphBuilder usage in remaining packages and examples:

    1. Hono package (packages/hono/tests/):
       - Update middleware and per-request scope tests
       - Replace deprecated GraphBuilder methods

    2. Testing package (packages/testing/tests/):
       - Update test harness and mock adapter tests
       - Ensure GraphBuilder test utilities work with new API

    3. Examples (examples/react-showcase/, examples/hono-todo/):
       - Based on earlier grep, these files need updating:
         - examples/react-showcase/src/di/app-graph.ts
         - examples/react-showcase/src/di/root-graph.ts
         - examples/react-showcase/src/plugins/types.ts
       - Replace deprecated methods in example code
       - Ensure examples demonstrate best practices with new API

    Replace all instances of deprecated methods and rename withUnsafeDepthOverride references.

  </action>
  <verify>grep -rE "provideAsync|provideFirstError|provideUnchecked|mergeWith|withUnsafeDepthOverride" packages/hono/tests/ packages/testing/tests/ examples/ should return 0 matches</verify>
  <done>All remaining packages and examples updated to use new GraphBuilder API</done>
</task>

</tasks>

<verification>
Run tests across all packages to ensure the API changes work correctly:

```bash
# Run all package tests
pnpm test

# Type check all packages
pnpm typecheck

# Verify examples still build
pnpm --filter "./examples/*" build
```

</verification>

<success_criteria>

1. No references to deprecated GraphBuilder methods in any package tests
2. All references to withUnsafeDepthOverride replaced with withExtendedDepth
3. All package tests pass (1634+ tests)
4. Type checking succeeds across all packages
5. Examples build successfully with the new API
   </success_criteria>

<output>
After completion, create `.planning/phases/12-api-cleanup/12-03-SUMMARY.md`
</output>
