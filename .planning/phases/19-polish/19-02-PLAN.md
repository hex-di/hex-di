---
phase: 19-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/types/validation-errors.ts
  - packages/runtime/tests/type-level-error-examples.test.ts
autonomous: true

must_haves:
  truths:
    - "Template literal error types include code examples"
    - "Error messages show how to fix the issue"
    - "Examples are specific to each error type"
  artifacts:
    - path: "packages/runtime/src/types/validation-errors.ts"
      provides: "Enhanced template literal error types with examples"
      contains: "Example:"
    - path: "packages/runtime/tests/type-level-error-examples.test.ts"
      provides: "Tests for type-level error messages"
      min_lines: 50
  key_links:
    - from: "packages/runtime/src/types/validation-errors.ts"
      to: "template literal types"
      via: "ERROR[TYPE-XX] pattern"
      pattern: "ERROR\\[TYPE-"
---

<objective>
Enhance compile-time template literal error types with copy-paste-ready code examples.

Purpose: Provide immediate, actionable guidance when type validation fails at compile time.
Output: Enhanced template literal error types with embedded code examples and tests.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-polish/19-RESEARCH.md

# Current validation error types

@packages/runtime/src/types/validation-errors.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance template literal error types with examples</name>
  <files>packages/runtime/src/types/validation-errors.ts</files>
  <action>
    Add code examples to each template literal error type:

    1. For PortNotInGraphError:
       - Add "Fix:" section explaining how to add the adapter
       - Include "Example:" section with GraphBuilder code
       - Show adding the missing adapter to the graph

    2. For MissingDependenciesError:
       - Add "Fix:" section about ensuring dependencies exist
       - Include "Example:" showing how to add missing adapters
       - Demonstrate proper dependency order in GraphBuilder

    3. For any other ERROR[TYPE-XX] patterns found:
       - Add appropriate "Fix:" and "Example:" sections
       - Ensure examples use actual port/adapter names from type params
       - Make examples copy-paste ready

    Format for each error:
    ```
    ERROR[TYPE-XX]: {error description}

    {Current state/what's wrong}

    Fix: {Brief explanation of solution}

    Example:
      {Copy-paste ready code showing the fix}
    ```

    Use template literal type parameters to make examples specific.
    Keep examples concise but complete enough to be useful.

  </action>
  <verify>grep -c "Example:" packages/runtime/src/types/validation-errors.ts returns at least 2</verify>
  <done>Template literal error types enhanced with code examples</done>
</task>

<task type="auto">
  <name>Task 2: Create tests for type-level error examples</name>
  <files>packages/runtime/tests/type-level-error-examples.test.ts</files>
  <action>
    Create tests that verify the enhanced error messages work correctly:

    1. Test PortNotInGraphError example:
       - Use @ts-expect-error to trigger the error
       - Comment showing the expected error message with example
       - Verify the error includes the code example

    2. Test MissingDependenciesError example:
       - Trigger error with missing dependencies
       - Comment showing expected error with example
       - Verify the fix example is present

    3. Test that examples use actual type parameters:
       - Create scenarios with specific port names
       - Verify error examples include those specific names
       - Not just generic placeholders

    Use expectTypeOf or similar type testing utilities if available.
    Include comments explaining what each test verifies.

    Note: These are compile-time tests, so we're mainly documenting
    the expected behavior and using @ts-expect-error to verify errors occur.

  </action>
  <verify>File exists at packages/runtime/tests/type-level-error-examples.test.ts</verify>
  <done>Type-level error example tests created</done>
</task>

</tasks>

<verification>
Check that enhanced error messages appear correctly:
- Trigger a type error intentionally
- Verify the error message includes the code example
- Run type checking: `pnpm --filter @hex-di/runtime typecheck`
</verification>

<success_criteria>

- Template literal errors include "Fix:" and "Example:" sections
- Examples use actual type parameter values (not placeholders)
- Code examples are syntactically correct and copy-paste ready
- Type tests document expected error messages
  </success_criteria>

<output>
After completion, create `.planning/phases/19-polish/19-02-SUMMARY.md`
</output>
