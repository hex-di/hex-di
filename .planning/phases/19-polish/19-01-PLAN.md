---
phase: 19-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/runtime/src/errors/index.ts
  - packages/runtime/src/util/string-similarity.ts
  - packages/runtime/tests/error-suggestions.test.ts
autonomous: true

must_haves:
  truths:
    - "Programming errors show actionable fix suggestions"
    - "Error messages include copy-paste-ready code examples"
    - "Misspelled port names trigger 'Did you mean X?' suggestions"
  artifacts:
    - path: "packages/runtime/src/errors/index.ts"
      provides: "ContainerError subclasses with suggestion property"
      min_lines: 200
    - path: "packages/runtime/src/util/string-similarity.ts"
      provides: "Levenshtein distance calculation"
      exports: ["levenshteinDistance", "suggestSimilarPort"]
    - path: "packages/runtime/tests/error-suggestions.test.ts"
      provides: "Tests for error suggestions and code examples"
      min_lines: 100
  key_links:
    - from: "packages/runtime/src/errors/index.ts"
      to: "packages/runtime/src/util/string-similarity.ts"
      via: "import for port name suggestions"
      pattern: "import.*string-similarity"
---

<objective>
Enhance runtime errors with actionable suggestions and "did you mean" recommendations.

Purpose: Help developers quickly fix issues by providing specific guidance directly in error messages.
Output: Enhanced ContainerError classes with suggestion properties, Levenshtein distance utility, and comprehensive tests.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-polish/19-RESEARCH.md

# Key error class patterns from research

@packages/runtime/src/errors/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create string similarity utility</name>
  <files>packages/runtime/src/util/string-similarity.ts</files>
  <action>
    Create a utility module for string similarity calculations:

    1. Implement `levenshteinDistance(a: string, b: string): number`
       - Use dynamic programming matrix approach
       - Handle edge cases (empty strings)
       - Add comprehensive JSDoc with examples

    2. Implement `suggestSimilarPort(attemptedName: string, availablePorts: string[]): string | undefined`
       - Use MAX_DISTANCE = 2 (maximum 2 character edits)
       - Return closest match if distance <= 2
       - Return undefined if no good match found
       - Add JSDoc explaining the threshold logic

    The implementation should be self-contained (no external dependencies) and well-documented.
    Target ~30 lines of implementation code plus documentation.

  </action>
  <verify>File exists at packages/runtime/src/util/string-similarity.ts with both functions exported</verify>
  <done>String similarity utility created with levenshteinDistance and suggestSimilarPort functions</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ContainerError subclasses with suggestions</name>
  <files>packages/runtime/src/errors/index.ts</files>
  <action>
    Add `suggestion` property to programming errors (isProgrammingError: true):

    1. Add optional `suggestion?: string` property to ContainerError abstract class

    2. Enhance these specific error classes with actionable suggestions:
       - ScopeRequiredError: Include code example for creating scope
       - CircularDependencyError: Show how to refactor to break cycle
       - DisposedScopeError: Explain scope lifecycle and how to check disposal
       - AlreadyInitializedError: Show proper initialization pattern
       - NotInitializedError: Include initialization code example

    3. For PortNotFoundError (if it exists or similar):
       - Import suggestSimilarPort from string-similarity
       - When port not found, check for similar port names
       - Include "Did you mean X?" in error message if match found

    Each suggestion should include:
    - Brief explanation of the fix
    - Copy-paste-ready code example
    - Reference to relevant API method

    Do NOT add suggestions to runtime errors (isProgrammingError: false) like:
    - FactoryError
    - AsyncFactoryError
    These are user code failures where we can't provide specific fixes.

  </action>
  <verify>grep -c "suggestion" packages/runtime/src/errors/index.ts returns at least 5</verify>
  <done>ContainerError subclasses enhanced with actionable suggestions and code examples</done>
</task>

<task type="auto">
  <name>Task 3: Create tests for error suggestions</name>
  <files>packages/runtime/tests/error-suggestions.test.ts</files>
  <action>
    Create comprehensive tests for the enhanced error system:

    1. Test Levenshtein distance calculations:
       - Exact matches (distance 0)
       - Single character differences (distance 1)
       - Multiple differences (distance 2+)
       - Edge cases (empty strings)

    2. Test suggestSimilarPort function:
       - Returns closest match when distance <= 2
       - Returns undefined when no close match
       - Handles multiple similar matches (picks closest)

    3. Test error suggestions for each enhanced error:
       - ScopeRequiredError includes scope creation example
       - CircularDependencyError includes refactoring guidance
       - DisposedScopeError includes lifecycle explanation
       - AlreadyInitializedError includes proper pattern
       - NotInitializedError includes init example

    4. Test "Did you mean?" functionality:
       - Mock a port resolution scenario with typo
       - Verify suggestion appears in error message
       - Test threshold (distance > 2 = no suggestion)

    Use describe/it blocks for clear organization.
    Verify both the presence and content of suggestions.

  </action>
  <verify>pnpm --filter @hex-di/runtime test error-suggestions passes all tests</verify>
  <done>Error suggestion tests created and passing</done>
</task>

</tasks>

<verification>
Run tests to ensure error suggestions work correctly:
- `pnpm --filter @hex-di/runtime test error-suggestions`
- Manually trigger an error to see enhanced message
</verification>

<success_criteria>

- Programming errors include actionable `suggestion` properties
- Code examples are copy-paste ready
- "Did you mean?" suggestions appear for similar port names
- All error suggestion tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/19-polish/19-01-SUMMARY.md`
</output>
