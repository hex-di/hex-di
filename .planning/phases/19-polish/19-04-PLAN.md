---
phase: 19-polish
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/runtime/src/types/container.ts
  - packages/runtime/src/types/scope.ts
  - packages/runtime/src/types/options.ts
  - packages/runtime/src/container/base-impl.ts
autonomous: true

must_haves:
  truths:
    - "Type parameters have @typeParam JSDoc documentation"
    - "IDE shows helpful descriptions on hover"
    - "Complex types are explained clearly"
  artifacts:
    - path: "packages/runtime/src/types/container.ts"
      provides: "@typeParam documentation for Container type"
      contains: "@typeParam"
    - path: "packages/runtime/src/types/scope.ts"
      provides: "@typeParam documentation for Scope type"
      contains: "@typeParam"
    - path: "packages/runtime/src/types/options.ts"
      provides: "JSDoc for configuration types"
      contains: "@remarks"
  key_links:
    - from: "packages/runtime/src/types/container.ts"
      to: "JSDoc annotations"
      via: "@typeParam tags"
      pattern: "@typeParam T"
---

<objective>
Add comprehensive @typeParam JSDoc documentation to improve IDE hover information.

Purpose: Help developers understand complex type parameters directly in their IDE without reading source code.
Output: Enhanced JSDoc annotations with @typeParam tags throughout the runtime types.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-polish/19-RESEARCH.md

# Current type definitions needing documentation

@packages/runtime/src/types/container.ts
@packages/runtime/src/types/scope.ts
@packages/runtime/src/types/options.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Container and Scope type parameters</name>
  <files>packages/runtime/src/types/container.ts, packages/runtime/src/types/scope.ts</files>
  <action>
    Add @typeParam JSDoc to Container and Scope types:

    1. For Container type parameters:
       - @typeParam TProvides - Union of Port types available from the container's graph
       - @typeParam TExtends - Union of Port types added via .extend() (never for root)
       - @typeParam TAsyncPorts - Union of Port types with async factories
       - @typeParam TPhase - Container initialization phase ('uninitialized' | 'initialized')

    2. For Scope type parameters:
       - @typeParam TProvides - Union of Port types inherited from parent container
       - @typeParam TExtends - Union of Port types extended in parent (propagated to scope)
       - @typeParam TAsyncPorts - Union of async ports from parent
       - Document scope-specific behavior differences

    3. Add @remarks sections explaining:
       - How the brand property provides nominal typing
       - Resolution behavior based on phase
       - Relationship between TProvides and TExtends
       - Why certain operations require specific phases

    4. Add @example sections showing:
       - Basic usage patterns
       - Type parameter flow
       - Common scenarios

    Ensure all complex type aliases have JSDoc, not just the main types.
    Focus on explaining "why" not just "what" for each parameter.

  </action>
  <verify>grep -c "@typeParam" packages/runtime/src/types/container.ts returns at least 4</verify>
  <done>Container and Scope types documented with @typeParam annotations</done>
</task>

<task type="auto">
  <name>Task 2: Document configuration and option types</name>
  <files>packages/runtime/src/types/options.ts, packages/runtime/src/container/base-impl.ts</files>
  <action>
    Add comprehensive JSDoc to configuration types:

    1. For CreateContainerOptions:
       - Document each field's purpose and impact
       - Add @default annotations for optional fields
       - Include @example showing different configurations

    2. For ContainerHooks:
       - Explain hook execution order (FIFO vs LIFO)
       - Document when hooks fire in resolution lifecycle
       - Add @example showing hook registration

    3. For Performance options:
       - Explain captureTimestamps impact
       - Document when to disable for production
       - Show performance implications

    4. For ContainerPhase type:
       - Explain state transitions
       - Document what operations are available in each phase
       - Add @see references to initialization methods

    5. In base-impl.ts, enhance class-level documentation:
       - Add @internal markers for implementation details
       - Document protected methods that subclasses might override
       - Explain the relationship between impl and public interface

    Use @remarks for additional context and gotchas.
    Include @see references to related types and methods.

  </action>
  <verify>grep -c "@example\|@default\|@remarks" packages/runtime/src/types/options.ts returns at least 5</verify>
  <done>Configuration and option types fully documented with JSDoc</done>
</task>

</tasks>

<verification>
Test IDE hover information:
- Open a TypeScript file that uses Container
- Hover over Container type to see enhanced JSDoc
- Verify @typeParam descriptions appear
- Check that examples are formatted correctly
</verification>

<success_criteria>

- All Container type parameters have @typeParam documentation
- All Scope type parameters have @typeParam documentation
- Configuration types have @default and @example annotations
- JSDoc explains "why" not just "what" for complex types
- IDE hover shows helpful parameter descriptions
  </success_criteria>

<output>
After completion, create `.planning/phases/19-polish/19-04-SUMMARY.md`
</output>
