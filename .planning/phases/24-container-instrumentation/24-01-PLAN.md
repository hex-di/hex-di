---
phase: 24-container-instrumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tracing/src/instrumentation/span-stack.ts
  - packages/tracing/src/instrumentation/container.ts
  - packages/tracing/src/instrumentation/types.ts
autonomous: true

must_haves:
  truths:
    - "Single container can be instrumented with tracing hooks"
    - "Resolution spans have parent-child relationships"
    - "Span stack maintains active span context"
    - "Double instrumentation is handled gracefully"
  artifacts:
    - path: "packages/tracing/src/instrumentation/span-stack.ts"
      provides: "Module-level span stack management"
      min_lines: 30
      exports: ["pushSpan", "popSpan", "getActiveSpan", "clearStack"]
    - path: "packages/tracing/src/instrumentation/container.ts"
      provides: "Container instrumentation function"
      min_lines: 100
      exports: ["instrumentContainer"]
    - path: "packages/tracing/src/instrumentation/types.ts"
      provides: "Instrumentation option types"
      min_lines: 50
      exports: ["AutoInstrumentOptions"]
  key_links:
    - from: "container.ts"
      to: "span-stack.ts"
      via: "imports and uses span stack"
      pattern: "import.*span-stack"
    - from: "container.ts"
      to: "@hex-di/runtime hooks"
      via: "installs resolution hooks"
      pattern: "addHook.*beforeResolve|afterResolve"
---

<objective>
Implement core container instrumentation with span stack management for distributed tracing.

Purpose: Enable automatic span creation for every dependency resolution with proper parent-child relationships via a module-level span stack.
Output: Working instrumentContainer function that installs tracing hooks and manages span context propagation.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 23 established the tracing package foundation

@.planning/phases/23-core-tracing-foundation/23-08-SUMMARY.md

# Existing tracing package structure

@packages/tracing/src/index.ts
@packages/tracing/src/ports/tracer.ts
@packages/tracing/src/ports/span.ts

# Runtime hooks system we'll integrate with

@packages/runtime/src/resolution/hooks.ts
@packages/runtime/src/types/container.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create span stack management module</name>
  <files>packages/tracing/src/instrumentation/span-stack.ts</files>
  <action>
    Create a module-level span stack for context propagation across container boundaries.

    Implementation requirements:
    - Module-scoped array to track active spans (not exported)
    - `pushSpan(span: Span): void` - adds span to stack
    - `popSpan(): Span | undefined` - removes and returns top span
    - `getActiveSpan(): Span | undefined` - returns current span without removing
    - `clearStack(): void` - resets stack (useful for tests)
    - `getStackDepth(): number` - returns current stack size for debugging

    Use proper type imports from @hex-di/tracing ports. No AsyncLocalStorage - this is a simple module-level array that works in all environments.

  </action>
  <verify>pnpm --filter @hex-di/tracing typecheck</verify>
  <done>span-stack.ts exports all required functions with proper types</done>
</task>

<task type="auto">
  <name>Task 2: Create instrumentation types</name>
  <files>packages/tracing/src/instrumentation/types.ts</files>
  <action>
    Define types for container instrumentation options and filters.

    Create AutoInstrumentOptions interface with:
    - `traceSyncResolutions?: boolean` (default: true per Claude's discretion)
    - `traceAsyncResolutions?: boolean` (default: true per Claude's discretion)
    - `traceCachedResolutions?: boolean` (default: true per user decision)
    - `portFilter?: PortFilter` - union type
    - `additionalAttributes?: Record<string, unknown>` (flat object per Claude's discretion)
    - `minDurationMs?: number` (default: 0 - no filtering)
    - `includeStackTrace?: boolean` (default: false per user decision)

    Define PortFilter as union:
    - Predicate: `(portName: string) => boolean`
    - Declarative: `{ include?: string[], exclude?: string[] }`

    Export helper type guards and default options constant.

  </action>
  <verify>pnpm --filter @hex-di/tracing typecheck</verify>
  <done>types.ts defines AutoInstrumentOptions with all fields from INST-08</done>
</task>

<task type="auto">
  <name>Task 3: Implement container instrumentation</name>
  <files>packages/tracing/src/instrumentation/container.ts</files>
  <action>
    Create instrumentContainer function that installs resolution hooks on a single container.

    Implementation requirements (INST-01, INST-04, INST-05, INST-06):

    1. Use WeakMap to track installed cleanups for double-instrumentation handling
    2. Create beforeResolve hook that:
       - Checks if resolution should be traced (portFilter, cache settings)
       - Gets parent span from stack via getActiveSpan()
       - Creates child span with name `resolve:${portName}` (concise format per Claude's discretion)
       - Sets attributes per INST-06: hex-di.port.name, hex-di.port.lifetime, hex-di.resolution.cached, hex-di.container.name
       - Adds any additionalAttributes from options
       - Pushes span to stack

    3. Create afterResolve hook that:
       - Pops span from stack
       - If error: records exception event with status=ERROR
       - If success: sets status=OK
       - Checks duration against minDurationMs (filter at span-end per Claude's discretion)
       - Ends span in finally block

    4. Handle double-instrumentation per user decision:
       - Check WeakMap for existing cleanup
       - If exists, call it first (auto-cleanup old hooks)
       - Install new hooks and store new cleanup

    5. Return idempotent cleanup function (can be called multiple times safely)

    Import types from @hex-di/runtime for ResolutionHookContext and ResolutionResultContext.
    Use try/finally to ensure span cleanup even on errors.

  </action>
  <verify>pnpm --filter @hex-di/tracing test:types</verify>
  <done>instrumentContainer installs hooks, manages span stack, returns cleanup function</done>
</task>

</tasks>

<verification>
- [ ] Span stack module provides push/pop/getActive functions
- [ ] AutoInstrumentOptions type includes all fields from INST-08
- [ ] instrumentContainer installs beforeResolve/afterResolve hooks
- [ ] Hooks create spans with proper parent-child relationships via stack
- [ ] Double-instrumentation handled via WeakMap tracking
- [ ] Cleanup function removes hooks when called
- [ ] All attributes from INST-06 are set on spans
</verification>

<success_criteria>

- Module compiles without TypeScript errors
- Span stack maintains proper LIFO ordering
- Container instrumentation creates spans for resolutions
- Parent-child relationships maintained via stack
- Requirements INST-01, INST-03, INST-04, INST-05, INST-06 satisfied
  </success_criteria>

<output>
After completion, create `.planning/phases/24-container-instrumentation/24-01-SUMMARY.md`
</output>
