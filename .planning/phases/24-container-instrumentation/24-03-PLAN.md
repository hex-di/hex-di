---
phase: 24-container-instrumentation
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - packages/tracing/src/instrumentation/hooks.ts
  - packages/tracing/src/instrumentation/index.ts
  - packages/tracing/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Standalone tracing hooks can be created for manual registration"
    - "All instrumentation functions are exported from package"
    - "Options have sensible defaults"
  artifacts:
    - path: "packages/tracing/src/instrumentation/hooks.ts"
      provides: "Standalone hook factory"
      min_lines: 80
      exports: ["createTracingHook"]
    - path: "packages/tracing/src/instrumentation/index.ts"
      provides: "Instrumentation module exports"
      min_lines: 15
      exports:
        [
          "instrumentContainer",
          "instrumentContainerTree",
          "createTracingHook",
          "AutoInstrumentOptions",
        ]
    - path: "packages/tracing/src/index.ts"
      provides: "Updated package exports"
      contains: "export.*from.*instrumentation"
  key_links:
    - from: "hooks.ts"
      to: "span-stack.ts"
      via: "uses span stack for context"
      pattern: "import.*span-stack"
    - from: "index.ts"
      to: "instrumentation/index.ts"
      via: "re-exports instrumentation"
      pattern: "export.*from.*instrumentation"
---

<objective>
Create standalone hook factory for manual registration and export all instrumentation functions.

Purpose: Provide flexibility for users who want to manually register hooks while also making all instrumentation capabilities easily accessible from the package root.
Output: Complete instrumentation module with createTracingHook factory and proper exports.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Core instrumentation from previous plan

@.planning/phases/24-container-instrumentation/24-01-SUMMARY.md

# Existing package exports

@packages/tracing/src/index.ts

# Runtime hook types

@packages/runtime/src/resolution/hooks.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create standalone hook factory</name>
  <files>packages/tracing/src/instrumentation/hooks.ts</files>
  <action>
    Implement createTracingHook for manual hook registration (INST-07).

    Create a factory function that returns ResolutionHooks object:

    ```typescript
    export function createTracingHook(
      tracer: Tracer,
      options: AutoInstrumentOptions = {}
    ): ResolutionHooks
    ```

    Implementation:
    1. Merge options with defaults (use same defaults as instrumentContainer)
    2. Return object with beforeResolve and afterResolve functions
    3. beforeResolve:
       - Check filtering (shouldTracePort, cache settings)
       - Get parent span from stack
       - Create child span with attributes
       - Push to stack
    4. afterResolve:
       - Pop span from stack
       - Handle error/success
       - Check minDurationMs
       - End span

    This is essentially the same logic as instrumentContainer but returns the hooks object directly instead of installing them. Users can then manually call container.addHook() with the returned object.

    Import types from @hex-di/runtime and use span stack from ./span-stack.js
    Reuse filtering logic from ./utils.js

  </action>
  <verify>pnpm --filter @hex-di/tracing typecheck</verify>
  <done>createTracingHook returns ResolutionHooks object for manual registration</done>
</task>

<task type="auto">
  <name>Task 2: Create instrumentation module exports</name>
  <files>packages/tracing/src/instrumentation/index.ts</files>
  <action>
    Create barrel export file for all instrumentation functionality.

    Export:
    - `instrumentContainer` from ./container.js
    - `instrumentContainerTree` from ./tree.js
    - `createTracingHook` from ./hooks.js
    - `AutoInstrumentOptions` and `PortFilter` types from ./types.js
    - Do NOT export internal utilities or span stack (those are implementation details)

    Use clean re-exports with explicit names.

  </action>
  <verify>pnpm --filter @hex-di/tracing build</verify>
  <done>instrumentation/index.ts exports all public APIs</done>
</task>

<task type="auto">
  <name>Task 3: Update package root exports</name>
  <files>packages/tracing/src/index.ts</files>
  <action>
    Add instrumentation exports to the main package entry point.

    Add a new export section:
    ```typescript
    // Instrumentation
    export {
      instrumentContainer,
      instrumentContainerTree,
      createTracingHook,
      type AutoInstrumentOptions,
      type PortFilter,
    } from "./instrumentation/index.js";
    ```

    Keep all existing exports (ports, adapters, context, utils).
    This makes instrumentation available directly from @hex-di/tracing.

  </action>
  <verify>pnpm --filter @hex-di/tracing build && pnpm --filter @hex-di/tracing test:exports</verify>
  <done>Package exports include all instrumentation functions</done>
</task>

</tasks>

<verification>
- [ ] createTracingHook returns ResolutionHooks for manual registration
- [ ] Hook factory uses same logic as instrumentContainer
- [ ] instrumentation/index.ts exports all public APIs
- [ ] Main package index includes instrumentation exports
- [ ] Package builds successfully with new exports
- [ ] All types are properly exported
</verification>

<success_criteria>

- createTracingHook factory enables manual hook registration
- All instrumentation functions accessible from @hex-di/tracing
- Package maintains clean export structure
- Requirement INST-07 satisfied
- Internal implementation details remain private
  </success_criteria>

<output>
After completion, create `.planning/phases/24-container-instrumentation/24-03-SUMMARY.md`
</output>
