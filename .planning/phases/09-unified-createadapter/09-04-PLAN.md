---
phase: 09-unified-createadapter
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - packages/core/tests/unified-adapter.test-d.ts
autonomous: true

must_haves:
  truths:
    - "Type tests verify factory variant compiles correctly"
    - "Type tests verify class variant compiles correctly"
    - "Type tests verify mutual exclusion produces branded errors"
    - "Type tests verify default type inference works"
    - "Type tests verify async factory detection works"
  artifacts:
    - path: "packages/core/tests/unified-adapter.test-d.ts"
      provides: "Compile-time type tests for unified createAdapter"
      min_lines: 100
  key_links:
    - from: "packages/core/tests/unified-adapter.test-d.ts"
      to: "packages/core/src/adapters/unified.ts"
      via: "import createAdapter"
      pattern: "import.*createAdapter.*from"
---

<objective>
Create comprehensive type tests for unified createAdapter API.

Purpose: Validate compile-time behavior including type inference, mutual exclusion errors, async detection, and default values. These tests ensure the type system catches errors before runtime.

Output: Type test file with expectTypeOf assertions covering all API requirements.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-unified-createadapter/09-CONTEXT.md
@packages/core/tests/factory.test-d.ts
@packages/graph/tests/soundness.test-d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create type tests for factory variant</name>
  <files>packages/core/tests/unified-adapter.test-d.ts</files>
  <action>
Create `unified-adapter.test-d.ts` with type tests for factory-based createAdapter:

1. Test fixtures:
   - Define test ports: `LoggerPort`, `DatabasePort`, `UserServicePort`
   - Define test interfaces: `Logger`, `Database`, `UserService`

2. Factory variant tests:
   - **Sync factory with defaults:** Verify adapter has `lifetime: "singleton"`, `factoryKind: "sync"`, `clonable: false`, `requires: readonly []`
   - **Sync factory with explicit requires:** Verify requires tuple type is preserved
   - **Sync factory with explicit lifetime:** Verify lifetime literal is preserved (e.g., `"scoped"`)
   - **Sync factory with explicit clonable:** Verify clonable literal is preserved
   - **Async factory detection:** Verify `factory: async () => ...` produces adapter with `factoryKind: "async"` and `lifetime: "singleton"`
   - **Factory return type validation:** Verify compile error when factory returns wrong type (use `// @ts-expect-error`)

Use `expectTypeOf` from vitest for assertions. Pattern:

```typescript
const adapter = createAdapter({ provides: LoggerPort, factory: () => ({ log: () => {} }) });
expectTypeOf(adapter.lifetime).toEqualTypeOf<"singleton">();
expectTypeOf(adapter.factoryKind).toEqualTypeOf<"sync">();
```

  </action>
  <verify>Run `pnpm test:types` - type tests should pass</verify>
  <done>Factory variant type tests cover defaults, explicit values, and async detection</done>
</task>

<task type="auto">
  <name>Task 2: Create type tests for class variant and mutual exclusion</name>
  <files>packages/core/tests/unified-adapter.test-d.ts</files>
  <action>
Add type tests for class-based createAdapter and mutual exclusion:

1. Class variant tests:
   - **Class with defaults:** Verify adapter has `factoryKind: "sync"`, defaults applied
   - **Class with explicit requires:** Verify requires tuple preserved
   - **Class with explicit lifetime:** Verify all lifetimes work (singleton, scoped, transient)
   - **Class return type validation:** Verify compile error when class doesn't match port type

2. Mutual exclusion tests:
   - **Both factory and class:** Verify return type includes `BothFactoryAndClassError`
     ```typescript
     // @ts-expect-error - should error when both provided
     const invalid = createAdapter({
       provides: LoggerPort,
       factory: () => ({ log: () => {} }),
       class: ConsoleLogger,
     });
     ```
   - **Neither factory nor class:** Verify return type includes `NeitherFactoryNorClassError`
     ```typescript
     // @ts-expect-error - should error when neither provided
     const invalid = createAdapter({ provides: LoggerPort });
     ```

3. Default value inference tests:
   - **requires omitted:** Verify `adapter.requires` is `readonly []`
   - **lifetime omitted:** Verify `adapter.lifetime` is `"singleton"`
   - **clonable omitted:** Verify `adapter.clonable` is `false`
     </action>
     <verify>Run `pnpm test:types` - all type tests pass</verify>
     <done>Class variant and mutual exclusion type tests complete</done>
     </task>

</tasks>

<verification>
- `pnpm test:types` passes in core package
- Type tests cover all requirements from CONTEXT.md
- `@ts-expect-error` comments verify compile-time error cases
- Tests use `expectTypeOf` for positive assertions
</verification>

<success_criteria>

1. Factory variant: defaults, explicit values, async detection all tested
2. Class variant: defaults, explicit values, lifetime options all tested
3. Mutual exclusion: both-provided and neither-provided errors tested
4. All tests pass with `pnpm test:types`
   </success_criteria>

<output>
After completion, create `.planning/phases/09-unified-createadapter/09-04-SUMMARY.md`
</output>
