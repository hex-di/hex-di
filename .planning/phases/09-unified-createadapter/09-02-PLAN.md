---
phase: 09-unified-createadapter
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/core/src/adapters/unified.ts
autonomous: true

must_haves:
  truths:
    - "createAdapter with factory property creates sync adapter"
    - "createAdapter with async factory auto-detects and creates async adapter"
    - "requires defaults to [] when omitted"
    - "lifetime defaults to 'singleton' when omitted"
    - "clonable defaults to false when omitted"
    - "Factory return type is validated against provides port type"
  artifacts:
    - path: "packages/core/src/adapters/unified.ts"
      provides: "Factory-based createAdapter overloads"
      exports: ["createAdapter"]
      min_lines: 150
  key_links:
    - from: "packages/core/src/adapters/unified.ts"
      to: "packages/core/src/adapters/constants.ts"
      via: "import SYNC, ASYNC, SINGLETON, FALSE, EMPTY_REQUIRES"
      pattern: "import.*SYNC.*ASYNC.*from"
---

<objective>
Implement factory-based createAdapter overloads with async auto-detection.

Purpose: Enable users to create adapters with factory functions using a single API entry point. Async factories are automatically detected from return type and forced to singleton lifetime.

Output: Working createAdapter function for factory-based adapters with proper defaults and type inference.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-unified-createadapter/09-CONTEXT.md
@.planning/phases/09-unified-createadapter/09-RESEARCH.md
@packages/core/src/adapters/factory.ts
@packages/core/src/adapters/unified-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add IsAsyncFactory type helper and factory overloads</name>
  <files>packages/core/src/adapters/unified.ts</files>
  <action>
Add async detection helper and factory-based overloads:

1. Add `IsAsyncFactory<TFactory>` type helper:

   ```typescript
   type IsAsyncFactory<TFactory> = TFactory extends (...args: never[]) => Promise<unknown>
     ? true
     : false;
   ```

2. Add factory overloads following the pattern from `factory.ts`:

   **Overload 1:** Factory with all defaults (no requires, no lifetime, no clonable)
   - Config has: `provides`, `factory`, optionally `finalizer`
   - Returns: Adapter with `lifetime: "singleton"`, `clonable: false`, `requires: readonly []`
   - For async factory: factoryKind is "async"
   - For sync factory: factoryKind is "sync"

   **Overload 2:** Factory with explicit requires (no lifetime, no clonable)
   - Config has: `provides`, `requires`, `factory`
   - Returns: Adapter preserving requires tuple type

   **Overload 3:** Factory with explicit lifetime (no clonable)
   - Config has: `provides`, `lifetime`, `factory`, optionally `requires`
   - Note: If async factory, lifetime is forced to "singleton" regardless of input

   **Overload 4:** Factory with explicit clonable
   - Config has: `provides`, `factory`, `clonable`
   - Preserves literal boolean type for clonable

   **Overload 5:** Full explicit (requires + lifetime + clonable)
   - All properties explicitly provided
   - Preserves all literal types

Use `const` modifier on TRequires, TLifetime, TClonable for literal inference.
Key constraint: If `IsAsyncFactory<TFactory>` is true, the returned Adapter's factoryKind must be "async" and lifetime must be "singleton".
</action>
<verify>Run `pnpm typecheck` - overloads should compile. No runtime tests yet.</verify>
<done>Factory overloads defined with proper type inference for async detection and defaults</done>
</task>

<task type="auto">
  <name>Task 2: Add createAdapter implementation for factory variant</name>
  <files>packages/core/src/adapters/unified.ts</files>
  <action>
Add the implementation signature and runtime logic for factory-based createAdapter:

1. Implementation signature that accepts all possible configs:

   ```typescript
   export function createAdapter(config: {
     provides: Port<unknown, string>;
     factory?: Function;
     class?: new (...args: unknown[]) => unknown;
     requires?: readonly Port<unknown, string>[];
     lifetime?: Lifetime;
     clonable?: boolean;
     finalizer?: Function;
   }): Adapter<
     Port<unknown, string>,
     unknown,
     Lifetime,
     FactoryKind,
     boolean,
     readonly Port<unknown, string>[]
   >;
   ```

2. Runtime implementation:
   - Detect async factory by checking if `config.factory` returns a Promise (use try-catch with a dummy call, OR check for `config.factory.constructor.name === 'AsyncFunction'`)
   - Actually, for runtime: just check `factory.constructor.name === 'AsyncFunction'` for async function detection, OR allow both and let runtime handle it by always trying to resolve as Promise-like
   - Better approach: Set factoryKind based on whether the config explicitly has async behavior. Since we can't know at runtime if a function returns Promise without calling it, use SYNC by default and let the caller use the type system for async detection.

   Actually, simplest approach for v1:
   - Always use SYNC factoryKind at runtime
   - The TYPE system handles async detection via return type inference
   - Async adapters work because the graph runtime awaits all factory results anyway

   Apply defaults:
   - `requires` defaults to `EMPTY_REQUIRES`
   - `lifetime` defaults to `SINGLETON`
   - `clonable` defaults to `FALSE`

3. Call existing validation logic (copy `assertValidAdapterConfig` from factory.ts or factor it out)

4. Return frozen adapter object

Note: Class variant will be added in Plan 03. For now, the implementation should handle only the factory case. When `config.class` is provided, throw a runtime error "Class-based createAdapter not yet implemented" (this will be removed in 09-03).
</action>
<verify>Run `pnpm typecheck` - implementation should compile with overloads</verify>
<done>createAdapter implementation handles factory configs with proper defaults and validation</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `createAdapter` is exported from unified.ts
- Factory overloads cover: defaults only, explicit requires, explicit lifetime, explicit clonable, full explicit
- Implementation applies SINGLETON, EMPTY_REQUIRES, FALSE defaults
</verification>

<success_criteria>

1. `createAdapter({ provides: port, factory: () => ... })` compiles and works
2. `createAdapter({ provides: port, factory: async () => ... })` returns Adapter with factoryKind "async"
3. Omitting requires/lifetime/clonable applies correct default literal types
4. Factory return type is validated against InferService<TProvides>
   </success_criteria>

<output>
After completion, create `.planning/phases/09-unified-createadapter/09-02-SUMMARY.md`
</output>
