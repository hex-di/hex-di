---
phase: 09-unified-createadapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/adapters/unified.ts
  - packages/core/src/adapters/unified-types.ts
autonomous: true

must_haves:
  truths:
    - "Config types enforce exactly one of factory or class is provided"
    - "BothFactoryAndClassError appears when both are specified"
    - "NeitherFactoryNorClassError appears when neither is specified"
    - "Factory variant config type-checks with factory property"
    - "Class variant config type-checks with class property"
  artifacts:
    - path: "packages/core/src/adapters/unified-types.ts"
      provides: "Config types and branded error types for unified createAdapter"
      exports:
        - "BaseUnifiedConfig"
        - "FactoryConfig"
        - "ClassConfig"
        - "BothFactoryAndClassError"
        - "NeitherFactoryNorClassError"
    - path: "packages/core/src/adapters/unified.ts"
      provides: "Placeholder file for createAdapter implementation"
      min_lines: 10
  key_links:
    - from: "packages/core/src/adapters/unified-types.ts"
      to: "packages/core/src/ports/types.ts"
      via: "import Port, InferService"
      pattern: "import.*Port.*InferService.*from"
---

<objective>
Define config types and branded error types for unified createAdapter API.

Purpose: Establish the type foundation that enforces mutual exclusion between `factory` and `class` properties at compile time, with clear error messages when violations occur.

Output: `unified-types.ts` with config interfaces and branded error types.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-unified-createadapter/09-CONTEXT.md
@.planning/phases/09-unified-createadapter/09-RESEARCH.md
@packages/core/src/adapters/types.ts
@packages/core/src/adapters/constants.ts
@packages/graph/src/validation/types/error-messages.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded error types for mutual exclusion</name>
  <files>packages/core/src/adapters/unified-types.ts</files>
  <action>
Create `unified-types.ts` with branded error types for config validation:

1. `BothFactoryAndClassError` - branded error type when both factory and class provided:

   ```typescript
   export type BothFactoryAndClassError = {
     readonly __error: "BothFactoryAndClassError";
     readonly __hint: "Provide either 'factory' or 'class', not both. Use 'factory' for custom instantiation logic, 'class' for constructor injection.";
   };
   ```

2. `NeitherFactoryNorClassError` - branded error type when neither provided:
   ```typescript
   export type NeitherFactoryNorClassError = {
     readonly __error: "NeitherFactoryNorClassError";
     readonly __hint: "Must provide either 'factory' (function that creates instance) or 'class' (constructor for dependency injection).";
   };
   ```

Follow the pattern from `error-messages.ts` in graph package. Use readonly properties for immutability.
</action>
<verify>Run `pnpm typecheck` - file should compile without errors</verify>
<done>Two branded error types exported with descriptive **error and **hint properties</done>
</task>

<task type="auto">
  <name>Task 2: Create base and variant config types</name>
  <files>packages/core/src/adapters/unified-types.ts</files>
  <action>
Add config interfaces to `unified-types.ts`:

1. `BaseUnifiedConfig<TProvides, TRequires>` - shared properties:
   - `provides: TProvides` - the port being implemented
   - `requires?: TRequires` - optional, defaults to [] at runtime
   - `lifetime?: Lifetime` - optional, defaults to 'singleton' at runtime
   - `clonable?: boolean` - optional, defaults to false at runtime
   - `finalizer?: (instance: InferService<TProvides>) => void | Promise<void>`

2. `FactoryConfig<TProvides, TRequires, TFactory>` - factory variant:
   - Extends `BaseUnifiedConfig<TProvides, TRequires>`
   - `factory: TFactory` - required factory function
   - `class?: never` - explicitly disallowed (enables mutual exclusion)

3. `ClassConfig<TProvides, TRequires, TClass>` - class variant:
   - Extends `BaseUnifiedConfig<TProvides, TRequires>`
   - `class: TClass` - required class constructor
   - `factory?: never` - explicitly disallowed (enables mutual exclusion)

Import types from:

- `Port`, `InferService` from `../ports/types.js`
- `Lifetime`, `ResolvedDeps` from `./types.js`
- `TupleToUnion` from `../utils/type-utilities.js`

Use `readonly` for all properties. Use `const TRequires extends readonly Port<unknown, string>[]` pattern for tuple inference.
</action>
<verify>Run `pnpm typecheck` - no errors. Types should be exported correctly.</verify>
<done>BaseUnifiedConfig, FactoryConfig, and ClassConfig types exported with proper constraints</done>
</task>

<task type="auto">
  <name>Task 3: Create placeholder unified.ts and add PortsToServices helper</name>
  <files>packages/core/src/adapters/unified.ts</files>
  <action>
Create `unified.ts` with:

1. Module documentation explaining this will contain unified createAdapter
2. Import the types from `unified-types.js`
3. Add `PortsToServices<T>` type (copy from service.ts lines 493-495):
   ```typescript
   export type PortsToServices<T extends readonly Port<unknown, string>[]> = {
     [K in keyof T]: T[K] extends Port<infer S, string> ? S : never;
   };
   ```
4. Export placeholder comment `// createAdapter implementation will be added in 09-02`

This prepares the file structure. The actual overloads come in Plan 02.
</action>
<verify>Run `pnpm typecheck` - file should compile without errors</verify>
<done>unified.ts exists with PortsToServices type and imports from unified-types.ts</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes with no errors in core package
- `unified-types.ts` exports: BothFactoryAndClassError, NeitherFactoryNorClassError, BaseUnifiedConfig, FactoryConfig, ClassConfig
- `unified.ts` exists and imports from unified-types.ts
</verification>

<success_criteria>

1. Config types correctly constrain factory/class mutual exclusion via `?: never` pattern
2. Branded error types have actionable \_\_hint messages
3. All types use proper generics for Port, Lifetime, and boolean literals
4. Files compile without TypeScript errors
   </success_criteria>

<output>
After completion, create `.planning/phases/09-unified-createadapter/09-01-SUMMARY.md`
</output>
