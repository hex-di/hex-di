---
phase: 09-unified-createadapter
plan: 06
type: execute
wave: 4
depends_on: ["09-04", "09-05"]
files_modified:
  - packages/core/src/adapters/index.ts
  - packages/core/src/index.ts
autonomous: true

must_haves:
  truths:
    - "createAdapter is exported from @hex-di/core"
    - "Old createAdapter from factory.ts remains available (not yet removed)"
    - "Unified types are exported for advanced users"
    - "All existing tests continue to pass"
  artifacts:
    - path: "packages/core/src/adapters/index.ts"
      provides: "Barrel exports including unified createAdapter"
      contains: "export { createAdapter }"
    - path: "packages/core/src/index.ts"
      provides: "Package public API"
      contains: "createAdapter"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/adapters/unified.ts"
      via: "re-export"
      pattern: "export.*from.*unified"
---

<objective>
Export unified createAdapter from package public API.

Purpose: Make the new unified createAdapter available to users while maintaining backward compatibility with existing code until Phase 11 removes old APIs.

Output: Updated exports in adapters/index.ts and core/index.ts.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-unified-createadapter/09-CONTEXT.md
@packages/core/src/adapters/index.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update adapters barrel export</name>
  <files>packages/core/src/adapters/index.ts</files>
  <action>
Update `packages/core/src/adapters/index.ts` to export from unified module:

1. Add exports from unified.ts:

   ```typescript
   export { createAdapter as createUnifiedAdapter, PortsToServices } from "./unified.js";
   ```

   Note: Export as `createUnifiedAdapter` for now to avoid conflict with existing `createAdapter` from factory.ts. Phase 11 will remove old APIs and this can be renamed.

2. Add exports from unified-types.ts (for advanced users who need the config types):
   ```typescript
   export type {
     BaseUnifiedConfig,
     FactoryConfig,
     ClassConfig,
     BothFactoryAndClassError,
     NeitherFactoryNorClassError,
   } from "./unified-types.js";
   ```

Keep all existing exports intact. This is additive only.
</action>
<verify>Run `pnpm typecheck` - no errors. Exports should resolve.</verify>
<done>Adapters barrel exports unified createAdapter and types</done>
</task>

<task type="auto">
  <name>Task 2: Update package public API and run full test suite</name>
  <files>packages/core/src/index.ts</files>
  <action>
1. Update `packages/core/src/index.ts` to re-export unified createAdapter:
   - The adapters index.ts already re-exports, so if index.ts does `export * from "./adapters/index.js"`, no changes needed
   - If selective exports, add: `export { createUnifiedAdapter } from "./adapters/index.js"`

2. Verify all exports are accessible:
   - `createAdapter` (from factory.ts - existing)
   - `createUnifiedAdapter` (from unified.ts - new)
   - `createAsyncAdapter` (from factory.ts - existing)
   - `createClassAdapter` (from service.ts - existing)
   - Config types from unified-types.ts

3. Run full test suite to ensure nothing is broken:
   - `pnpm typecheck`
   - `pnpm test`
   - `pnpm test:types`

Note: The goal is additive. Old APIs remain unchanged. Phase 11 will handle removal.
</action>
<verify>Run `pnpm typecheck && pnpm test && pnpm test:types` - all pass</verify>
<done>Unified createAdapter exported from @hex-di/core, all tests pass</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes across all packages
- `pnpm test` passes across all packages
- `pnpm test:types` passes across all packages
- `createUnifiedAdapter` is importable from `@hex-di/core`
- Existing `createAdapter` still works (backward compatible)
</verification>

<success_criteria>

1. `import { createUnifiedAdapter } from "@hex-di/core"` works
2. All existing tests continue to pass
3. No breaking changes to existing public API
4. Type exports available for advanced users
   </success_criteria>

<output>
After completion, create `.planning/phases/09-unified-createadapter/09-06-SUMMARY.md`
</output>
