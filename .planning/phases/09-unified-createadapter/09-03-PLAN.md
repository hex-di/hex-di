---
phase: 09-unified-createadapter
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/core/src/adapters/unified.ts
autonomous: true

must_haves:
  truths:
    - "createAdapter with class property creates adapter with constructor injection"
    - "Constructor parameters are injected in requires array order"
    - "Class-based adapter supports all lifetimes (singleton, scoped, transient)"
    - "Class instance type is validated against provides port type"
  artifacts:
    - path: "packages/core/src/adapters/unified.ts"
      provides: "Class-based createAdapter overloads"
      exports: ["createAdapter"]
      min_lines: 250
  key_links:
    - from: "packages/core/src/adapters/unified.ts"
      to: "packages/core/src/adapters/service.ts"
      via: "extractServicesInOrder pattern"
      pattern: "requires\\.map.*port.*__portName"
---

<objective>
Implement class-based createAdapter overloads with constructor injection.

Purpose: Enable users to create adapters from class constructors using the same createAdapter API. Dependencies are injected into the constructor in the order specified by the requires array.

Output: Working createAdapter function for class-based adapters integrated with factory variant.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-unified-createadapter/09-CONTEXT.md
@.planning/phases/09-unified-createadapter/09-RESEARCH.md
@packages/core/src/adapters/service.ts
@packages/core/src/adapters/unified.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add class-based overloads</name>
  <files>packages/core/src/adapters/unified.ts</files>
  <action>
Add class-based overloads to createAdapter:

1. **Overload: Class with all defaults** (no lifetime, no clonable)
   - Config has: `provides`, `class`, optionally `requires`, `finalizer`
   - `requires` defaults to `[]`, `lifetime` defaults to `"singleton"`, `clonable` defaults to `false`
   - Class constructor type: `new (...args: PortsToServices<TRequires>) => InferService<TProvides>`
   - Returns: Adapter with factoryKind "sync" (class instantiation is always sync)

2. **Overload: Class with explicit requires** (no lifetime, no clonable)
   - Config has: `provides`, `requires`, `class`
   - Preserves requires tuple type

3. **Overload: Class with explicit lifetime** (no clonable)
   - Config has: `provides`, `class`, `lifetime`, optionally `requires`
   - Preserves lifetime literal type

4. **Overload: Class with explicit clonable**
   - Config has: `provides`, `class`, `clonable`
   - Preserves clonable literal type

5. **Overload: Class full explicit** (requires + lifetime + clonable)
   - All properties explicitly provided
   - Preserves all literal types

Key constraint: Class-based adapters are ALWAYS sync (factoryKind: "sync"). The class constructor is called synchronously, unlike factory functions which can be async.

Use `PortsToServices<TRequires>` from this file (added in 09-01) to type the constructor parameters.
</action>
<verify>Run `pnpm typecheck` - class overloads should compile alongside factory overloads</verify>
<done>Class overloads defined with proper constructor parameter typing</done>
</task>

<task type="auto">
  <name>Task 2: Implement class variant in createAdapter</name>
  <files>packages/core/src/adapters/unified.ts</files>
  <action>
Update createAdapter implementation to handle class configs:

1. Add `extractServicesInOrder` helper (copy pattern from service.ts lines 501-510):

   ```typescript
   function extractServicesInOrder(
     deps: Record<string, unknown>,
     requires: readonly Port<unknown, string>[]
   ): unknown[] {
     return requires.map(port => deps[port.__portName]);
   }
   ```

2. Update implementation to check for `config.class`:

   ```typescript
   if (config.class !== undefined) {
     const ClassConstructor = config.class;
     const factory = (deps: Record<string, unknown>): unknown => {
       const args = extractServicesInOrder(deps, requires);
       return new ClassConstructor(...args);
     };
     // ... rest of adapter construction with factory
   }
   ```

3. Remove the temporary "not implemented" error from 09-02

4. Add runtime validation:
   - If both `config.factory` and `config.class` provided, throw TypeError with HEX error code
   - If neither provided, throw TypeError with HEX error code
   - Error messages should match the branded error type hints

5. For class variant, factoryKind is always SYNC (class instantiation cannot be async)
   </action>
   <verify>Run `pnpm typecheck` - implementation compiles. No runtime tests yet.</verify>
   <done>createAdapter handles both factory and class configs with proper runtime validation</done>
   </task>

</tasks>

<verification>
- `pnpm typecheck` passes
- createAdapter handles both factory and class in the implementation
- Runtime error thrown when both factory and class provided
- Runtime error thrown when neither factory nor class provided
- Class-based adapters always have factoryKind "sync"
</verification>

<success_criteria>

1. `createAdapter({ provides: port, class: MyClass, requires: [...] })` compiles and works
2. Constructor receives dependencies in requires array order
3. Class-based adapter is always sync (factoryKind: "sync")
4. Runtime errors match branded error type messages
   </success_criteria>

<output>
After completion, create `.planning/phases/09-unified-createadapter/09-03-SUMMARY.md`
</output>
