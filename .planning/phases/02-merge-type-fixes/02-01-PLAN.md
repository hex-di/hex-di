---
phase: 02-merge-type-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true

must_haves:
  truths:
    - "After merge, ports from either parent graph can be overridden (no type error)"
    - "UnsafeDepthOverride flag preserved using OR semantics (true if either input has it)"
    - "merge-parent-provides.test-d.ts passes all 6 tests"
    - "merge-unsafe-override-preservation.test-d.ts passes all 7 tests"
  artifacts:
    - path: "packages/graph/src/builder/types/state.ts"
      provides: "UnifiedMergeInternals type with correct merge logic"
      contains: "MergeParentProvides<GetParentProvides<T1>, GetParentProvides<T2>>"
    - path: "packages/graph/src/builder/types/state.ts"
      provides: "BoolOr utility for flag merging"
      contains: "BoolOr<GetUnsafeDepthOverride<T1>, GetUnsafeDepthOverride<T2>>"
  key_links:
    - from: "UnifiedMergeInternals"
      to: "MergeParentProvides"
      via: "line 612 in state.ts"
      pattern: "MergeParentProvides<GetParentProvides<T1>, GetParentProvides<T2>>"
    - from: "UnifiedMergeInternals"
      to: "BoolOr"
      via: "line 614 in state.ts"
      pattern: "BoolOr<GetUnsafeDepthOverride<T1>, GetUnsafeDepthOverride<T2>>"
---

<objective>
Verify MERGE-01 and MERGE-02 requirements are implemented correctly and update planning documentation.

Purpose: Confirm that graph merge operations preserve all type-level metadata from both input graphs. Based on research findings, the implementation already exists and tests pass - this is verification work.

Output: Verified requirements with updated planning documentation marking Phase 2 complete.
</objective>

<execution_context>
@/Users/u1070457/.claude/get-shit-done/workflows/execute-plan.md
@/Users/u1070457/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@packages/graph/src/builder/types/state.ts
@packages/graph/tests/merge-parent-provides.test-d.ts
@packages/graph/tests/merge-unsafe-override-preservation.test-d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify MERGE-01 and MERGE-02 implementations</name>
  <files>packages/graph/tests/merge-parent-provides.test-d.ts, packages/graph/tests/merge-unsafe-override-preservation.test-d.ts</files>
  <action>
Run the type tests to verify both merge requirements are implemented:

1. Run `pnpm --filter @hex-di/graph test:types` and verify:
   - merge-parent-provides.test-d.ts passes all 6 tests (MERGE-01)
   - merge-unsafe-override-preservation.test-d.ts passes all 7 tests (MERGE-02)

2. Verify implementation in state.ts:
   - Line 612 uses `MergeParentProvides<GetParentProvides<T1>, GetParentProvides<T2>>` (MERGE-01)
   - Line 614 uses `BoolOr<GetUnsafeDepthOverride<T1>, GetUnsafeDepthOverride<T2>>` (MERGE-02)

3. Confirm test coverage includes:
   - MERGE-01: Tests for merging child builders from different parents, override capability from both
   - MERGE-02: Tests for T1+T2 override combinations (true+false, false+true, true+true, false+false)

Expected: All tests pass, implementation matches requirements.
</action>
<verify>
`pnpm --filter @hex-di/graph test:types` shows:

- merge-parent-provides.test-d.ts (6 tests) - PASS
- merge-unsafe-override-preservation.test-d.ts (7 tests) - PASS
- "Type Errors: no errors"
  </verify>
  <done>Both MERGE-01 and MERGE-02 implementations verified working via 13 passing type tests</done>
  </task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md to mark MERGE-01 and MERGE-02 complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit .planning/REQUIREMENTS.md:

1. Change `- [ ] **MERGE-01**:` to `- [x] **MERGE-01**:`
2. Change `- [ ] **MERGE-02**:` to `- [x] **MERGE-02**:`
3. Update traceability table:
   - MERGE-01 row: Status from "Pending" to "Complete"
   - MERGE-02 row: Status from "Pending" to "Complete"
4. Update "Last updated" date at bottom

Commit with message: `docs(02): mark MERGE-01 and MERGE-02 requirements complete`
</action>
<verify>REQUIREMENTS.md shows both MERGE-01 and MERGE-02 with [x] checkbox and "Complete" status</verify>
<done>REQUIREMENTS.md updated with both merge requirements marked complete</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md and STATE.md for Phase 2 completion</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
Edit .planning/ROADMAP.md:

1. Change Phase 2 header from `- [ ]` to `- [x]`
2. Update Plans: Change "TBD" to "1 plan"
3. Update plan list: Change `- [ ] 02-01: TBD during plan-phase` to `- [x] 02-01-PLAN.md — Verification of MERGE-01 and MERGE-02`
4. Update progress table:
   - Phase 2 row: Plans Complete "1/1", Status "Complete", Completed date "2026-02-01"

Edit .planning/STATE.md:

1. Update "Phase: 2 of 2" to "All phases complete"
2. Update "Status: Ready to plan" to "Status: Complete"
3. Update "Progress: 50%" to "Progress: 100%"
4. Update "Last activity" to reflect completion
5. Add Phase 2 to Performance Metrics table

Commit with message: `docs(02): complete Phase 2 - Merge Type Fixes verified`
</action>
<verify>

- ROADMAP.md shows Phase 2 complete with [x] checkbox and completion date
- STATE.md shows 100% progress and "All phases complete"
  </verify>
  <done>ROADMAP.md and STATE.md updated to reflect Phase 2 and milestone completion</done>
  </task>

</tasks>

<verification>
Phase verification checklist:

1. Type tests pass:
   - `pnpm --filter @hex-di/graph test:types` shows merge-parent-provides.test-d.ts and merge-unsafe-override-preservation.test-d.ts passing

2. Implementation confirmed:
   - UnifiedMergeInternals uses MergeParentProvides for parentProvides (MERGE-01)
   - UnifiedMergeInternals uses BoolOr for UnsafeDepthOverride (MERGE-02)

3. Documentation updated:
   - REQUIREMENTS.md: MERGE-01 and MERGE-02 marked complete
   - ROADMAP.md: Phase 2 marked complete
   - STATE.md: 100% progress, milestone complete
     </verification>

<success_criteria>

- All 13 type tests pass (6 for MERGE-01, 7 for MERGE-02)
- REQUIREMENTS.md shows [x] for both MERGE-01 and MERGE-02
- ROADMAP.md shows Phase 2 complete with date
- STATE.md shows 100% progress and milestone complete
- No code changes needed - verification only confirms existing implementation
  </success_criteria>

<output>
After completion, create `.planning/phases/02-merge-type-fixes/02-01-SUMMARY.md`
</output>
